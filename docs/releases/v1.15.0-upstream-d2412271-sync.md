# gsd-hermes v1.15.0 — upstream sync to d2412271

## Summary

`gsd-hermes@1.15.0` syncs downstream `pingchesu/gsd-hermes` with upstream `gsd-build/get-shit-done@d2412271`.

This is a downstream packaging/release sync. It keeps the package identity as `gsd-hermes`, keeps the Hermes-first install path, and preserves strict provider-routed agent execution semantics.

## Upstream range

- Previous downstream sync target: `73f7ad33`
- New upstream sync target: `d2412271`
- Upstream range: `73f7ad33..d2412271`

## Notable upstream changes included

- Windows persistent SDK shim and legacy `gsd-tools.cjs` shim replacement.
- Bounded git subprocess timeouts for worktree health checks.
- Phase-plan index wave grouping by dependency DAG, including wave `0` preservation.
- Workstream migration name normalization and deeper phase lifecycle seams.
- Code-review SUMMARY parser hardening for critical/blocker parsing.
- State frontmatter fixes for curated progress preservation, percent formula, nested plan counting, and YAML canonical field preference.
- Shared model catalog source of truth for agent profiles and runtime tier defaults.
- `config set` support for `model_overrides.<agent-id>`.
- Package legitimacy gate against slopsquatting.
- SDK and query-handler hardening across install, phase, skill, state, and workstream paths.

## Downstream preservation notes

- Package name remains `gsd-hermes`.
- Repository metadata remains `pingchesu/gsd-hermes`.
- `hermes/*` model overrides remain routed through Hermes without passing `--provider`.
- Unknown agents remain unsupported/fail-fast rather than silently falling back to Claude/Codex aliases.
- Hermes project-linked install semantics remain flat under `./.gsd-hermes/skills`.

## Validation

Local validation for this release should include:

```bash
env -u NODE_OPTIONS npm test
env -u NODE_OPTIONS npm pack --dry-run
```

Publish validation should use GitHub Actions Trusted Publishing:

1. Trusted Publishing dry-run on tag `v1.15.0`.
2. Formal Trusted Publishing publish on tag `v1.15.0`.
3. Verify `npm view gsd-hermes@1.15.0 version` and `npm view gsd-hermes@latest version`.
