# gsd-hermes v1.6.0 — Upstream Main 9472f343 Sync

## Summary

`gsd-hermes@1.6.0` syncs the downstream fork from upstream base `f3685d91` to `upstream/main@9472f343` while preserving Hermes Agent install behavior, dash-form skill discovery, downstream package identity, and strict runtime/model binding receipts.

## Upstream changes imported

- `--minimal` / `--core-only` install profile support for smaller core-only installs.
- `/gsd-edit-phase` command support.
- Phase lifecycle hardening for roadmap parsing, phase directory discovery, and milestone state updates.
- Workstream-aware config/query threading and model resolver handling.
- Expanded runtime profile aliases for Gemini, Qwen, OpenCode, and Copilot.
- Codex agent/hook TOML format fixes for newer Codex versions.
- Graphify CLI invocation updates and post-merge build/test gate workflow coverage.
- Parser fixes for quoted/unquoted dependency and colon-containing values.

## Downstream preservation

- Package identity remains `gsd-hermes`.
- Hermes install support remains available via `--hermes` global and project-linked modes.
- Hermes local mode still uses `skills.external_dirs` rather than claiming a native local skill-install mode.
- Explicit Hermes per-agent model overrides remain strict: run exactly as configured or fail fast with actionable diagnostics.
- Runtime/model receipts remain resolver/handoff proof; provider wire-level enforcement is not overclaimed.

## Verification

- Local validation passed: `npm run test:hermes`, `npm test`, `npm run lint:tests`, and `npm pack --dry-run --json`.
- PR CI pending after branch push.

## Release

- npm package: `gsd-hermes@1.6.0`
- GitHub Release: `v1.6.0`
- Upstream target: `9472f343`
