# Upstream Sync Log: 2026-04 -> upstream/main@0a049149

## Metadata

| Field | Value |
|-------|-------|
| Sync date | 2026-04-XX (populated during execution) |
| Upstream commit pinned | `0a049149e1e9a25d522a6f583fbb69bfc6dc5d2b` (`0a049149`) |
| Upstream commit range | `v1.38.2` (`7397f580`) .. `upstream/main` (`0a049149`) — 97 commits |
| Previous sync base | `get-shit-done-cc@1.38.2` (synced in v1.1 via PR #12) |
| Downstream target version | `gsd-hermes@1.3.0` (metadata update = Phase 8 REL-03) |
| Sync branch | `sync/upstream-0a049149` |
| PR reference | #TBD (opened in Phase 9 REL-04) |
| Merge commit | TBD (populated after `git commit` completes merge) |
| Merge strategy | `git merge --allow-unrelated-histories upstream/main` (v1.1 squash left no common ancestor) |
| .gitattributes active | `package.json merge=ours` landed on main at commit TBD before sync branch diverged |

## Summary

- Files changed in upstream range: 443
- Files changed in Hermes delta: 45
- Files requiring merge resolution: 29
- Conflict hunks resolved: TBD (populated during execution)
- `bin/install.js` hermes ref count before -> after: 147 -> TBD (acceptance near 147)

## Conflict Classification (Per-Hunk)

### Legend
- **Owner** — one of: `upstream base` | `Hermes adapter seam` | `downstream governance`
- **Resolution** — one of: `took upstream` | `kept ours` | `merged: <description>`
- **Risk** — Phase 7 impact note (or `none`)

### bin/install.js (primary Hermes adapter seam)

| file:line | Owner | Resolution | Risk |
|-----------|-------|------------|------|
| TBD | | | |

### package.json + dependency reconciliation (D-12 explicit commit)

| file:line | Owner | Resolution | Risk |
|-----------|-------|------------|------|
| TBD | | | |

### Other files

| file:line | Owner | Resolution | Risk |
|-----------|-------|------------|------|
| TBD | | | |

## Deferred Decisions

- TBD (populated during execution — candidates from RESEARCH: `bin.gsd-sdk` addition, `files[].sdk/dist`, `esbuild` removal)

## Verification Evidence

- `node -p "require('./package.json').name"` -> TBD (acceptance: `gsd-hermes`)
- `node -p "require('./package.json').bin['gsd-hermes']"` -> TBD (acceptance: `bin/install.js`)
- `git grep -c "hermes\|Hermes" bin/install.js` -> TBD (acceptance near 147)
- `npm run test:hermes` -> TBD (acceptance: exit 0)
- Manual runtime-routing review:
  - `runtime === 'hermes'` branches present: TBD
  - `--hermes` flag parsing present: TBD
  - `~/.hermes` path logic present: TBD
- `ls .planning/phases/` confirms only `06-upstream-merge-baseline/`: TBD

## Operator Notes

(Free-form section for anything that didn't fit the table; e.g., surprises, red flags, Phase 7 follow-up hints. Populated during P3/P4 execution.)
