---
"gsd-hermes": minor
---

Sync downstream `gsd-hermes` to `gsd-build/get-shit-done@d2412271` while preserving Hermes-first package identity, project-linked install semantics, and strict provider-routed `model_overrides` execution. This sync also keeps unknown-agent model resolution fail-fast instead of silently falling back to Claude aliases.
