---
type: Added
---
Add Hermes-native provider-routing drivers so explicit `model_overrides` can run through `hermes chat` or `hermes chat --toolsets terminal,file` without silently falling back to Claude/Codex CLIs.
