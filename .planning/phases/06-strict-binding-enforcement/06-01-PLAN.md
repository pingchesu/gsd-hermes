---
phase: 06-strict-binding-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/src/query/runtime-model-contract.ts
  - sdk/src/query/runtime-model-validation.ts
  - sdk/src/query/config-query.ts
  - sdk/src/query/init.ts
  - sdk/src/query/config-query.test.ts
  - sdk/src/query/init.test.ts
autonomous: true
requirements:
  - SBV-01
  - SBV-02
  - SBV-03
  - SBV-04
user_setup: []
must_haves:
  truths:
    - Fail-fast validation is introduced only at real run entry points in this phase; config editing flows such as config-set remain non-blocking.
    - The validator consumes Phase 5 binding semantics and preserves explicit, profile-derived, inherit, and runtime-default outcomes instead of flattening them into token strings.
    - 'Unsupported explicit overrides and unsupported profile-derived resolved models fail before planning or execution begins, while inherit and resolve_model_ids: "omit" remain valid by themselves.'
    - Every blocking error exposes the agent, runtime, configured or resolved model, rejection reason, suggested fix, and whether cross_ai_execution is an available alternative without auto-routing into it.
    - Legacy configurations remain allowed unless they explicitly request an unsupported runtime/model binding.
  artifacts:
    - sdk/src/query/runtime-model-validation.ts
    - sdk/src/query/runtime-model-contract.ts
    - sdk/src/query/init.ts
    - sdk/src/query/config-query.test.ts
    - sdk/src/query/init.test.ts
  key_links:
    - sdk/src/query/runtime-model-validation.ts imports and evaluates resolveAgentBinding results from sdk/src/query/runtime-model-contract.ts instead of re-deriving resolution rules.
    - sdk/src/query/init.ts uses one shared assertion path for init.plan-phase and init.execute-phase so workflow entry failures stay consistent.
    - sdk/src/query/config-query.test.ts and sdk/src/query/init.test.ts lock the same diagnostic contract used by the runtime validator.
---

<objective>
Add the shared strict-binding validator and enforce it at plan/execute workflow init boundaries.

Purpose: Phase 6 begins by converting the Phase 5 contract into a real fail-fast gate exactly where planning and execution begin, while preserving compatibility for inherit and runtime-default behavior.
Output: A reusable validation module, runtime compatibility policy wired into the contract surface, blocking init.plan-phase/init.execute-phase checks, and tests that prove actionable diagnostics and migration-safe exceptions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-strict-binding-enforcement/06-CONTEXT.md
@.planning/phases/06-strict-binding-enforcement/06-RESEARCH.md
@.planning/phases/06-strict-binding-enforcement/06-PATTERNS.md
@.planning/phases/06-strict-binding-enforcement/06-VALIDATION.md
@.planning/phases/05-runtime-model-contract/05-01-SUMMARY.md
@.planning/phases/05-runtime-model-contract/05-02-SUMMARY.md
@sdk/src/query/runtime-model-contract.ts
@sdk/src/query/config-query.ts
@sdk/src/query/init.ts
@sdk/src/query/config-query.test.ts
@sdk/src/query/init.test.ts
@docs/CONFIGURATION.md
@docs/USER-GUIDE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add a shared strict-binding validation layer on top of the Phase 5 contract</name>
  <files>sdk/src/query/runtime-model-validation.ts, sdk/src/query/runtime-model-contract.ts, sdk/src/query/config-query.ts</files>
  <read_first>sdk/src/query/runtime-model-contract.ts, sdk/src/query/config-query.ts, .planning/phases/06-strict-binding-enforcement/06-CONTEXT.md, .planning/phases/06-strict-binding-enforcement/06-RESEARCH.md</read_first>
  <action>Create sdk/src/query/runtime-model-validation.ts as the single reusable enforcement layer for Phase 6. It must accept resolveAgentBinding output and return structured validation results for one agent or a set of agents, plus a formatter/assertion helper that can throw one operator-facing GSDError. Extend the contract/policy surface only as needed to represent actual runtime/model compatibility rather than the current all-supported placeholder behavior. Keep the compatibility policy conservative: inherit and runtime-default stay valid, unknown-agent remains unsupported, and blocking only occurs when an explicit override or profile-derived resolved model is incompatible with the active runtime. Make the result shape include at minimum agent, runtime, binding kind/source, configured model, resolved model, rejection reason, suggested fix, cross_ai_execution support/configuration signal, and whether cross-AI is merely a recommendation. Update config-query exports only if needed so downstream callers can access the shared validator without re-encoding message strings.</action>
  <verify>cd /home/whiskey/workspace/project/central/v2/gsd-hermes/sdk && npm test -- config-query</verify>
  <acceptance_criteria>
    - sdk/src/query/runtime-model-validation.ts exists and exposes reusable validate/assert/format helpers for strict binding enforcement.
    - The validator consumes resolveAgentBinding() results rather than duplicating override/profile/omit/inherit precedence.
    - The compatibility policy blocks unsupported explicit or profile-derived bindings for the active runtime but does not error on inherit or resolve_model_ids: "omit" by themselves.
    - The formatted failure includes agent, runtime, configured or resolved model, reason, suggested fix, and cross-AI alternative status.
    - No Task 1 change adds blocking validation to config-set or other config mutation flows.
  </acceptance_criteria>
  <done>Phase 6 has one shared validation contract that can reject unsupported bindings consistently before any agent session starts.</done>
</task>

<task type="auto">
  <name>Task 2: Fail fast in init.plan-phase and init.execute-phase with operator-friendly diagnostics</name>
  <files>sdk/src/query/init.ts, sdk/src/query/runtime-model-validation.ts, sdk/src/query/config-query.ts</files>
  <read_first>sdk/src/query/init.ts, sdk/src/query/runtime-model-validation.ts, .planning/phases/06-strict-binding-enforcement/06-CONTEXT.md, .planning/phases/06-strict-binding-enforcement/06-VALIDATION.md</read_first>
  <action>Wire the shared validator into the two actual workflow entry seams for this phase. initPlanPhase must validate the planning agent set: gsd-phase-researcher, gsd-planner, and gsd-plan-checker only when workflow.plan_check is enabled. initExecutePhase must validate the execution agent set: gsd-executor and gsd-verifier only when workflow.verifier is enabled. On failure, reject the init query before returning workflow payload data, using one structured GSDError path instead of ad hoc strings. Keep current outward payload fields intact for valid configurations and do not auto-route through cross_ai_execution; only mention it when the active runtime could use it as an explicit next step. Make sure the entry gate still preserves the difference between explicit, profile-derived, inherit, and runtime-default internally even if outward model tokens stay empty for inherit/runtime-default success cases.</action>
  <verify>cd /home/whiskey/workspace/project/central/v2/gsd-hermes/sdk && npm test -- init config-query</verify>
  <acceptance_criteria>
    - initPlanPhase rejects unsupported planner/researcher/checker bindings before returning a plan payload.
    - initExecutePhase rejects unsupported executor/verifier bindings before returning an execute payload.
    - Optional agents are gated only when their workflow toggle is enabled.
    - Blocking errors originate from one shared structured validation path and remain descriptive rather than terse.
    - Valid inherit and runtime-default configurations continue returning successful init payloads with omission semantics.
  </acceptance_criteria>
  <done>Normal plan-phase and execute-phase workflow entry points now fail fast on unsupported bindings with actionable diagnostics.</done>
</task>

<task type="auto">
  <name>Task 3: Add SDK tests covering strict validation, diagnostics, and migration-safe exceptions</name>
  <files>sdk/src/query/config-query.test.ts, sdk/src/query/init.test.ts</files>
  <read_first>sdk/src/query/config-query.test.ts, sdk/src/query/init.test.ts, .planning/phases/06-strict-binding-enforcement/06-VALIDATION.md</read_first>
  <action>Add table-driven tests that prove the new validator and init gates enforce SBV-01 through SBV-04. Include unsupported explicit override cases, unsupported profile-derived cases, runtime-default omission success, inherit success, and cross_ai_execution recommendation text without auto-fallback. Cover both planning and execution entry sets. Use realistic fixture combinations from the research notes, such as codex plus balanced profile resolving to Claude IDs, codex plus resolve_model_ids:"omit" remaining valid, and codex plus a runtime-compatible openai override remaining valid. Assert that error messages or structured payload metadata include agent, runtime, configured/resolved model, reason, suggested fix, and cross-AI hint fields.</action>
  <verify>cd /home/whiskey/workspace/project/central/v2/gsd-hermes/sdk && npm test -- config-query init</verify>
  <acceptance_criteria>
    - sdk/src/query/config-query.test.ts covers unsupported explicit bindings, unsupported profile-derived bindings, inherit success, and runtime-default success.
    - sdk/src/query/init.test.ts proves init.plan-phase and init.execute-phase fail before payload return when bindings are unsupported.
    - Tests assert diagnostics include agent, runtime, model, reason, suggested fix, and cross-AI alternative visibility.
    - At least one test locks the migration-safe case where a non-Claude runtime with resolve_model_ids: "omit" still passes.
    - No new test encodes Phase 7 routing behavior or claims automatic cross-AI execution.
  </acceptance_criteria>
  <done>Phase 6 entry-gate enforcement is covered by targeted SDK tests and preserves the intended migration boundary.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] cd /home/whiskey/workspace/project/central/v2/gsd-hermes/sdk && npm test -- config-query init
- [ ] Unsupported explicit model bindings fail before init.plan-phase or init.execute-phase returns success
- [ ] Unsupported profile-derived bindings fail before init.plan-phase or init.execute-phase returns success
- [ ] inherit and resolve_model_ids: "omit" remain non-blocking when no unsupported explicit binding is requested
- [ ] Error text or structured metadata exposes agent, runtime, configured/resolved model, reason, suggested fix, and cross-AI alternative status
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Plan/execute workflow init boundaries enforce strict binding without broadening scope into config editing, cross-AI routing, or docs cleanup
- Phase 6 keeps Phase 7 and Phase 8 concerns explicitly deferred
</success_criteria>

<output>
After completion, create `.planning/phases/06-strict-binding-enforcement/06-01-SUMMARY.md`
</output>
