---
phase: 06-strict-binding-enforcement
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - sdk/src/phase-runner.ts
  - sdk/src/session-runner.ts
  - sdk/src/query/runtime-model-validation.ts
  - sdk/src/query/runtime-model-contract.ts
  - sdk/src/phase-runner.test.ts
  - sdk/src/phase-runner.integration.test.ts
  - tests/bug-1829-inherit-model-profile.test.cjs
  - tests/cross-ai-execution.test.cjs
autonomous: true
requirements:
  - SBV-01
  - SBV-02
  - SBV-03
  - SBV-04
user_setup: []
must_haves:
  truths:
    - PhaseRunner adds a second execution-time strict-binding guard so direct SDK usage cannot bypass init-layer validation.
    - Execution uses the validated per-agent binding for each step, with explicit/profile-derived models passed through and inherit/runtime-default continuing as omitted model selection.
    - session-runner changes remain minimal and correctness-focused; this phase must not become a deep backend rewrite.
    - Legacy and migration-safe configs continue to run unless they explicitly request an unsupported binding, and cross_ai_execution remains suggestion-only in Phase 6.
    - Documentation rollouts, workflow-wide propagation beyond the real entry points, and cross-AI hardening remain deferred to later phases.
  artifacts:
    - sdk/src/phase-runner.ts
    - sdk/src/session-runner.ts
    - sdk/src/phase-runner.test.ts
    - sdk/src/phase-runner.integration.test.ts
    - tests/bug-1829-inherit-model-profile.test.cjs
    - tests/cross-ai-execution.test.cjs
  key_links:
    - sdk/src/phase-runner.ts reuses the Phase 6 validator before planning and execution step groups instead of inventing a separate compatibility check.
    - sdk/src/phase-runner.ts maps concrete steps to agent bindings and passes SessionOptions.model intentionally, avoiding session-runner profile drift.
    - tests/bug-1829-inherit-model-profile.test.cjs and tests/cross-ai-execution.test.cjs confirm migration safety and scope containment at the root test layer.
---

<objective>
Add the execution-time safety guard and align runtime session model selection with the validated agent binding.

Purpose: Workflow init checks are not enough because direct SDK/programmatic entry can still reach execution. Phase 6 finishes by making PhaseRunner enforce the same policy and by applying the already-validated per-agent model token at run time without a deep session-runner rewrite.
Output: A PhaseRunner preflight for planning/execution groups, minimal model-threading changes for actual sessions, and regression tests proving fail-fast behavior plus migration-safe legacy survival.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-strict-binding-enforcement/06-CONTEXT.md
@.planning/phases/06-strict-binding-enforcement/06-RESEARCH.md
@.planning/phases/06-strict-binding-enforcement/06-PATTERNS.md
@.planning/phases/06-strict-binding-enforcement/06-VALIDATION.md
@.planning/phases/06-strict-binding-enforcement/06-01-PLAN.md
@.planning/phases/05-runtime-model-contract/05-01-SUMMARY.md
@.planning/phases/05-runtime-model-contract/05-02-SUMMARY.md
@sdk/src/query/runtime-model-contract.ts
@sdk/src/query/runtime-model-validation.ts
@sdk/src/phase-runner.ts
@sdk/src/session-runner.ts
@sdk/src/phase-runner.test.ts
@sdk/src/phase-runner.integration.test.ts
@tests/bug-1829-inherit-model-profile.test.cjs
@tests/cross-ai-execution.test.cjs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add PhaseRunner strict-binding preflight for planning and execution step groups</name>
  <files>sdk/src/phase-runner.ts, sdk/src/query/runtime-model-validation.ts</files>
  <read_first>sdk/src/phase-runner.ts, sdk/src/query/runtime-model-validation.ts, .planning/phases/06-strict-binding-enforcement/06-CONTEXT.md, .planning/phases/06-strict-binding-enforcement/06-RESEARCH.md</read_first>
  <action>Update PhaseRunner so it calls the shared Phase 6 validator before any step group that can launch agents. Add one planning gate for gsd-phase-researcher, gsd-planner, and optional gsd-plan-checker, and one execution gate for gsd-executor and optional gsd-verifier. Use the same structured diagnostics as the init-layer checks so SDK callers receive equivalent failures. Integrate the guard in a way that blocks before the first relevant session starts, including re-plan/re-check loops, but does not alter unrelated discuss or advance semantics. Keep the implementation additive and local to PhaseRunner rather than expanding into broader workflow rewrites.</action>
  <verify>cd /home/whiskey/workspace/project/central/v2/gsd-hermes/sdk && npm test -- phase-runner</verify>
  <acceptance_criteria>
    - PhaseRunner rejects unsupported planning bindings before research/plan/plan-check sessions start.
    - PhaseRunner rejects unsupported execution bindings before execute/verify sessions start.
    - Validation uses the shared Phase 6 validator and emits equivalent diagnostic detail to the init entry gates.
    - The guard does not require deep changes to discuss, advance, or unrelated workflow logic.
    - Re-plan/re-check paths remain covered by the same plan-step gate rather than opening a bypass.
  </acceptance_criteria>
  <done>Programmatic SDK execution can no longer bypass strict binding enforcement.</done>
</task>

<task type="auto">
  <name>Task 2: Thread validated per-agent model tokens into actual sessions with only minimal session-runner changes</name>
  <files>sdk/src/phase-runner.ts, sdk/src/session-runner.ts, sdk/src/query/runtime-model-contract.ts</files>
  <read_first>sdk/src/session-runner.ts, sdk/src/phase-runner.ts, sdk/src/query/runtime-model-contract.ts, .planning/phases/06-strict-binding-enforcement/06-RESEARCH.md</read_first>
  <action>Fix the Phase 6 correctness gap where session-runner still uses pre-Phase-5 generic profile mapping. In PhaseRunner, resolve the concrete agent binding for each spawned step using the canonical contract, then pass SessionOptions.model explicitly when the binding is explicit or profile-derived and pass undefined when the binding kind is inherit or runtime-default. Keep session-runner changes minimal: only remove or narrow the stale internal profile logic as necessary so an explicitly provided SessionOptions.model always wins and unvalidated profile maps do not override the validated per-agent selection. Do not redesign session-runner architecture, runtime backends, or cross-AI routing in this phase.</action>
  <verify>cd /home/whiskey/workspace/project/central/v2/gsd-hermes/sdk && npm test -- phase-runner</verify>
  <acceptance_criteria>
    - research, plan, plan_check, execute, and verify sessions each receive the validated agent-specific model behavior.
    - Explicit/profile-derived bindings propagate to SessionOptions.model intentionally instead of relying on stale generic profile mapping.
    - inherit and runtime-default continue by omitting the model field rather than passing literal inherit or an accidental Claude fallback.
    - session-runner remains minimally changed and is not deeply rewritten for this phase.
    - No Task 2 change introduces automatic cross-AI routing or broad backend restructuring.
  </acceptance_criteria>
  <done>Actual runtime sessions now use the same model decision that the validator approved.</done>
</task>

<task type="auto">
  <name>Task 3: Add runner and regression coverage for defense-in-depth and migration safety</name>
  <files>sdk/src/phase-runner.test.ts, sdk/src/phase-runner.integration.test.ts, tests/bug-1829-inherit-model-profile.test.cjs, tests/cross-ai-execution.test.cjs</files>
  <read_first>sdk/src/phase-runner.test.ts, sdk/src/phase-runner.integration.test.ts, tests/bug-1829-inherit-model-profile.test.cjs, tests/cross-ai-execution.test.cjs, .planning/phases/06-strict-binding-enforcement/06-VALIDATION.md</read_first>
  <action>Add tests showing that PhaseRunner blocks the same invalid runtime/model combinations even when init validation is bypassed. Cover both planning and execution paths, plus a successful path for resolve_model_ids:"omit" and inherit. Add or update root regressions to prove legacy-safe configs still work when they do not explicitly request unsupported bindings, and to confirm Phase 6 still only suggests cross_ai_execution rather than automatically using it. Keep any root test adjustments scoped to strict-binding enforcement and avoid turning this plan into Phase 7 hardening or Phase 8 docs rollout.</action>
  <verify>cd /home/whiskey/workspace/project/central/v2/gsd-hermes/sdk && npm test -- phase-runner && cd /home/whiskey/workspace/project/central/v2/gsd-hermes && npm test -- tests/bug-1829-inherit-model-profile.test.cjs tests/cross-ai-execution.test.cjs</verify>
  <acceptance_criteria>
    - sdk/src/phase-runner.test.ts or sdk/src/phase-runner.integration.test.ts proves the runner blocks unsupported planning bindings before any session launch.
    - sdk/src/phase-runner.test.ts or sdk/src/phase-runner.integration.test.ts proves the runner blocks unsupported execution bindings before any session launch.
    - Root regression coverage keeps inherit and resolve_model_ids: "omit" valid where no unsupported explicit binding is present.
    - Root cross-AI regression remains recommendation/config-surface scoped and does not assert automatic routing.
    - Tests verify that the model actually passed to sessions matches the validated binding for at least one explicit and one omitted case.
  </acceptance_criteria>
  <done>Defense-in-depth and migration-safety behavior are verified at both SDK and root test layers.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] cd /home/whiskey/workspace/project/central/v2/gsd-hermes/sdk && npm test -- phase-runner
- [ ] cd /home/whiskey/workspace/project/central/v2/gsd-hermes && npm test -- tests/bug-1829-inherit-model-profile.test.cjs tests/cross-ai-execution.test.cjs
- [ ] PhaseRunner blocks unsupported bindings before any agent session starts
- [ ] Validated explicit/profile-derived bindings are the ones actually passed to sessions
- [ ] inherit and resolve_model_ids: "omit" remain omission-based success paths when no unsupported explicit model is requested
- [ ] Phase 7 cross-AI hardening and Phase 8 broad docs/workflow propagation remain explicitly deferred
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Strict binding enforcement applies at both workflow init and programmatic execution entry points
- session-runner drift is neutralized with a minimal fix rather than a deep rewrite
- Phase 6 finishes without absorbing Phase 7 or Phase 8 scope
</success_criteria>

<output>
After completion, create `.planning/phases/06-strict-binding-enforcement/06-02-SUMMARY.md`
</output>
