---
phase: 04-core-workflow-parity
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - bin/install.js
  - tests/hermes-core-workflow.test.cjs
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03
user_setup: []
must_haves:
  truths:
    - "Installed Hermes workflow files do not contain executable `~/.claude` or `$HOME/.claude` references."
    - "Path replacement hardening is centralized in installer conversion code rather than broad workflow rewrites."
  artifacts:
    - path: bin/install.js
      provides: "Hermes-safe copied workflow path replacement"
      contains: "bareGlobalClaudeRegex"
    - path: tests/hermes-core-workflow.test.cjs
      provides: "installed workflow path-leak regression coverage"
      contains: "assertNoExecutableClaudePathLeaks"
---

<objective>
Harden Hermes copied workflow content so core workflow files load Hermes-owned
paths instead of stale Claude runtime paths.
</objective>

<execution_context>
@/home/whiskey/.codex/get-shit-done/workflows/execute-plan.md
@/home/whiskey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-core-workflow-parity/04-CONTEXT.md
@.planning/phases/04-core-workflow-parity/04-RESEARCH.md
@.planning/phases/04-core-workflow-parity/04-PATTERNS.md
@tests/hermes-core-workflow.test.cjs
@bin/install.js
@get-shit-done/workflows/settings.md
@get-shit-done/workflows/update.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add installed workflow leak assertions</name>
  <files>tests/hermes-core-workflow.test.cjs</files>
  <read_first>tests/hermes-core-workflow.test.cjs, .planning/phases/04-core-workflow-parity/04-VALIDATION.md, bin/install.js</read_first>
  <action>Add helper `assertNoExecutableClaudePathLeaks(rootDir)`. It must recursively scan `.md` files under `rootDir`, skip files whose relative path contains `CHANGELOG.md`, `USER-PROFILE.md`, or `backup`, read each file, and collect matches for `/(?:~|\\$HOME)\\/\\.claude\\b/g`. If any matches are found, fail with a message listing `relativePath:count` joined by commas. Add a test that runs `--hermes --global --no-sdk`, then calls `assertNoExecutableClaudePathLeaks(path.join(tmpHome, '.hermes', 'get-shit-done'))`. Add a second test that runs `--hermes --local --no-sdk`, then calls `assertNoExecutableClaudePathLeaks(path.join(tmpProject, '.gsd-hermes', 'get-shit-done'))`.</action>
  <verify>node --test tests/hermes-core-workflow.test.cjs</verify>
  <acceptance_criteria>
    - `tests/hermes-core-workflow.test.cjs` contains `function assertNoExecutableClaudePathLeaks`
    - `tests/hermes-core-workflow.test.cjs` contains `(?:~|\\$HOME)\\/\\.claude\\b`
    - `tests/hermes-core-workflow.test.cjs` contains `get-shit-done`
  </acceptance_criteria>
  <done>Tests expose stale installed workflow path leaks for Hermes fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: Replace bare Claude runtime paths during copyWithPathReplacement</name>
  <files>bin/install.js</files>
  <read_first>bin/install.js, tests/hermes-core-workflow.test.cjs, .planning/phases/04-core-workflow-parity/04-RESEARCH.md</read_first>
  <action>Inside `copyWithPathReplacement()`, in the existing non-Copilot/non-Antigravity markdown replacement block, add bare path replacement alongside the current slash-form replacements. Define `const bareGlobalClaudeRegex = /~\\/\\.claude\\b/g`, `const bareGlobalClaudeHomeRegex = /\\$HOME\\/\\.claude\\b/g`, and `const bareLocalClaudeRegex = /\\.\\/\\.claude\\b/g`. Define `const normalizedPathPrefix = pathPrefix.replace(/\\/$/, '')`. Apply `content = content.replace(bareGlobalClaudeRegex, normalizedPathPrefix)`, `content = content.replace(bareGlobalClaudeHomeRegex, normalizedPathPrefix)`, and `content = content.replace(bareLocalClaudeRegex, './' + dirName)`. Keep existing slash replacements unchanged. Do not edit workflow markdown files directly for this fix.</action>
  <verify>node --test tests/hermes-core-workflow.test.cjs</verify>
  <acceptance_criteria>
    - `bin/install.js` contains `const bareGlobalClaudeRegex = /~\\/\\.claude\\b/g`
    - `bin/install.js` contains `const bareGlobalClaudeHomeRegex = /\\$HOME\\/\\.claude\\b/g`
    - `bin/install.js` contains `const bareLocalClaudeRegex = /\\.\\/\\.claude\\b/g`
    - `bin/install.js` contains `pathPrefix.replace(/\\/$/, '')`
    - `node --test tests/hermes-core-workflow.test.cjs` exits 0
  </acceptance_criteria>
  <done>Copied Hermes workflow files no longer retain executable bare Claude runtime paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| source workflows -> installed Hermes workflows | Source files are Claude-oriented; installed output must point at Hermes runtime paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-04-03 | T | copied workflow paths | mitigate | Add bare path replacement in the central installer copy function. |
| T-04-04 | R | parity claim | mitigate | Fail tests if installed Hermes workflow files still contain executable Claude runtime paths. |
</threat_model>

<verification>
- [ ] `node --test tests/hermes-core-workflow.test.cjs`
- [ ] `node --test tests/hermes-install.test.cjs tests/hermes-project-linked.test.cjs`
</verification>

<success_criteria>
- Installed Hermes workflow trees contain no executable `~/.claude` or `$HOME/.claude` path refs.
- The fix is centralized in `bin/install.js`.
- Existing Hermes install tests still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-workflow-parity/04-02-SUMMARY.md`.
</output>
