# Phase 4: Core Workflow Parity - Discussion Log

**Mode:** `--auto`
**Date:** 2026-04-19
**Workflow:** `$gsd-discuss-phase 4 --auto`

## Auto Mode Note

No interactive questions were asked. Per `--auto`, all identified gray areas
were selected and the recommended/default option was chosen for each area.

## Gray Areas Discussed

| Area | Selected Option | Rationale |
| --- | --- | --- |
| Core workflow smoke ladder | Staged lifecycle smoke from read-only to mutating commands | Minimizes risk while proving the actual GSD loop, not just command discovery. |
| Runtime validation boundary | Deterministic CI tests plus optional/probe-gated real Hermes smoke | Keeps tests reliable without requiring Hermes in every environment. |
| Workflow content compatibility | Patch executable blockers through adapter/shim layers first | Preserves upstream workflow structure and reduces sync drift. |
| Degraded-path policy | Explicit Hermes guidance or fail-fast behavior | FLOW-03 requires intentional fallback instead of silent failure. |
| Fixture isolation | Temporary `HOME` and project directories for mutating tests | Prevents accidental writes to real `~/.hermes` or this repo's `.planning/`. |
| Scope boundary | Core lifecycle only; lifecycle tooling and broad parity later | Keeps Phase 4 focused and respects Phase 5/6 ownership. |

## Decisions Recorded

| Decision | Auto Selection |
| --- | --- |
| How to define parity success? | Generated GSD artifacts and lifecycle continuity are the success signal. |
| Should Phase 4 test every command? | No. Test the FLOW-01/FLOW-02 core lifecycle commands. |
| Should CI require a real Hermes binary? | No. Real Hermes smoke is optional/probe-gated. |
| Where should compatibility fixes land? | Prefer Hermes conversion/shim/docs/tests before upstream workflow edits. |
| How should unsupported runtime behavior behave? | Clear degraded path or explicit failure, never silent partial support. |
| Where should mutating smoke run? | Disposable fixture workspaces with isolated `HOME`. |
| Is update/uninstall/doctor fully in scope? | No. Full lifecycle tooling remains Phase 5. |

## Key Findings From Context Scan

- Phase 3 installed `/gsd-*` discovery for global and project-linked Hermes
  modes, but intentionally deferred workflow parity.
- `docs/hermes-compatibility.md` currently marks core workflow execution as
  planned, so Phase 4 should update that truth only after implementation.
- `bin/install.js` already has bounded Hermes seams:
  `copyCommandsAsHermesSkills()` and `ensureHermesExternalDir()`.
- Generated Hermes command skills already replace common `.claude` path forms,
  but underlying workflow files still contain Claude-specific path and tool
  assumptions that can affect execution.
- `get-shit-done/workflows/update.md` and `settings.md` are immediate high-risk
  files because they contain direct runtime directory assumptions.

## Deferred Items

- Full Hermes update/uninstall/doctor lifecycle behavior belongs to Phase 5.
- Final broad parity matrix and upstream sync closure belong to Phase 6.
- Native Hermes local install support remains out of scope.

---

*Generated by `$gsd-discuss-phase 4 --auto`.*
