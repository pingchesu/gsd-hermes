---
phase: 04-core-workflow-parity
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - tests/hermes-core-workflow.test.cjs
  - docs/hermes-install.md
  - docs/hermes-compatibility.md
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03
user_setup: []
must_haves:
  truths:
    - "Phase 4 has a deterministic full core workflow smoke path and an optional real Hermes probe."
    - "Absence of a real Hermes CLI produces a skip diagnostic, not a CI failure."
  artifacts:
    - path: tests/hermes-core-workflow.test.cjs
      provides: "full deterministic and optional Hermes smoke coverage"
      contains: "optional real Hermes CLI smoke"
    - path: docs/hermes-install.md
      provides: "operator guidance for deterministic and real Hermes smoke"
      contains: "Optional real Hermes smoke"
---

<objective>
Close Phase 4 with an end-to-end core workflow smoke contract that is useful in
CI and can optionally validate a real Hermes installation when available.
</objective>

<execution_context>
@/home/whiskey/.codex/get-shit-done/workflows/execute-plan.md
@/home/whiskey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-core-workflow-parity/04-CONTEXT.md
@.planning/phases/04-core-workflow-parity/04-RESEARCH.md
@.planning/phases/04-core-workflow-parity/04-VALIDATION.md
@tests/hermes-core-workflow.test.cjs
@docs/hermes-install.md
@docs/hermes-compatibility.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optional real Hermes CLI smoke probe</name>
  <files>tests/hermes-core-workflow.test.cjs</files>
  <read_first>tests/hermes-core-workflow.test.cjs, docs/hermes-install.md, .planning/phases/04-core-workflow-parity/04-VALIDATION.md</read_first>
  <action>Add a test named `optional real Hermes CLI smoke is skipped when hermes is unavailable`. Use `spawnSync('hermes', ['--version'], { encoding: 'utf8' })`. If `result.error && result.error.code === 'ENOENT'`, call `t.diagnostic('hermes binary not found; skipping optional real Hermes CLI smoke')` and `return`. If Hermes exists, run the project-linked installer in temp `HOME` and temp project, then run `spawnSync('hermes', ['skills', 'list'], { cwd: tmpProject, env: { ...process.env, HOME: tmpHome }, encoding: 'utf8' })`. Assert the command exits `0` or, if Hermes uses a different noninteractive list surface, call `t.diagnostic()` with stdout/stderr and do not fail solely on unsupported list syntax. Always assert the fixture install itself exited `0` before the optional CLI check.</action>
  <verify>node --test tests/hermes-core-workflow.test.cjs</verify>
  <acceptance_criteria>
    - `tests/hermes-core-workflow.test.cjs` contains `optional real Hermes CLI smoke`
    - `tests/hermes-core-workflow.test.cjs` contains `hermes binary not found; skipping optional real Hermes CLI smoke`
    - `tests/hermes-core-workflow.test.cjs` contains `hermes`
    - `node --test tests/hermes-core-workflow.test.cjs` exits 0
  </acceptance_criteria>
  <done>The test suite records real Hermes availability when possible and skips cleanly when the binary is absent.</done>
</task>

<task type="auto">
  <name>Task 2: Add final core workflow smoke docs and compatibility closure</name>
  <files>docs/hermes-install.md, docs/hermes-compatibility.md</files>
  <read_first>docs/hermes-install.md, docs/hermes-compatibility.md, tests/hermes-docs.test.cjs</read_first>
  <action>In `docs/hermes-install.md`, add an `Optional real Hermes smoke` subsection that explains deterministic Node tests are the CI source of truth and real Hermes CLI checks are best-effort. Include exact commands `node --test tests/hermes-core-workflow.test.cjs`, `hermes skills list`, and `hermes chat -q "/gsd-help"`. In `docs/hermes-compatibility.md`, update the Phase Mapping row for `Core workflow execution` to say Phase 4 is complete and routes through deterministic fixture coverage plus documented degraded paths. Do not mark update/uninstall/doctor complete.</action>
  <verify>node --test tests/hermes-docs.test.cjs</verify>
  <acceptance_criteria>
    - `docs/hermes-install.md` contains `Optional real Hermes smoke`
    - `docs/hermes-install.md` contains `node --test tests/hermes-core-workflow.test.cjs`
    - `docs/hermes-install.md` contains `hermes chat -q "/gsd-help"`
    - `docs/hermes-compatibility.md` contains `Phase 4`
    - `docs/hermes-compatibility.md` contains `deterministic fixture coverage`
  </acceptance_criteria>
  <done>Docs tell maintainers how to verify Phase 4 without confusing deterministic CI coverage and optional real-runtime smoke.</done>
</task>

<task type="auto">
  <name>Task 3: Run the Phase 4 verification suite</name>
  <files>tests/hermes-core-workflow.test.cjs, docs/hermes-install.md, docs/hermes-compatibility.md</files>
  <read_first>tests/hermes-core-workflow.test.cjs, tests/hermes-install.test.cjs, tests/hermes-project-linked.test.cjs, tests/hermes-docs.test.cjs</read_first>
  <action>Run targeted Hermes tests and then the full test suite. Required targeted command: `node --test tests/hermes-core-workflow.test.cjs tests/hermes-install.test.cjs tests/hermes-project-linked.test.cjs tests/hermes-docs.test.cjs tests/multi-runtime-select.test.cjs`. Required full command: `npm test`. If `npm test` exposes an unrelated flaky failure, record the exact failing test and rerun the targeted Hermes tests before deciding whether the phase is blocked.</action>
  <verify>node --test tests/hermes-core-workflow.test.cjs tests/hermes-install.test.cjs tests/hermes-project-linked.test.cjs tests/hermes-docs.test.cjs tests/multi-runtime-select.test.cjs</verify>
  <acceptance_criteria>
    - Targeted Hermes test command exits 0
    - `npm test` exits 0 or any non-Hermes failure is explicitly documented in the summary with targeted Hermes tests still passing
  </acceptance_criteria>
  <done>Phase 4 has final verification evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| optional runtime probe -> CI result | Missing Hermes should not fail deterministic CI. |
| docs -> operator actions | Smoke docs must not imply real Hermes is mandatory for all contributors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-04-07 | D | CI portability | mitigate | Skip optional real Hermes probe when the binary is unavailable. |
| T-04-08 | R | verification evidence | mitigate | Require targeted Hermes tests and full `npm test` evidence in the plan summary. |
</threat_model>

<verification>
- [ ] `node --test tests/hermes-core-workflow.test.cjs tests/hermes-install.test.cjs tests/hermes-project-linked.test.cjs tests/hermes-docs.test.cjs tests/multi-runtime-select.test.cjs`
- [ ] `npm test`
</verification>

<success_criteria>
- Optional real Hermes smoke is probe-gated.
- Deterministic Phase 4 test suite passes.
- Docs separate CI source of truth from best-effort real runtime smoke.
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-workflow-parity/04-04-SUMMARY.md`.
</output>
