---
phase: 04-core-workflow-parity
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - bin/install.js
  - tests/hermes-core-workflow.test.cjs
  - docs/hermes-install.md
  - docs/hermes-compatibility.md
  - tests/hermes-docs.test.cjs
autonomous: true
requirements:
  - FLOW-02
  - FLOW-03
user_setup: []
must_haves:
  truths:
    - "Hermes users receive explicit fallback guidance when workflows depend on Claude-native tools."
    - "Compatibility docs describe core workflow parity as supported with documented degraded paths, not silently complete."
  artifacts:
    - path: bin/install.js
      provides: "generated Hermes skill degraded-path note"
      contains: "Hermes runtime compatibility"
    - path: docs/hermes-compatibility.md
      provides: "truthful Phase 4 compatibility matrix"
      contains: "supported with degraded paths"
---

<objective>
Add explicit Hermes degraded-path guidance for core workflows so unsupported
runtime capabilities fail clearly instead of silently claiming full parity.
</objective>

<execution_context>
@/home/whiskey/.codex/get-shit-done/workflows/execute-plan.md
@/home/whiskey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-core-workflow-parity/04-CONTEXT.md
@.planning/phases/04-core-workflow-parity/04-RESEARCH.md
@docs/hermes-install.md
@docs/hermes-compatibility.md
@tests/hermes-docs.test.cjs
@bin/install.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject Hermes runtime compatibility note into generated skills</name>
  <files>bin/install.js, tests/hermes-core-workflow.test.cjs</files>
  <read_first>bin/install.js, tests/hermes-core-workflow.test.cjs, .planning/phases/04-core-workflow-parity/04-CONTEXT.md</read_first>
  <action>Add helper `appendHermesRuntimeNote(content)` near `copyCommandsAsHermesSkills()`. The helper must append a short markdown section containing the exact heading `## Hermes runtime compatibility` and these exact phrases: `Use text-mode fallback when AskUserQuestion is unavailable`, `Use sequential inline execution when Task-style subagents are unavailable`, and `Do not report full parity when a workflow needs a degraded path`. Update `copyCommandsAsHermesSkills()` so generated Hermes `SKILL.md` content passes through this helper after path conversion/frontmatter conversion and before writing. Extend `tests/hermes-core-workflow.test.cjs` to read `gsd-plan-phase/SKILL.md` from a global Hermes fixture and assert it contains all three exact phrases.</action>
  <verify>node --test tests/hermes-core-workflow.test.cjs</verify>
  <acceptance_criteria>
    - `bin/install.js` contains `function appendHermesRuntimeNote(`
    - `bin/install.js` contains `## Hermes runtime compatibility`
    - `bin/install.js` contains `Use text-mode fallback when AskUserQuestion is unavailable`
    - `bin/install.js` contains `Use sequential inline execution when Task-style subagents are unavailable`
    - `node --test tests/hermes-core-workflow.test.cjs` exits 0
  </acceptance_criteria>
  <done>Generated Hermes skills include explicit fallback guidance for unavailable interactive/subagent runtime features.</done>
</task>

<task type="auto">
  <name>Task 2: Update Hermes docs and docs tests for Phase 4 parity truth</name>
  <files>docs/hermes-install.md, docs/hermes-compatibility.md, tests/hermes-docs.test.cjs</files>
  <read_first>docs/hermes-install.md, docs/hermes-compatibility.md, tests/hermes-docs.test.cjs, .planning/phases/04-core-workflow-parity/04-CONTEXT.md</read_first>
  <action>Update `docs/hermes-install.md` by adding a `Core Workflow Smoke` section with commands for `/gsd-new-project`, `/gsd-progress`, `/gsd-discuss-phase 1 --auto`, `/gsd-plan-phase 1 --auto`, and `/gsd-execute-phase 1`. The section must state that Hermes support uses documented degraded paths for unavailable `AskUserQuestion` and `Task` style capabilities. Update `docs/hermes-compatibility.md` so the `Core workflow execution` matrix row status becomes `supported with degraded paths`, and the note mentions `new-project, discuss, plan, execute, verify, progress, settings, and update smoke coverage`. Update known gaps so it no longer says core workflow parity is not complete yet; instead state that full lifecycle update/uninstall/doctor remains Phase 5. Extend `tests/hermes-docs.test.cjs` to assert `supported with degraded paths`, `Core Workflow Smoke`, `AskUserQuestion`, and `Task` appear in the docs.</action>
  <verify>node --test tests/hermes-docs.test.cjs</verify>
  <acceptance_criteria>
    - `docs/hermes-install.md` contains `Core Workflow Smoke`
    - `docs/hermes-install.md` contains `/gsd-new-project`
    - `docs/hermes-install.md` contains `AskUserQuestion`
    - `docs/hermes-compatibility.md` contains `Core workflow execution | supported with degraded paths`
    - `tests/hermes-docs.test.cjs` contains `supported with degraded paths`
    - `node --test tests/hermes-docs.test.cjs` exits 0
  </acceptance_criteria>
  <done>Docs now publish the Phase 4 truth: core workflow execution is usable with explicit degraded paths, while lifecycle tooling remains later work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| generated skill instructions -> user expectations | Runtime notes tell Hermes users how to handle unavailable features. |
| compatibility docs -> project truth | Docs define what maintainers are allowed to claim as supported. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-04-05 | R | degraded path behavior | mitigate | Generated skills must contain exact fallback language. |
| T-04-06 | I | compatibility claims | mitigate | Docs tests pin `supported with degraded paths` wording. |
</threat_model>

<verification>
- [ ] `node --test tests/hermes-core-workflow.test.cjs`
- [ ] `node --test tests/hermes-docs.test.cjs`
</verification>

<success_criteria>
- Generated Hermes core skills include explicit compatibility fallback guidance.
- Hermes docs list core workflow smoke commands.
- Compatibility matrix truthfully reports supported-with-degraded-paths behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-workflow-parity/04-03-SUMMARY.md`.
</output>
