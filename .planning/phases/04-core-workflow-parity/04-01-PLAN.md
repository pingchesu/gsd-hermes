---
phase: 04-core-workflow-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/hermes-core-workflow.test.cjs
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
user_setup: []
must_haves:
  truths:
    - "Hermes installs expose the core GSD lifecycle skills required by FLOW-01 and FLOW-02."
    - "Core workflow parity tests run in temp HOME/project fixtures and do not require a real Hermes binary."
  artifacts:
    - path: tests/hermes-core-workflow.test.cjs
      provides: "deterministic core workflow fixture coverage"
      contains: "CORE_WORKFLOW_SKILLS"
  key_links:
    - from: tests/hermes-core-workflow.test.cjs
      to: bin/install.js
      via: "sandboxed `--hermes --global --no-sdk` and `--hermes --local --no-sdk` installer runs"
      pattern: "gsd-new-project|gsd-discuss-phase|gsd-plan-phase|gsd-execute-phase"
---

<objective>
Create a deterministic Hermes core workflow fixture suite that proves the core
`/gsd-*` lifecycle skill set is installed and discoverable without requiring a
real Hermes binary.
</objective>

<execution_context>
@/home/whiskey/.codex/get-shit-done/workflows/execute-plan.md
@/home/whiskey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-core-workflow-parity/04-CONTEXT.md
@.planning/phases/04-core-workflow-parity/04-RESEARCH.md
@.planning/phases/04-core-workflow-parity/04-VALIDATION.md
@tests/hermes-install.test.cjs
@tests/hermes-project-linked.test.cjs
@bin/install.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add core Hermes workflow fixture helpers</name>
  <files>tests/hermes-core-workflow.test.cjs</files>
  <read_first>tests/hermes-install.test.cjs, tests/hermes-project-linked.test.cjs, tests/helpers.cjs, bin/install.js</read_first>
  <action>Create `tests/hermes-core-workflow.test.cjs`. Set `process.env.GSD_TEST_MODE = '1'`. Import `node:test`, `node:assert/strict`, `node:fs`, `node:path`, `node:child_process`, and `{ createTempDir, cleanup }` from `./helpers.cjs`. Define `CORE_WORKFLOW_SKILLS` exactly as `['gsd-new-project', 'gsd-discuss-phase', 'gsd-plan-phase', 'gsd-execute-phase', 'gsd-verify-work', 'gsd-progress', 'gsd-settings', 'gsd-update']`. Define `runInstaller(args, options)` matching the existing Hermes tests: copy `process.env`, set `HOME`, delete `GSD_TEST_MODE`, and call `spawnSync(process.execPath, [installPath, ...args], { cwd, env, encoding: 'utf8' })`.</action>
  <verify>node --test tests/hermes-core-workflow.test.cjs</verify>
  <acceptance_criteria>
    - `tests/hermes-core-workflow.test.cjs` contains `process.env.GSD_TEST_MODE = '1'`
    - `tests/hermes-core-workflow.test.cjs` contains `CORE_WORKFLOW_SKILLS`
    - `tests/hermes-core-workflow.test.cjs` contains `spawnSync(process.execPath, [installPath, ...args]`
  </acceptance_criteria>
  <done>The new test file has isolated installer helpers and the exact core lifecycle skill list.</done>
</task>

<task type="auto">
  <name>Task 2: Assert global and project-linked core skill installation</name>
  <files>tests/hermes-core-workflow.test.cjs</files>
  <read_first>tests/hermes-core-workflow.test.cjs, bin/install.js, .planning/phases/04-core-workflow-parity/04-VALIDATION.md</read_first>
  <action>Add two tests. The first runs `runInstaller(['--hermes', '--global', '--no-sdk'], { home: tmpHome, cwd: tmpProject })`, asserts exit status `0`, then for each `skillName` in `CORE_WORKFLOW_SKILLS` asserts `path.join(tmpHome, '.hermes', 'skills', skillName, 'SKILL.md')` exists and contains a frontmatter line matching `new RegExp('^name:\\\\s+' + skillName + '$', 'm')`. The second runs `runInstaller(['--hermes', '--local', '--no-sdk'], { home: tmpHome, cwd: tmpProject })`, asserts exit status `0`, then for each skill asserts `path.join(tmpProject, '.gsd-hermes', 'skills', skillName, 'SKILL.md')` exists. Also assert `path.join(tmpHome, '.hermes', 'config.yaml')` contains the absolute fixture `.gsd-hermes/skills` path exactly once.</action>
  <verify>node --test tests/hermes-core-workflow.test.cjs</verify>
  <acceptance_criteria>
    - `tests/hermes-core-workflow.test.cjs` contains `--global`
    - `tests/hermes-core-workflow.test.cjs` contains `--local`
    - `tests/hermes-core-workflow.test.cjs` contains `.gsd-hermes`
    - `node --test tests/hermes-core-workflow.test.cjs` exits 0
  </acceptance_criteria>
  <done>Both supported Hermes install modes prove the core lifecycle command set exists in fixture output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| installer output -> Hermes command surface | Generated skill directories become the user's `/gsd-*` command inventory. |
| test fixture -> user environment | Tests must never write to the user's real `~/.hermes`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-04-01 | T | core skill set | mitigate | Assert every FLOW-01/FLOW-02 core skill exists by exact directory and frontmatter name. |
| T-04-02 | I | user environment | mitigate | Use temp `HOME` and temp project dirs in every mutating test. |
</threat_model>

<verification>
- [ ] `node --test tests/hermes-core-workflow.test.cjs`
</verification>

<success_criteria>
- Global Hermes fixture installs all core lifecycle skills.
- Project-linked Hermes fixture installs all core lifecycle skills.
- Project-linked fixture config contains exactly one fixture `skills.external_dirs` entry.
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-workflow-parity/04-01-SUMMARY.md`.
</output>
