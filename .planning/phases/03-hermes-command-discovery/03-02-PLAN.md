---
phase: 03-hermes-command-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - bin/install.js
  - tests/hermes-project-linked.test.cjs
autonomous: true
requirements:
  - HERM-02
user_setup: []
must_haves:
  truths:
    - "A Hermes project-linked install writes GSD skills under `.gsd-hermes/skills/` in the target project."
    - "A Hermes project-linked install registers the absolute project skills path in `~/.hermes/config.yaml` under `skills.external_dirs` without duplicating it."
  artifacts:
    - path: bin/install.js
      provides: "project-linked Hermes install path and `skills.external_dirs` mutation"
      contains: "ensureHermesExternalDir"
    - path: tests/hermes-project-linked.test.cjs
      provides: "project-linked Hermes install regression coverage"
      contains: ".gsd-hermes"
  key_links:
    - from: bin/install.js
      to: tests/hermes-project-linked.test.cjs
      via: "`--hermes --local --no-sdk` sandbox install creates project skills and mutates Hermes config"
      pattern: "ensureHermesExternalDir|\\.gsd-hermes|external_dirs"
---

<objective>
Add Hermes project-linked install support through `skills.external_dirs` without claiming native local install semantics.

Purpose: Close HERM-02 by letting a project opt into GSD commands through a project-owned skills directory scanned by Hermes.
Output: Project-linked installer branch, conservative Hermes config mutation, and focused tests.
</objective>

<execution_context>
@/home/whiskey/.codex/get-shit-done/workflows/execute-plan.md
@/home/whiskey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hermes-command-discovery/03-CONTEXT.md
@.planning/phases/03-hermes-command-discovery/03-RESEARCH.md
@.planning/phases/03-hermes-command-discovery/03-PATTERNS.md
@.planning/phases/03-hermes-command-discovery/03-01-SUMMARY.md
@bin/install.js
@tests/hermes-install.test.cjs

<interfaces>
Phase 3 context requires the secondary mode to be called "project-linked install". The generated project skills root should be `.gsd-hermes/skills/`, and the absolute skills path must be added to `~/.hermes/config.yaml` under `skills.external_dirs`. Global Hermes installs must not mutate `config.yaml`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route Hermes local selection to project-linked mode</name>
  <files>bin/install.js</files>
  <read_first>bin/install.js, .planning/phases/03-hermes-command-discovery/03-CONTEXT.md, .planning/phases/03-hermes-command-discovery/03-RESEARCH.md</read_first>
  <action>Replace the Phase 2 Hermes local rejection behavior with project-linked mode. Remove or narrow the early guards that exit for `hasHermes && hasLocal`, `hasLocal && selectedRuntimes.includes('hermes')`, and `runtime === 'hermes' && !isGlobal`. For Hermes with `isGlobal === false`, set `targetDir` to `path.join(process.cwd(), '.gsd-hermes')` instead of `path.join(process.cwd(), '.hermes')`, set `locationLabel` so output displays `./.gsd-hermes`, and install skills into `path.join(targetDir, 'skills')`. Keep global Hermes unchanged at `path.join(getGlobalDir('hermes', explicitConfigDir), 'skills')`. Update user-facing output strings so local Hermes says `project-linked mode` and `skills.external_dirs`, never `native local install`.</action>
  <verify>node -e 'const fs=require("fs");const s=fs.readFileSync("bin/install.js","utf8");const checks=[/\\.gsd-hermes/,/project-linked mode/,/external_dirs/,/isHermes[\\s\\S]{0,600}isGlobal[\\s\\S]{0,600}getGlobalDir\\([\"\\x27]hermes[\"\\x27]/,/isHermes[\\s\\S]{0,800}!isGlobal[\\s\\S]{0,800}\\.gsd-hermes/];if(checks.some(re=>!re.test(s))) process.exit(1); if(/Hermes local install is not supported in Phase 2/.test(s)) process.exit(1);'</verify>
  <acceptance_criteria>
    - `bin/install.js` contains `.gsd-hermes`
    - `bin/install.js` contains `project-linked mode`
    - `bin/install.js` does not contain `Hermes local install is not supported in Phase 2`
    - `bin/install.js` contains `external_dirs`
  </acceptance_criteria>
  <done>Hermes `--local` becomes a truthful project-linked install path instead of a rejected native-local claim.</done>
</task>

<task type="auto">
  <name>Task 2: Add conservative external_dirs config mutation</name>
  <files>bin/install.js</files>
  <read_first>bin/install.js, .planning/phases/03-hermes-command-discovery/03-CONTEXT.md</read_first>
  <action>Add and export `ensureHermesExternalDir(configPath, externalDir)`. The helper must create parent directories, normalize `externalDir` with `path.resolve(externalDir).replace(/\\/g, '/')`, quote the value in YAML using existing `yamlQuote()`, and ensure `configPath` contains exactly one entry for that path under `skills.external_dirs`. If `configPath` does not exist or is empty, write exactly `skills:\n  external_dirs:\n    - "<absolute-path>"\n`. If a `skills:` section exists without `external_dirs:`, insert `  external_dirs:\n    - "<absolute-path>"\n` directly after the `skills:` line. If an `external_dirs:` block exists, append `    - "<absolute-path>"` only when neither the quoted nor unquoted path already appears in that block. Preserve unrelated lines and comments outside the inserted block. Call this helper after project-linked Hermes skill generation with `configPath = path.join(getGlobalDir('hermes', explicitConfigDir), 'config.yaml')` and `externalDir = skillsDir`. Do not call it for global Hermes installs.</action>
  <verify>node -e 'const m=require("./bin/install.js"); if (typeof m.ensureHermesExternalDir !== "function") process.exit(1)'</verify>
  <acceptance_criteria>
    - `bin/install.js` contains `function ensureHermesExternalDir(`
    - `bin/install.js` contains `skills:`
    - `bin/install.js` contains `external_dirs:`
    - `bin/install.js` contains `ensureHermesExternalDir(configPath, skillsDir)`
    - `bin/install.js` contains `ensureHermesExternalDir,`
  </acceptance_criteria>
  <done>Project-linked installs register the project skills directory in Hermes config without global installs touching config.</done>
</task>

<task type="auto">
  <name>Task 3: Cover project-linked mode and idempotence</name>
  <files>tests/hermes-project-linked.test.cjs</files>
  <read_first>tests/hermes-install.test.cjs, bin/install.js, .planning/phases/03-hermes-command-discovery/03-VALIDATION.md</read_first>
  <action>Create `tests/hermes-project-linked.test.cjs`. Set `process.env.GSD_TEST_MODE = '1'`, use `node:test`, `assert/strict`, `fs`, `path`, `child_process.spawnSync`, and the existing helper style from `tests/hermes-install.test.cjs`. Add tests that: 1) call exported `ensureHermesExternalDir()` on a missing config file and assert it writes `skills:`, `external_dirs:`, and one absolute path; 2) call it twice and assert the absolute path appears exactly once; 3) start from a config containing unrelated text such as `model:\n  provider: test\n# keep me\nskills:\n  enabled: true\n`, call the helper, and assert `provider: test`, `# keep me`, `enabled: true`, `external_dirs:`, and the path remain present; 4) run the installer with `['--hermes', '--local', '--no-sdk']` in temp HOME and temp project, assert exit 0, assert `.gsd-hermes/skills/gsd-help/SKILL.md` exists, assert `~/.hermes/config.yaml` contains the absolute project skills path, assert stdout contains `project-linked mode`, then run the same command again and assert the config path count is still one.</action>
  <verify>node --test tests/hermes-project-linked.test.cjs</verify>
  <acceptance_criteria>
    - `tests/hermes-project-linked.test.cjs` contains `ensureHermesExternalDir`
    - `tests/hermes-project-linked.test.cjs` contains `--hermes`
    - `tests/hermes-project-linked.test.cjs` contains `--local`
    - `tests/hermes-project-linked.test.cjs` contains `.gsd-hermes`
    - `node --test tests/hermes-project-linked.test.cjs` exits 0
  </acceptance_criteria>
  <done>Project-linked Hermes discovery is covered through helper-level tests and a sandboxed installer smoke.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project directory -> Hermes global config | A project install writes a path into user-level Hermes config |
| repeated installer runs -> config integrity | Re-running install must not duplicate or damage `external_dirs` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T | `~/.hermes/config.yaml` mutation | mitigate | Limit mutation to `skills.external_dirs`, preserve unrelated text, and test existing config preservation |
| T-03-05 | R | project-linked mode wording | mitigate | Require output to say `project-linked mode` and forbid the old Phase 2 unsupported-local error |
| T-03-06 | D | repeated installs | mitigate | De-duplicate the absolute project skills path and test repeated `--hermes --local --no-sdk` installs |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `node -e 'const m=require("./bin/install.js"); if (typeof m.ensureHermesExternalDir !== "function") process.exit(1)'`
- [ ] `node --test tests/hermes-project-linked.test.cjs`
- [ ] `node --test tests/hermes-install.test.cjs`
</verification>

<success_criteria>

- `--hermes --local` performs a project-linked install into `.gsd-hermes/skills`
- Hermes config gains one `skills.external_dirs` entry for the project skills path
- Global Hermes installs still do not mutate `~/.hermes/config.yaml`
- Repeated project-linked installs are idempotent
</success_criteria>

<output>
After completion, create `.planning/phases/03-hermes-command-discovery/03-02-SUMMARY.md`
</output>
