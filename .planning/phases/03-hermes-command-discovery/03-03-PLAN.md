---
phase: 03-hermes-command-discovery
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - bin/install.js
  - docs/hermes-install.md
  - docs/hermes-compatibility.md
  - docs/README.md
  - tests/hermes-docs.test.cjs
autonomous: true
requirements:
  - HERM-01
  - HERM-02
user_setup: []
must_haves:
  truths:
    - "Docs explain both Hermes global command discovery and project-linked discovery through `skills.external_dirs`."
    - "Docs preserve the boundary that core workflow parity is Phase 4 and lifecycle tooling is Phase 5."
  artifacts:
    - path: docs/hermes-install.md
      provides: "Hermes install and command discovery guide"
      contains: "/gsd-help"
    - path: tests/hermes-docs.test.cjs
      provides: "documentation and final discovery regression coverage"
      contains: "skills.external_dirs"
  key_links:
    - from: docs/README.md
      to: docs/hermes-install.md
      via: "fork docs index link"
      pattern: "hermes-install.md|Hermes install"
    - from: docs/hermes-compatibility.md
      to: Phase 3 requirements
      via: "compatibility matrix status updates"
      pattern: "Global Hermes install|Project-linked external_dirs mode|Command discovery"
---

<objective>
Publish accurate Hermes command discovery docs and pin the Phase 3 boundary with regression checks.

Purpose: Make Phase 3 usable by developers without overclaiming Phase 4 workflow parity or Phase 5 lifecycle support.
Output: Hermes install docs, compatibility matrix updates, docs index link, and documentation/source seam tests.
</objective>

<execution_context>
@/home/whiskey/.codex/get-shit-done/workflows/execute-plan.md
@/home/whiskey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hermes-command-discovery/03-CONTEXT.md
@.planning/phases/03-hermes-command-discovery/03-RESEARCH.md
@.planning/phases/03-hermes-command-discovery/03-VALIDATION.md
@.planning/phases/03-hermes-command-discovery/03-01-SUMMARY.md
@.planning/phases/03-hermes-command-discovery/03-02-SUMMARY.md
@docs/hermes-compatibility.md
@docs/README.md
@bin/install.js

<interfaces>
Phase 3 docs must explain the two supported install modes and command discovery smoke paths. They must not claim `/gsd-new-project`, discuss, plan, execute, update, uninstall, or doctor parity yet.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Hermes install and discovery guide</name>
  <files>docs/hermes-install.md, docs/README.md</files>
  <read_first>docs/README.md, docs/hermes-compatibility.md, .planning/phases/03-hermes-command-discovery/03-CONTEXT.md, .planning/phases/03-hermes-command-discovery/03-RESEARCH.md</read_first>
  <action>Create `docs/hermes-install.md` with sections `## Install Modes`, `## Global Mode`, `## Project-Linked Mode`, `## Command Discovery Smoke`, `## Current Boundaries`, and `## Troubleshooting`. Include exact commands `npx get-shit-done-cc --hermes --global`, `npx get-shit-done-cc --hermes --local`, `hermes skills list`, `hermes chat -q "/gsd-help"`, and mention `/gsd-progress`. State that global mode writes `~/.hermes/skills/gsd-*/SKILL.md`. State that project-linked mode writes `.gsd-hermes/skills/gsd-*/SKILL.md` and registers the absolute path in `~/.hermes/config.yaml` under `skills.external_dirs`. State that if both global and project-linked skills exist, Hermes may prefer globally discovered duplicate names. State that core workflow parity is Phase 4 and update/uninstall/doctor lifecycle support is Phase 5. Add one row to `docs/README.md` linking `[Hermes Install](hermes-install.md)` with a description of install modes and command discovery.</action>
  <verify>node -e 'const fs=require("fs");const d=fs.readFileSync("docs/hermes-install.md","utf8");const idx=fs.readFileSync("docs/README.md","utf8");const checks=[/## Install Modes/,/--hermes --global/,/--hermes --local/,/skills\\.external_dirs/,/\\/gsd-help/,/\\/gsd-progress/,/Phase 4/,/Phase 5/];if(checks.some(re=>!re.test(d))) process.exit(1); if(!/hermes-install\\.md/.test(idx)) process.exit(1);'</verify>
  <acceptance_criteria>
    - `docs/hermes-install.md` contains `## Install Modes`
    - `docs/hermes-install.md` contains `npx get-shit-done-cc --hermes --global`
    - `docs/hermes-install.md` contains `npx get-shit-done-cc --hermes --local`
    - `docs/hermes-install.md` contains `skills.external_dirs`
    - `docs/hermes-install.md` contains `/gsd-help`
    - `docs/README.md` contains `hermes-install.md`
  </acceptance_criteria>
  <done>Developers can find accurate global and project-linked Hermes discovery instructions from the docs index.</done>
</task>

<task type="auto">
  <name>Task 2: Update compatibility matrix and installer copy for supported discovery</name>
  <files>docs/hermes-compatibility.md, bin/install.js</files>
  <read_first>docs/hermes-compatibility.md, bin/install.js, .planning/phases/03-hermes-command-discovery/03-CONTEXT.md</read_first>
  <action>Update `docs/hermes-compatibility.md` so `Global Hermes install`, `Project-linked external_dirs mode`, and `Command discovery for /gsd-*` show Phase 3 support status instead of Phase 1/2 planned wording. Keep `Core workflow execution` planned for Phase 4, `Update / uninstall / doctor` planned for Phase 5, and `Native local Hermes install mode` out of scope. Update Hermes installer help/finish copy in `bin/install.js` so it no longer says project-linked support and command discovery are planned later after they are implemented. The finish output must distinguish `global mode` from `project-linked mode`, mention `skills.external_dirs` only for project-linked mode, and recommend restarting/reloading Hermes before trying `/gsd-help` if needed.</action>
  <verify>node -e 'const fs=require("fs");const doc=fs.readFileSync("docs/hermes-compatibility.md","utf8");const install=fs.readFileSync("bin/install.js","utf8");const docChecks=[/Global Hermes install \\| supported/,/Project-linked external_dirs mode \\| supported/,/Command discovery for \\/gsd-\\* \\| supported/,/Core workflow execution \\| planned/,/Native local Hermes install mode \\| out of scope/];if(docChecks.some(re=>!re.test(doc))) process.exit(1); const installChecks=[/project-linked mode/,/skills\\.external_dirs/,/\\/gsd-help/];if(installChecks.some(re=>!re.test(install))) process.exit(1); if(/command discovery are planned for later phases/.test(install)) process.exit(1);'</verify>
  <acceptance_criteria>
    - `docs/hermes-compatibility.md` contains `Global Hermes install | supported`
    - `docs/hermes-compatibility.md` contains `Project-linked external_dirs mode | supported`
    - `docs/hermes-compatibility.md` contains `Command discovery for /gsd-* | supported`
    - `bin/install.js` contains `/gsd-help`
    - `bin/install.js` does not contain `command discovery are planned for later phases`
  </acceptance_criteria>
  <done>Compatibility docs and installer output reflect Phase 3 support without claiming later-phase parity.</done>
</task>

<task type="auto">
  <name>Task 3: Add docs/source regression test and run full verification</name>
  <files>tests/hermes-docs.test.cjs</files>
  <read_first>tests/hermes-install.test.cjs, tests/hermes-project-linked.test.cjs, docs/hermes-install.md, docs/hermes-compatibility.md, bin/install.js</read_first>
  <action>Create `tests/hermes-docs.test.cjs` using `node:test`, `assert/strict`, and `fs`. Add tests that read `docs/hermes-install.md`, `docs/hermes-compatibility.md`, `docs/README.md`, and `bin/install.js`. Assert the install guide contains `--hermes --global`, `--hermes --local`, `skills.external_dirs`, `.gsd-hermes/skills`, `/gsd-help`, `/gsd-progress`, `Phase 4`, and `Phase 5`. Assert the compatibility matrix contains `Global Hermes install | supported`, `Project-linked external_dirs mode | supported`, `Command discovery for /gsd-* | supported`, and `Native local Hermes install mode | out of scope`. Assert the docs index contains `hermes-install.md`. Assert `bin/install.js` contains `project-linked mode`, `skills.external_dirs`, and `/gsd-help` and does not contain `command discovery are planned for later phases`. After adding the test, run `node --test tests/hermes-install.test.cjs tests/hermes-project-linked.test.cjs tests/hermes-docs.test.cjs` and `npm test`.</action>
  <verify>node --test tests/hermes-install.test.cjs tests/hermes-project-linked.test.cjs tests/hermes-docs.test.cjs && npm test</verify>
  <acceptance_criteria>
    - `tests/hermes-docs.test.cjs` contains `docs/hermes-install.md`
    - `tests/hermes-docs.test.cjs` contains `skills.external_dirs`
    - `tests/hermes-docs.test.cjs` contains `Command discovery for /gsd-*`
    - `node --test tests/hermes-install.test.cjs tests/hermes-project-linked.test.cjs tests/hermes-docs.test.cjs` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Phase 3 has docs/source regression coverage and full test-suite verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> user expectations | Docs become the public compatibility contract for Hermes users |
| installer output -> operator action | Finish messages guide users toward smoke commands and mode expectations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | I | Hermes docs | mitigate | Test docs for required mode, smoke, and phase-boundary strings so later edits cannot overclaim parity silently |
| T-03-08 | R | compatibility matrix | mitigate | Update matrix rows to supported/planned/out-of-scope states and assert exact rows in tests |
| T-03-09 | S | installer finish copy | mitigate | Require installer output to mention the actual configured mode and `/gsd-help` smoke path rather than stale planned-later text |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/hermes-install.test.cjs tests/hermes-project-linked.test.cjs tests/hermes-docs.test.cjs`
- [ ] `npm test`
</verification>

<success_criteria>

- Hermes global and project-linked command discovery are documented with exact commands and paths
- Compatibility docs show Phase 3 discovery support while preserving Phase 4/5 boundaries
- Docs/source regression tests pass
- Full `npm test` passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-hermes-command-discovery/03-03-SUMMARY.md`
</output>
