---
phase: 8.2
plan: 2
title: Execute-Phase Provider-Routed Dispatcher Workflow
status: complete
completed: 2026-04-29T20:09:55+08:00
requirements:
  - DISP-01
  - DISP-02
  - DISP-03
  - DISP-04
  - DISP-05
---

# Summary — Plan 8.2-02 Execute-Phase Provider-Routed Dispatcher Workflow

## What Changed

- Updated `get-shit-done/workflows/execute-phase.md` to parse and display `agent_execution_bindings`.
- Added provider-cli executor dispatch instructions before Task/delegate fallback.
- Added provider-cli verifier dispatch instructions before verifier Task fallback.
- Demoted `workflow.cross_ai_execution` to a lower-priority legacy whole-plan fallback when provider-cli direct bindings are valid.
- Documented `workflow.agent_execution_router = "provider-cli"` in settings/config references.
- Updated inventory artifacts for the new CJS helper and kept workflow size under budget.

## Key Files Modified

- `get-shit-done/workflows/execute-phase.md`
- `get-shit-done/workflows/settings-advanced.md`
- `get-shit-done/references/planning-config.md`
- `docs/INVENTORY.md`
- `docs/INVENTORY-MANIFEST.json`

## Verification

- Static workflow checks found `agent_execution_bindings`, `provider-cli`, `codex exec`, `claude -p`, and lower-priority `cross_ai_execution` wording.
- CLI fixture rendered `openai/gpt-5.5` as `codex exec --model gpt-5.5 --full-auto -C ... < /tmp/plan.md`.
- `npm run test:hermes`
- `npm test` — 5935/5935 pass
- `npm run lint:tests`

## Self-Check: PASSED

The workflow now states that provider-cli consumes SDK execution bindings as canonical dispatch input. Valid provider-cli bindings take priority over legacy cross-AI fallback; non-zero selected CLI execution is a failure and must not silently retry through another runtime.
