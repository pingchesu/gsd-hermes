# Verification: Phase 8.3 Strict Regression Coverage

**Date:** 2026-04-29
**Status:** Passed
**Mode:** Hermes degraded sequential inline execution; no Task-style subagents were used.

## Commands run

```bash
node --test tests/agent-execution-router.test.cjs tests/provider-cli-dispatch.test.cjs tests/hermes-provider-routing-regression.test.cjs
npm run test:hermes
npm test
npm run lint:tests
```

## Results

All required gates passed.

- Focused provider routing regression suite: passed.
- Hermes compatibility gate: passed.
- Full test suite: passed.
  - `5942/5942` tests passed.
- Test lint gate: passed.
  - `326` test files checked.
  - `0` violations.

## Manual/static routing evidence

`node bin/gsd-sdk.js query init.execute-phase 8.3` reports:

```text
executor: openai/gpt-5.5 → codex-cli / gpt-5.5
verifier: anthropic/claude-opus-4-7 → claude-cli / claude-opus-4-7
```

The new tests also assert:

```text
OpenAI binding command output does not contain claude / claude -p / --agent.
Anthropic binding command output does not contain codex / codex exec / --full-auto.
Unsupported or incomplete bindings fail before command rendering.
Missing required CLI preflight returns an actionable diagnostic without making provider calls.
```

## Acceptance criteria

- [x] Anthropic/OpenAI provider detection and CLI normalization are covered by unit tests.
- [x] `init.execute-phase` fixture tests prove executor/verifier bindings route to the intended drivers.
- [x] Command renderer tests prove OpenAI cannot render `claude -p` and Claude cannot render `codex exec`.
- [x] Unsupported provider and missing CLI paths fail fast with actionable diagnostics.
- [x] `npm run test:hermes`, `npm test`, and `npm run lint:tests` passed before Phase 8.3 was marked complete.

## Proof boundary

This verification is credential-free. It proves GSD's deterministic driver selection, model argument normalization, command rendering, and workflow guardrails. It does not prove wire-level Anthropic/OpenAI API routing inside the external CLI tools.
