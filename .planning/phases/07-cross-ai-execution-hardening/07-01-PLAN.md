---
phase: 07-cross-ai-execution-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/execute-phase.md
  - tests/cross-ai-execution.test.cjs
  - sdk/src/query/runtime-model-validation.ts
  - sdk/src/query/runtime-model-contract.ts
  - sdk/src/query/config-mutation.ts
autonomous: true
requirements:
  - XAI-01
  - XAI-02
  - XAI-03
  - XAI-04
user_setup: []
must_haves:
  truths:
    - Cross-AI routing is explicit and deterministic: CLI flags override config, and config overrides plan frontmatter.
    - Direct runtime binding remains the preferred path whenever the active runtime can truly honor the configured provider/model.
    - Cross-AI execution is only required when direct runtime binding is unavailable or explicitly forced.
    - Missing or malformed cross-AI configuration fails early with actionable remediation instead of implicit fallback.
    - External command output cannot count as success unless it satisfies the minimum accepted SUMMARY contract.
  artifacts:
    - get-shit-done/workflows/execute-phase.md
    - tests/cross-ai-execution.test.cjs
    - sdk/src/query/runtime-model-validation.ts
  key_links:
    - get-shit-done/workflows/execute-phase.md uses the same deterministic routing order documented in .planning/phases/07-cross-ai-execution-hardening/07-CONTEXT.md.
    - tests/cross-ai-execution.test.cjs locks both the “recommend only” Phase 6 boundary and the new Phase 7 routing/result-hardening behavior.
    - Hermes mixed-provider direct bindings remain validated by sdk/src/query/runtime-model-contract.ts and must not be forced through cross-AI.
---

<objective>
Harden the cross-AI routing contract so the system can explicitly decide when external delegation is required and reject misconfigured or malformed cross-AI runs before they can masquerade as success.

Purpose: Phase 7 begins by turning the current workflow-level cross-AI step into a deterministic, validated path for provider-restricted runtimes, while preserving Hermes-native mixed-provider direct execution when supported.
Output: Locked routing precedence, early misconfiguration detection, a minimum accepted external SUMMARY contract, and regression tests proving malformed cross-AI output or missing command setup cannot silently pass.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-cross-ai-execution-hardening/07-CONTEXT.md
@.planning/phases/07-cross-ai-execution-hardening/07-RESEARCH.md
@.planning/phases/07-cross-ai-execution-hardening/07-PATTERNS.md
@.planning/phases/07-cross-ai-execution-hardening/07-VALIDATION.md
@.planning/phases/07-cross-ai-execution-hardening/07-IMPLEMENTATION-STRATEGY.md
@.planning/phases/06-strict-binding-enforcement/06-01-SUMMARY.md
@get-shit-done/workflows/execute-phase.md
@get-shit-done/workflows/review.md
@get-shit-done/workflows/ship.md
@tests/cross-ai-execution.test.cjs
@sdk/src/query/runtime-model-contract.ts
@sdk/src/query/runtime-model-validation.ts
@sdk/src/query/config-mutation.ts
@docs/CONFIGURATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Introduce a minimal reusable cross-AI routing and result-validation contract</name>
  <files>get-shit-done/workflows/execute-phase.md, tests/cross-ai-execution.test.cjs, sdk/src/query/runtime-model-validation.ts</files>
  <read_first>get-shit-done/workflows/execute-phase.md, get-shit-done/workflows/review.md, get-shit-done/workflows/ship.md, .planning/phases/07-cross-ai-execution-hardening/07-RESEARCH.md</read_first>
  <action>Define and implement the smallest reusable contract needed for Phase 7. Lock routing precedence as `--no-cross-ai` / `--cross-ai` flags first, config second, plan frontmatter lower priority. Add or expose helper logic only if needed to make the routing/result semantics testable; avoid introducing a new orchestration framework. Also define the minimum accepted external SUMMARY contract: successful output must contain a completion summary, what changed, verification results, and any failure/deviation section when applicable. Preserve the Phase 6 rule that direct Hermes-native mixed-provider binding is not auto-routed into cross-AI when direct execution is valid.</action>
  <verify>cd /home/whiskey/workspace/project/central/v2/gsd-hermes && node --test tests/cross-ai-execution.test.cjs</verify>
  <acceptance_criteria>
    - The routing order is explicit and deterministic across CLI flags, config, and plan frontmatter.
    - The workflow or helper contract distinguishes “cross-AI required”, “cross-AI forced”, “cross-AI disabled”, and “direct runtime binding valid”.
    - A malformed or incomplete external SUMMARY candidate is treated as failure, not success.
    - No Task 1 change forces valid Hermes direct bindings through the external cross-AI path.
  </acceptance_criteria>
  <done>Phase 7 now has a locked, testable definition of when cross-AI should run and what minimum output counts as success.</done>
</task>

<task type="auto">
  <name>Task 2: Fail early on cross-AI misconfiguration and malformed result handling</name>
  <files>get-shit-done/workflows/execute-phase.md, tests/cross-ai-execution.test.cjs, docs/CONFIGURATION.md</files>
  <read_first>get-shit-done/workflows/execute-phase.md, tests/cross-ai-execution.test.cjs, docs/CONFIGURATION.md, .planning/phases/07-cross-ai-execution-hardening/07-VALIDATION.md</read_first>
  <action>Harden the execution path so missing `workflow.cross_ai_command`, timeout exit, non-zero exit, malformed summary, and partial execution all produce actionable failure handling. Keep the current retry / skip / abort model, but make it explicit that none of these failure classes may silently mark the plan complete. Update operator-facing configuration docs only as far as necessary to explain the hardened contract and the fact that Hermes direct mixed-provider support remains a separate direct-binding path.</action>
  <verify>cd /home/whiskey/workspace/project/central/v2/gsd-hermes && node --test tests/cross-ai-execution.test.cjs</verify>
  <acceptance_criteria>
    - Cross-AI-required runs fail fast when `workflow.cross_ai_command` is missing.
    - Timeout, non-zero exit, malformed summary, and partial execution are all surfaced distinctly enough for retry/skip/abort handling.
    - Successful cross-AI execution still depends on orchestrator-side acceptance of the resulting SUMMARY artifact.
    - Documentation updates explain the minimum result contract and explicit config requirements without claiming silent fallback behavior.
  </acceptance_criteria>
  <done>Cross-AI execution can no longer silently succeed on missing command setup or malformed external results.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] cd /home/whiskey/workspace/project/central/v2/gsd-hermes && node --test tests/cross-ai-execution.test.cjs
- [ ] Cross-AI routing precedence is deterministic and documented as CLI > config > frontmatter
- [ ] Missing `workflow.cross_ai_command` fails early when cross-AI routing is required
- [ ] Malformed or incomplete external SUMMARY output does not count as success
- [ ] Hermes mixed-provider direct bindings remain valid without requiring cross-AI
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 7 adds explicit cross-AI hardening without turning Hermes-native provider switching into a mandatory external delegation path
- Workflow changes remain narrow enough to keep future upstream sync cheap
</success_criteria>

<output>
After completion, create `.planning/phases/07-cross-ai-execution-hardening/07-01-SUMMARY.md`
</output>
