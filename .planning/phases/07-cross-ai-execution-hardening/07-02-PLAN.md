---
phase: 07-cross-ai-execution-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - get-shit-done/workflows/execute-phase.md
  - docs/CONFIGURATION.md
  - sdk/src/phase-runner.ts
  - sdk/src/init-runner.ts
  - tests/cross-ai-execution.test.cjs
  - sdk/src/query/init.test.ts
autonomous: true
requirements:
  - XAI-02
  - XAI-03
  - XAI-04
  - WDI-03
user_setup: []
must_haves:
  truths:
    - Orchestrator remains the source of truth for shared planning and execution state even when external AI performs delegated work.
    - Cross-AI execution may produce candidate code changes and summary output, but success is only finalized through orchestrator-owned acceptance and state updates.
    - Workflow behavior distinguishes between direct runtime support and explicit external delegation instead of flattening both paths into one abstraction.
    - Docs explain when to use Hermes direct multi-provider binding versus when to enable cross-AI execution for provider-restricted runtimes.
  artifacts:
    - get-shit-done/workflows/execute-phase.md
    - docs/CONFIGURATION.md
    - tests/cross-ai-execution.test.cjs
  key_links:
    - get-shit-done/workflows/execute-phase.md preserves orchestrator-owned state finalization after external execution completes.
    - docs/CONFIGURATION.md explains direct Hermes mixed-provider binding separately from `workflow.cross_ai_execution` fallback behavior.
    - Regression tests cover both the hardened external path and the preserved direct Hermes path.
---

<objective>
Integrate the hardened cross-AI contract into execution workflows and documentation without widening divergence from upstream more than necessary.

Purpose: After the Phase 7 routing and result contract is locked, this plan aligns workflow execution ownership and docs so maintainers understand exactly when to rely on Hermes direct provider switching and when to enable explicit external cross-AI delegation.
Output: execute-phase workflow semantics aligned with the hardened contract, docs clarifying the two paths, and regression tests proving both the external path and the direct Hermes path behave truthfully.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-cross-ai-execution-hardening/07-CONTEXT.md
@.planning/phases/07-cross-ai-execution-hardening/07-RESEARCH.md
@.planning/phases/07-cross-ai-execution-hardening/07-PATTERNS.md
@.planning/phases/07-cross-ai-execution-hardening/07-VALIDATION.md
@.planning/phases/07-cross-ai-execution-hardening/07-01-PLAN.md
@get-shit-done/workflows/execute-phase.md
@docs/CONFIGURATION.md
@tests/cross-ai-execution.test.cjs
@sdk/src/query/init.test.ts
@sdk/src/phase-runner.ts
@sdk/src/init-runner.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Align execute-phase state ownership and workflow behavior with the hardened cross-AI contract</name>
  <files>get-shit-done/workflows/execute-phase.md, tests/cross-ai-execution.test.cjs, sdk/src/phase-runner.ts</files>
  <read_first>get-shit-done/workflows/execute-phase.md, .planning/phases/07-cross-ai-execution-hardening/07-CONTEXT.md, .planning/phases/07-cross-ai-execution-hardening/07-PATTERNS.md</read_first>
  <action>Update the execute-phase workflow so external cross-AI success is always normalized through orchestrator-owned summary acceptance and shared state updates. Keep retry / skip / abort semantics conservative. If any SDK runner touch-up is needed, keep it narrow and adapter-like rather than pushing broad cross-AI orchestration into `phase-runner.ts`. The main goal is to make ownership boundaries explicit and truthful, not to redesign the runner stack.</action>
  <verify>cd /home/whiskey/workspace/project/central/v2/gsd-hermes && node --test tests/cross-ai-execution.test.cjs</verify>
  <acceptance_criteria>
    - Workflow text makes it explicit that external AI output is candidate execution output, not authoritative shared-state mutation.
    - Successful cross-AI plans are only removed from normal execution after orchestrator-side acceptance conditions are met.
    - Any `phase-runner.ts` change stays narrow and does not attempt to replicate the entire markdown workflow inside the SDK runner.
  </acceptance_criteria>
  <done>Cross-AI execution is integrated into the workflow with truthful ownership boundaries and no silent state drift.</done>
</task>

<task type="auto">
  <name>Task 2: Document the two supported provider-switching paths and lock regressions</name>
  <files>docs/CONFIGURATION.md, tests/cross-ai-execution.test.cjs, sdk/src/query/init.test.ts</files>
  <read_first>docs/CONFIGURATION.md, .planning/PROJECT.md, .planning/phases/07-cross-ai-execution-hardening/07-RESEARCH.md, sdk/src/query/init.test.ts</read_first>
  <action>Update configuration documentation so maintainers can distinguish clearly between: (a) direct mixed-provider binding under `runtime: hermes`, and (b) explicit `cross_ai_execution` for runtimes that cannot honor a configured binding directly. Extend regressions only as needed to prove this distinction remains true after Phase 7 changes. The docs must avoid implying silent provider translation or automatic fallback.</action>
  <verify>cd /home/whiskey/workspace/project/central/v2/gsd-hermes/sdk && npx vitest run src/query/init.test.ts && cd .. && node --test tests/cross-ai-execution.test.cjs</verify>
  <acceptance_criteria>
    - Docs explicitly separate Hermes direct binding from external cross-AI delegation.
    - Docs explain that unsupported direct bindings still fail fast rather than silently degrading.
    - Regression coverage proves Hermes mixed-provider init still succeeds while provider-restricted runtimes continue using the explicit cross-AI path only when configured.
  </acceptance_criteria>
  <done>Operator-facing docs and tests now reflect the repo's truthful two-path provider switching model.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] cd /home/whiskey/workspace/project/central/v2/gsd-hermes && node --test tests/cross-ai-execution.test.cjs
- [ ] cd /home/whiskey/workspace/project/central/v2/gsd-hermes/sdk && npx vitest run src/query/init.test.ts src/query/config-query.test.ts
- [ ] execute-phase workflow text keeps orchestrator-owned state finalization explicit
- [ ] docs/CONFIGURATION.md clearly distinguishes Hermes direct binding from external cross-AI delegation
- [ ] no workflow text implies silent provider translation or automatic fallback
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 7 remains focused on explicit fallback-path hardening rather than broad workflow redesign
- The resulting semantics are truthful, explicit, and cheap to maintain across upstream syncs
</success_criteria>

<output>
After completion, create `.planning/phases/07-cross-ai-execution-hardening/07-02-SUMMARY.md`
</output>
