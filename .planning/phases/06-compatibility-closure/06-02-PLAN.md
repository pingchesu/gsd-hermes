---
phase: 06-compatibility-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - docs/upstream-sync.md
  - tests/hermes-compatibility-closure.test.cjs
autonomous: true
requirements:
  - COMP-02
user_setup: []
must_haves:
  truths:
    - "Upstream sync has a copy-pasteable validation checklist."
    - "Release-blocker criteria distinguish deterministic Hermes regressions from optional real-Hermes skips."
  artifacts:
    - path: docs/upstream-sync.md
      provides: "Post-sync validation and release-blocker checklist"
      contains: "Post-Sync Validation Checklist"
    - path: tests/hermes-compatibility-closure.test.cjs
      provides: "Docs regression for upstream sync checklist"
      contains: "Post-Sync Validation Checklist"
---

<objective>
Formalize the upstream sync checklist so maintainers can repeat a merge from
upstream and validate Hermes compatibility without guessing the test sequence or
release-blocker rules.
</objective>

<execution_context>
@/home/whiskey/.codex/get-shit-done/workflows/execute-plan.md
@/home/whiskey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-compatibility-closure/06-CONTEXT.md
@.planning/phases/06-compatibility-closure/06-RESEARCH.md
@.planning/phases/06-compatibility-closure/06-PATTERNS.md
@docs/upstream-sync.md
@docs/fork-ownership.md
@tests/hermes-compatibility-closure.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add post-sync validation checklist</name>
  <files>docs/upstream-sync.md</files>
  <read_first>docs/upstream-sync.md, docs/fork-ownership.md, .planning/phases/06-compatibility-closure/06-CONTEXT.md</read_first>
  <action>In `docs/upstream-sync.md`, after `## Steady-State Merge Flow`, add a `## Post-Sync Validation Checklist` section. The section must include an ordered checklist with these exact command/code strings: `git status --short`, `git fetch upstream`, `git merge upstream/main`, `npm run test:hermes`, and `npm test`. It must instruct maintainers to classify conflicts and drift using the exact ownership labels `Upstream base`, `Hermes adapter seam`, and `Downstream governance`. It must state that `npm run test:hermes` is the targeted Hermes compatibility gate and `npm test` is the full-suite gate when feasible.</action>
  <verify>rg -n "Post-Sync Validation Checklist|npm run test:hermes|npm test|Upstream base|Hermes adapter seam|Downstream governance" docs/upstream-sync.md</verify>
  <acceptance_criteria>
    - `docs/upstream-sync.md` contains `## Post-Sync Validation Checklist`
    - `docs/upstream-sync.md` contains `git status --short`
    - `docs/upstream-sync.md` contains `npm run test:hermes`
    - `docs/upstream-sync.md` contains `npm test`
    - `docs/upstream-sync.md` contains `Upstream base`
    - `docs/upstream-sync.md` contains `Hermes adapter seam`
    - `docs/upstream-sync.md` contains `Downstream governance`
  </acceptance_criteria>
  <done>Maintainers have a repeatable post-sync validation sequence.</done>
</task>

<task type="auto">
  <name>Task 2: Define release-blocker criteria and docs regression</name>
  <files>docs/upstream-sync.md, tests/hermes-compatibility-closure.test.cjs</files>
  <read_first>docs/upstream-sync.md, tests/hermes-compatibility-closure.test.cjs, .planning/phases/06-compatibility-closure/06-CONTEXT.md</read_first>
  <action>Add a `## Release Blocker Criteria` section to `docs/upstream-sync.md`. It must say regressions in install, command discovery, core workflow lifecycle, update, uninstall, doctor, or compatibility docs are release blockers. It must also say optional real Hermes CLI unavailability is a warning/skip, not a release blocker by itself. Extend `tests/hermes-compatibility-closure.test.cjs` with a `Hermes upstream sync documentation` suite that reads `docs/upstream-sync.md` and asserts the file contains `Post-Sync Validation Checklist`, `Release Blocker Criteria`, `npm run test:hermes`, `npm test`, `optional real Hermes CLI unavailability`, and all three ownership labels.</action>
  <verify>node --test tests/hermes-compatibility-closure.test.cjs && npm run test:hermes</verify>
  <acceptance_criteria>
    - `docs/upstream-sync.md` contains `## Release Blocker Criteria`
    - `docs/upstream-sync.md` contains `install, command discovery, core workflow lifecycle, update, uninstall, doctor, or compatibility docs`
    - `docs/upstream-sync.md` contains `optional real Hermes CLI unavailability`
    - `tests/hermes-compatibility-closure.test.cjs` contains `Hermes upstream sync documentation`
    - `node --test tests/hermes-compatibility-closure.test.cjs` exits 0
    - `npm run test:hermes` exits 0
  </acceptance_criteria>
  <done>Release blockers and optional-Hermes skip behavior are explicit and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| upstream merge -> downstream Hermes support | Maintainers need deterministic gates before treating a synced fork as compatible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- |
| T-06-02 | R | upstream sync docs | mitigate | Add docs regression tests for checklist commands, blocker criteria, and ownership labels. |
</threat_model>

<verification>
- [ ] `node --test tests/hermes-compatibility-closure.test.cjs`
- [ ] `npm run test:hermes`
</verification>

<success_criteria>
- `docs/upstream-sync.md` contains a post-sync validation checklist.
- The checklist routes drift through ownership labels.
- Release-blocker criteria are explicit and regression-tested.
</success_criteria>

<output>
After completion, create `.planning/phases/06-compatibility-closure/06-02-SUMMARY.md`.
</output>

