---
phase: 06-compatibility-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - scripts/validate-hermes-compat.cjs
  - tests/hermes-compatibility-closure.test.cjs
autonomous: true
requirements:
  - COMP-01
user_setup: []
must_haves:
  truths:
    - "Maintainers have one stable targeted command for Hermes compatibility validation."
    - "The targeted command runs deterministic Hermes tests without requiring a real Hermes binary."
  artifacts:
    - path: package.json
      provides: "npm script entry point for Hermes compatibility validation"
      contains: "\"test:hermes\""
    - path: scripts/validate-hermes-compat.cjs
      provides: "Targeted Hermes compatibility test runner"
      contains: "tests/hermes-core-workflow.test.cjs"
    - path: tests/hermes-compatibility-closure.test.cjs
      provides: "Regression coverage for compatibility validation wiring"
      contains: "Hermes compatibility validation command"
---

<objective>
Create a deterministic Hermes compatibility validation command that packages
the existing high-value Hermes regression tests behind one stable entry point.
</objective>

<execution_context>
@/home/whiskey/.codex/get-shit-done/workflows/execute-plan.md
@/home/whiskey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-compatibility-closure/06-CONTEXT.md
@.planning/phases/06-compatibility-closure/06-RESEARCH.md
@.planning/phases/06-compatibility-closure/06-VALIDATION.md
@.planning/phases/06-compatibility-closure/06-PATTERNS.md
@package.json
@scripts/run-tests.cjs
@tests/hermes-core-workflow.test.cjs
@tests/hermes-lifecycle.test.cjs
@tests/hermes-docs.test.cjs
@tests/multi-runtime-select.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add targeted Hermes compatibility runner</name>
  <files>package.json, scripts/validate-hermes-compat.cjs</files>
  <read_first>package.json, scripts/run-tests.cjs, .planning/phases/06-compatibility-closure/06-CONTEXT.md</read_first>
  <action>Add a package script named `test:hermes` with value `node scripts/validate-hermes-compat.cjs`. Create `scripts/validate-hermes-compat.cjs` as a Node executable using `node:child_process` `spawnSync`. The script must run `process.execPath` with args `['--test', 'tests/hermes-core-workflow.test.cjs', 'tests/hermes-lifecycle.test.cjs', 'tests/hermes-docs.test.cjs', 'tests/multi-runtime-select.test.cjs']`, use `{ stdio: 'inherit' }`, and exit with the child status or `1` if status is null. Include a short header line `Running Hermes compatibility validation...` before spawning.</action>
  <verify>npm run test:hermes</verify>
  <acceptance_criteria>
    - `package.json` contains `"test:hermes": "node scripts/validate-hermes-compat.cjs"`
    - `scripts/validate-hermes-compat.cjs` contains `Running Hermes compatibility validation`
    - `scripts/validate-hermes-compat.cjs` contains `tests/hermes-core-workflow.test.cjs`
    - `scripts/validate-hermes-compat.cjs` contains `tests/hermes-lifecycle.test.cjs`
    - `scripts/validate-hermes-compat.cjs` contains `tests/hermes-docs.test.cjs`
    - `scripts/validate-hermes-compat.cjs` contains `tests/multi-runtime-select.test.cjs`
    - `npm run test:hermes` exits 0
  </acceptance_criteria>
  <done>Hermes compatibility can be validated with one targeted npm command.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage for validation wiring</name>
  <files>tests/hermes-compatibility-closure.test.cjs</files>
  <read_first>tests/hermes-docs.test.cjs, package.json, scripts/validate-hermes-compat.cjs</read_first>
  <action>Create `tests/hermes-compatibility-closure.test.cjs` using `node:test`, `node:assert/strict`, and `node:fs`. Add a suite named `Hermes compatibility validation command`. Add one test that parses `package.json` and asserts `scripts['test:hermes']` equals `node scripts/validate-hermes-compat.cjs`. Add one test that reads `scripts/validate-hermes-compat.cjs` and asserts it includes all four targeted test file paths and `spawnSync(process.execPath`. Do not execute the runner from this test; execution is covered by the plan verify command.</action>
  <verify>node --test tests/hermes-compatibility-closure.test.cjs && npm run test:hermes</verify>
  <acceptance_criteria>
    - `tests/hermes-compatibility-closure.test.cjs` contains `Hermes compatibility validation command`
    - `tests/hermes-compatibility-closure.test.cjs` contains `scripts['test:hermes']`
    - `tests/hermes-compatibility-closure.test.cjs` contains `spawnSync(process.execPath`
    - `node --test tests/hermes-compatibility-closure.test.cjs` exits 0
    - `npm run test:hermes` exits 0
  </acceptance_criteria>
  <done>The compatibility validation entry point is itself regression-checked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| npm script -> local test runner | Maintainers trust `npm run test:hermes` to run the intended deterministic checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- |
| T-06-01 | T | validation runner | mitigate | Add a regression test that asserts the runner invokes exactly the targeted Hermes test files through `process.execPath`. |
</threat_model>

<verification>
- [ ] `node --test tests/hermes-compatibility-closure.test.cjs`
- [ ] `npm run test:hermes`
</verification>

<success_criteria>
- `npm run test:hermes` runs the targeted Hermes compatibility tests.
- The validation runner does not require a real Hermes binary.
- Validation command wiring is covered by a deterministic test.
</success_criteria>

<output>
After completion, create `.planning/phases/06-compatibility-closure/06-01-SUMMARY.md`.
</output>

