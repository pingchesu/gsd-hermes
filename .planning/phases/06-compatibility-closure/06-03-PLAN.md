---
phase: 06-compatibility-closure
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - docs/hermes-compatibility.md
  - docs/hermes-install.md
  - docs/README.md
  - tests/hermes-docs.test.cjs
autonomous: true
requirements:
  - COMP-01
  - COMP-02
user_setup: []
must_haves:
  truths:
    - "Compatibility docs tie Hermes support claims to validation evidence."
    - "Maintenance docs keep native local Hermes install out of scope."
  artifacts:
    - path: docs/hermes-compatibility.md
      provides: "Test-linked compatibility matrix and maintenance rules"
      contains: "Validation evidence"
    - path: docs/hermes-install.md
      provides: "Install guide link to compatibility validation"
      contains: "npm run test:hermes"
    - path: tests/hermes-docs.test.cjs
      provides: "Docs regression for compatibility closure"
      contains: "documents Phase 6 compatibility closure"
---

<objective>
Publish compatibility and maintenance guidance that turns Phase 6 support
claims into a test-linked compatibility matrix and a repeatable maintenance
contract.
</objective>

<execution_context>
@/home/whiskey/.codex/get-shit-done/workflows/execute-plan.md
@/home/whiskey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-compatibility-closure/06-CONTEXT.md
@.planning/phases/06-compatibility-closure/06-RESEARCH.md
@.planning/phases/06-compatibility-closure/06-PATTERNS.md
@docs/hermes-compatibility.md
@docs/hermes-install.md
@docs/README.md
@tests/hermes-docs.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Link compatibility matrix to validation evidence</name>
  <files>docs/hermes-compatibility.md, tests/hermes-docs.test.cjs</files>
  <read_first>docs/hermes-compatibility.md, tests/hermes-docs.test.cjs, .planning/phases/06-compatibility-closure/06-CONTEXT.md</read_first>
  <action>Update `docs/hermes-compatibility.md` so the compatibility matrix has columns `Surface`, `Status`, `Validation evidence`, and `Notes`. Add `npm run test:hermes` as validation evidence for global install, project-linked mode, command discovery, core workflow execution, update/uninstall/doctor, and upstream sync routine. Keep native local Hermes install status as `out of scope` and note `Project-linked mode uses skills.external_dirs instead.` Add a `## Maintenance Contract` section that states `npm run test:hermes` is required after Hermes adapter seam changes and upstream syncs, `npm test` is the full-suite release gate when feasible, and docs must record known gaps instead of claiming unsupported parity.</action>
  <verify>node --test tests/hermes-docs.test.cjs && npm run test:hermes</verify>
  <acceptance_criteria>
    - `docs/hermes-compatibility.md` contains `Validation evidence`
    - `docs/hermes-compatibility.md` contains `npm run test:hermes`
    - `docs/hermes-compatibility.md` contains `## Maintenance Contract`
    - `docs/hermes-compatibility.md` contains `Project-linked mode uses skills.external_dirs instead.`
    - `docs/hermes-compatibility.md` contains `known gaps instead of claiming unsupported parity`
    - `node --test tests/hermes-docs.test.cjs` exits 0
  </acceptance_criteria>
  <done>Compatibility support claims are tied to validation evidence and maintenance rules.</done>
</task>

<task type="auto">
  <name>Task 2: Update install/docs index guidance and docs tests</name>
  <files>docs/hermes-install.md, docs/README.md, tests/hermes-docs.test.cjs</files>
  <read_first>docs/hermes-install.md, docs/README.md, tests/hermes-docs.test.cjs, docs/hermes-compatibility.md</read_first>
  <action>Add a `## Compatibility Validation` section to `docs/hermes-install.md` that tells maintainers to run `npm run test:hermes` after install-path, command-discovery, lifecycle, or upstream-sync changes, and `npm test` before release when feasible. Update `docs/README.md` so the `Hermes Compatibility and Guardrails` row says it includes the validation matrix and maintenance contract, and the `Upstream Sync Workflow` row says it includes the post-sync validation checklist. Extend `tests/hermes-docs.test.cjs` with assertions named `documents Phase 6 compatibility closure` that check `docs/hermes-install.md` includes `## Compatibility Validation`, `npm run test:hermes`, and `npm test`; `docs/README.md` includes `validation matrix and maintenance contract`; and `docs/README.md` includes `post-sync validation checklist`.</action>
  <verify>node --test tests/hermes-docs.test.cjs && npm run test:hermes && npm test</verify>
  <acceptance_criteria>
    - `docs/hermes-install.md` contains `## Compatibility Validation`
    - `docs/hermes-install.md` contains `npm run test:hermes`
    - `docs/hermes-install.md` contains `npm test`
    - `docs/README.md` contains `validation matrix and maintenance contract`
    - `docs/README.md` contains `post-sync validation checklist`
    - `tests/hermes-docs.test.cjs` contains `documents Phase 6 compatibility closure`
    - `node --test tests/hermes-docs.test.cjs` exits 0
    - `npm run test:hermes` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Operator-facing docs explain how to validate compatibility after maintenance work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| docs -> user expectations | Users and maintainers rely on compatibility docs to know what Hermes actually supports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- |
| T-06-03 | R | compatibility docs | mitigate | Test exact docs strings for validation evidence, native-local boundary, and maintenance contract. |
</threat_model>

<verification>
- [ ] `node --test tests/hermes-docs.test.cjs`
- [ ] `npm run test:hermes`
- [ ] `npm test`
</verification>

<success_criteria>
- Compatibility matrix includes validation evidence.
- Install docs and docs index point to the Phase 6 validation path.
- Full suite passes before Phase 6 closure.
</success_criteria>

<output>
After completion, create `.planning/phases/06-compatibility-closure/06-03-SUMMARY.md`.
</output>

