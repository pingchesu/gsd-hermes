---
phase: 05-runtime-model-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - get-shit-done/bin/lib/core.cjs
  - get-shit-done/bin/lib/init.cjs
  - get-shit-done/bin/lib/model-profiles.cjs
  - tests/runtime-model-parity.test.cjs
  - tests/init.test.cjs
  - tests/bug-2516-inherit-model-execute-phase.test.cjs
  - tests/cross-ai-execution.test.cjs
autonomous: true
requirements:
  - RMC-03
  - RMC-01
  - RMC-02
user_setup: []
must_haves:
  truths:
    - Legacy CJS resolves the same config matrix to the same semantic outcomes as the SDK contract for explicit, inherit, runtime-default, and unsupported-agent cases.
    - Legacy init payload generation preserves Phase 5 binding semantics instead of flattening unsupported or inherited cases into silent fallback strings.
    - Phase 5 validation evidence proves cross_ai_execution is represented in the contract surface but does not claim routing hardening beyond recognition.
  artifacts:
    - get-shit-done/bin/lib/core.cjs
    - get-shit-done/bin/lib/init.cjs
    - tests/runtime-model-parity.test.cjs
    - tests/init.test.cjs
  key_links:
    - get-shit-done/bin/lib/core.cjs consumes the canonical SDK Phase 5 contract/resolution path directly, or a generated/shared adapter derived from it, instead of reimplementing equivalent branching logic.
    - tests/runtime-model-parity.test.cjs compares SDK and CJS semantic outcomes from one shared matrix instead of asserting only string equality in isolation.
    - get-shit-done/bin/lib/init.cjs keeps existing payload keys but serializes inherit/runtime-default semantics compatibly with the SDK adapters.
---

<objective>
Bring legacy CJS and root-level tests to parity with the SDK runtime-model contract.

Purpose: Phase 5 is not complete until SDK and legacy CJS produce equivalent runtime-model outcomes, because both surfaces are still active product entry points.
Output: Updated legacy resolver/init adapters plus parity and regression tests that lock SDK/CJS behavior together.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-runtime-model-contract/05-CONTEXT.md
@.planning/phases/05-runtime-model-contract/05-RESEARCH.md
@.planning/phases/05-runtime-model-contract/05-PATTERNS.md
@.planning/phases/05-runtime-model-contract/05-VALIDATION.md
@.planning/phases/05-runtime-model-contract/05-01-SUMMARY.md
@get-shit-done/bin/lib/core.cjs
@get-shit-done/bin/lib/init.cjs
@get-shit-done/bin/lib/model-profiles.cjs
@tests/init.test.cjs
@tests/cross-ai-execution.test.cjs
@tests/bug-2516-inherit-model-execute-phase.test.cjs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Port legacy CJS resolver semantics to the SDK contract baseline</name>
  <files>get-shit-done/bin/lib/core.cjs, get-shit-done/bin/lib/init.cjs, get-shit-done/bin/lib/model-profiles.cjs</files>
  <read_first>get-shit-done/bin/lib/core.cjs, get-shit-done/bin/lib/init.cjs, sdk/src/query/runtime-model-contract.ts, sdk/src/query/config-query.ts</read_first>
  <action>Update resolveModelInternal in get-shit-done/bin/lib/core.cjs so legacy CJS consumes the canonical SDK contract/resolution path from Plan 05-01 directly, or through a generated/shared adapter derived from it, rather than reimplementing equivalent branching logic locally. Preserve the same precedence and outcomes: explicit override wins, resolve_model_ids:"omit" is runtime-default binding, inherit remains a semantic binding, alias expansion for resolve_model_ids:true stays intact, and unknown/partially covered agents stop silently resolving to sonnet. Adjust get-shit-done/bin/lib/init.cjs so legacy init payload generation keeps existing public keys but uses contract-compatible serialization for inherit omission and runtime-default omission. Only touch get-shit-done/bin/lib/model-profiles.cjs if a profile-surface normalization is required for parity with SDK tests; do not expand into Phase 6/7 validation or routing work.</action>
  <verify>cd /home/whiskey/workspace/project/central/v2/gsd-hermes && npm test -- tests/init.test.cjs tests/bug-2516-inherit-model-execute-phase.test.cjs</verify>
  <acceptance_criteria>
    - get-shit-done/bin/lib/core.cjs no longer returns "sonnet" as the silent default for unknown agents.
    - Legacy CJS uses the canonical SDK resolver or a generated/shared adapter derived from it instead of maintaining a second hand-kept branch matrix.
    - get-shit-done/bin/lib/core.cjs still preserves override precedence and resolve_model_ids:true alias expansion while treating resolve_model_ids:"omit" as runtime-default binding.
    - get-shit-done/bin/lib/init.cjs keeps existing output field names such as executor_model/planner_model while using omission-compatible serialization for inherit/runtime-default outcomes.
    - No Task 1 change claims cross-AI routing hardening; only contract recognition/parity is added in Phase 5 scope.
  </acceptance_criteria>
  <done>Legacy CJS behavior matches the SDK contract semantics closely enough to support one parity matrix.</done>
</task>

<task type="auto">
  <name>Task 2: Add root-level parity and regression coverage for SDK/CJS equivalence</name>
  <files>tests/runtime-model-parity.test.cjs, tests/init.test.cjs, tests/bug-2516-inherit-model-execute-phase.test.cjs, tests/cross-ai-execution.test.cjs</files>
  <read_first>tests/init.test.cjs, tests/bug-2516-inherit-model-execute-phase.test.cjs, tests/cross-ai-execution.test.cjs, sdk/src/query/config-query.test.ts</read_first>
  <action>Create a root-level parity test that drives one shared config matrix through both SDK and legacy CJS and compares semantic outcomes for explicit override, inherit, runtime-default, resolve_model_ids:true alias expansion, and unknown-agent rejection. Update existing init/regression tests so they assert no path passes literal inherit where omission is required, and so runtime-default bindings remain migration-safe for non-Claude installs. Keep cross-ai-execution coverage limited to proving the config/contract surface still recognizes workflow.cross_ai_execution keys without claiming Phase 7 routing/hardening is complete.</action>
  <verify>cd /home/whiskey/workspace/project/central/v2/gsd-hermes && npm test -- tests/runtime-model-parity.test.cjs tests/init.test.cjs tests/bug-2516-inherit-model-execute-phase.test.cjs tests/cross-ai-execution.test.cjs</verify>
  <acceptance_criteria>
    - tests/runtime-model-parity.test.cjs exists and exercises one table-driven matrix against both SDK and CJS.
    - Parity assertions compare semantic outcomes for explicit, inherit, runtime-default, and unsupported-agent cases, not only raw strings.
    - tests/bug-2516-inherit-model-execute-phase.test.cjs or tests/init.test.cjs explicitly guards against passing literal "inherit" when omission is required.
    - tests/cross-ai-execution.test.cjs remains Phase-5-scoped: it verifies contract/config recognition, not full routing or malformed-output handling.
  </acceptance_criteria>
  <done>Root-level regression and parity tests lock SDK and legacy CJS to the same runtime-model contract behavior.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] cd /home/whiskey/workspace/project/central/v2/gsd-hermes/sdk && npm test -- config-query init
- [ ] cd /home/whiskey/workspace/project/central/v2/gsd-hermes && npm test -- tests/runtime-model-parity.test.cjs tests/init.test.cjs tests/bug-2516-inherit-model-execute-phase.test.cjs tests/cross-ai-execution.test.cjs
- [ ] SDK and legacy CJS produce equivalent semantic outcomes for the agreed Phase 5 matrix
- [ ] Legacy CJS parity is enforced through the canonical SDK resolver path or a generated/shared adapter derived from it, not duplicate branching logic
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- SDK and legacy CJS parity is enforced by automated tests and shared/canonical resolution logic instead of informal drift-prone matching
- Phase 5 ends with cross_ai_execution recognized in the contract surface but Phase 7 hardening still explicitly deferred
</success_criteria>

<output>
After completion, create `.planning/phases/05-runtime-model-contract/05-02-SUMMARY.md`
</output>
