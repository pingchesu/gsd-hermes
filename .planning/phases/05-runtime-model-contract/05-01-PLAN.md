---
phase: 05-runtime-model-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/src/query/runtime-model-contract.ts
  - sdk/src/query/config-query.ts
  - sdk/src/query/helpers.ts
  - sdk/src/query/init.ts
  - sdk/src/config.ts
  - sdk/src/query/config-mutation.ts
  - sdk/src/query/config-query.test.ts
  - sdk/src/query/init.test.ts
autonomous: true
requirements:
  - RMC-01
  - RMC-02
user_setup: []
must_haves:
  truths:
    - SDK exposes one canonical runtime capability registry that explicitly represents explicit binding, inherit binding, runtime-default binding, and cross-AI execution recognition for every supported runtime.
    - SDK model resolution returns structured binding semantics that preserve the difference between explicit, inherit, and runtime-default outcomes instead of collapsing everything to ad hoc strings.
    - Unknown agents and agents missing required contract coverage produce a structured unsupported result or error path that can be rejected before execution instead of silently defaulting to sonnet.
    - 'resolve_model_ids: "omit" remains a valid runtime-default binding state and does not get reinterpreted as missing config.'
    - Phase 5 explicitly defines the profile-surface migration policy for adaptive versus inherit, either by supporting both as valid inputs or by normalizing one to the other with compatibility coverage and a documented follow-up.
  artifacts:
    - sdk/src/query/runtime-model-contract.ts
    - sdk/src/query/config-query.ts
    - sdk/src/query/init.ts
    - sdk/src/config.ts
    - sdk/src/query/config-mutation.ts
    - sdk/src/query/config-query.test.ts
    - sdk/src/query/init.test.ts
  key_links:
    - sdk/src/query/config-query.ts imports the canonical contract/resolver from sdk/src/query/runtime-model-contract.ts instead of duplicating capability rules.
    - sdk/src/query/init.ts serializes structured binding results through an adapter helper so runtime-default and inherit semantics survive into init payload generation.
    - sdk/src/config.ts and sdk/src/query/config-mutation.ts recognize the runtime-model and cross_ai_execution config surface needed by the contract without implementing Phase 7 routing.
---

<objective>
Create the SDK-first runtime model contract and make SDK resolution/init paths consume it.

Purpose: Phase 5 needs one source of truth for runtime/model semantics before Phase 6 can enforce fail-fast validation and before legacy CJS can be brought to parity.
Output: A new canonical SDK contract module, typed config/config-mutation updates, and SDK tests that prove binding-state preservation and deferred cross-AI recognition.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-runtime-model-contract/05-CONTEXT.md
@.planning/phases/05-runtime-model-contract/05-RESEARCH.md
@.planning/phases/05-runtime-model-contract/05-PATTERNS.md
@.planning/phases/05-runtime-model-contract/05-VALIDATION.md
@sdk/src/query/config-query.ts
@sdk/src/query/init.ts
@sdk/src/query/helpers.ts
@sdk/src/config.ts
@sdk/src/query/config-mutation.ts
@sdk/src/query/config-query.test.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create the canonical SDK runtime model contract module</name>
  <files>sdk/src/query/runtime-model-contract.ts, sdk/src/query/config-query.ts, sdk/src/query/helpers.ts</files>
  <read_first>sdk/src/query/config-query.ts, sdk/src/query/helpers.ts, .planning/phases/05-runtime-model-contract/05-CONTEXT.md, .planning/phases/05-runtime-model-contract/05-RESEARCH.md</read_first>
  <action>Add a new SDK-first module at sdk/src/query/runtime-model-contract.ts that defines the explicit runtime capability registry, binding-kind/result types, unknown-agent policy, structured validation/error-shape contract, and adapter helpers for legacy string consumers. Reuse Runtime and SUPPORTED_RUNTIMES from sdk/src/query/helpers.ts rather than creating a second runtime enum. Refactor resolveModel in sdk/src/query/config-query.ts to delegate to a structured resolver that preserves override > resolve_model_ids:"omit" > profile lookup > inherit semantics, represents runtime-default as an intentional binding state, recognizes cross_ai_execution support in the contract model only, and stops returning silent sonnet fallback for unknown or partially covered agents. Make the canonical contract explicitly resolve the adaptive versus inherit migration risk by either supporting both profile inputs in Phase 5 or normalizing one to the other behind the contract with compatibility notes and tests.</action>
  <verify>cd /home/whiskey/workspace/project/central/v2/gsd-hermes/sdk && npm test -- config-query helpers</verify>
  <acceptance_criteria>
    - sdk/src/query/runtime-model-contract.ts exports a runtime capability registry keyed by supported runtimes and includes an explicit cross-AI capability field.
    - sdk/src/query/runtime-model-contract.ts defines structured binding/result metadata that distinguishes explicit, inherit, and runtime-default states.
    - sdk/src/query/config-query.ts imports from ./runtime-model-contract.js and no longer hardcodes unknown-agent fallback to "sonnet".
    - resolve_model_ids: "omit" is represented by a named runtime-default binding path, not only by an empty-string sentinel hidden in branch logic.
    - The contract states how adaptive and inherit are handled in Phase 5 and does not leave profile-surface normalization ambiguous.
  </acceptance_criteria>
  <done>SDK has a canonical runtime-model contract and resolveModel is an adapter over the structured resolver.</done>
</task>

<task type="auto">
  <name>Task 2: Thread structured binding semantics through SDK config and init surfaces</name>
  <files>sdk/src/query/init.ts, sdk/src/config.ts, sdk/src/query/config-mutation.ts</files>
  <read_first>sdk/src/query/init.ts, sdk/src/config.ts, sdk/src/query/config-mutation.ts, get-shit-done/bin/lib/config-schema.cjs</read_first>
  <action>Extend sdk/src/config.ts with first-class types for runtime, model_overrides, resolve_model_ids, and workflow.cross_ai_execution / workflow.cross_ai_command / workflow.cross_ai_timeout. Update sdk/src/query/config-mutation.ts so the SDK allowlist accepts the cross-AI workflow keys already present in legacy config schema, but do not implement routing or timeout enforcement here. In sdk/src/query/init.ts, replace raw getModelAlias-style flattening with adapter helpers from the contract module so init payload composition preserves the semantic difference between explicit model tokens, inherit omission, and runtime-default omission while keeping existing outward field names intact.</action>
  <verify>cd /home/whiskey/workspace/project/central/v2/gsd-hermes/sdk && npm test -- config-mutation init config-query</verify>
  <acceptance_criteria>
    - sdk/src/config.ts contains typed fields for runtime, model_overrides, resolve_model_ids, and workflow.cross_ai_execution/cross_ai_command/cross_ai_timeout.
    - sdk/src/query/config-mutation.ts accepts workflow.cross_ai_execution, workflow.cross_ai_command, and workflow.cross_ai_timeout without widening scope to Phase 7 routing behavior.
    - sdk/src/query/init.ts derives model tokens from the structured contract helpers instead of defaulting empty/falsey results to "sonnet".
    - init code preserves current payload field names while keeping inherit and runtime-default omission semantics distinct internally.
  </acceptance_criteria>
  <done>SDK config/init surfaces recognize the contract model and do not lose binding-state semantics before later validation layers run.</done>
</task>

<task type="auto">
  <name>Task 3: Add SDK tests for contract semantics, error shape, and migration safety</name>
  <files>sdk/src/query/config-query.test.ts, sdk/src/query/init.test.ts</files>
  <read_first>sdk/src/query/config-query.test.ts, sdk/src/query/init.ts, .planning/phases/05-runtime-model-contract/05-VALIDATION.md</read_first>
  <action>Add table-driven SDK tests that cover known-agent profile resolution, explicit overrides, resolve_model_ids:true, resolve_model_ids:"omit", override-plus-omit precedence, model_profile:"inherit", runtime detection precedence, unknown/partial agent rejection, adaptive versus inherit compatibility behavior, and init serialization behavior. Assert structured error/result objects include agent, runtime, configured or resolved model, rejection reason, and suggested fix shape required by Phase 5. Keep migration-safety coverage explicit by proving non-Claude runtime-default configurations using resolve_model_ids:"omit" still serialize to omission rather than a forced Claude alias, and by proving the chosen adaptive/inherit policy is locked by tests.</action>
  <verify>cd /home/whiskey/workspace/project/central/v2/gsd-hermes/sdk && npm test -- config-query init</verify>
  <acceptance_criteria>
    - sdk/src/query/config-query.test.ts contains cases for explicit, inherit, runtime-default, and unknown-agent outcomes.
    - sdk/src/query/config-query.test.ts asserts the structured error/result contract includes agent, runtime, model/rejection reason, and suggested fix fields.
    - sdk/src/query/init.test.ts proves inherit binding is omitted/serialized correctly and resolve_model_ids:"omit" remains a valid runtime-default path.
    - SDK tests explicitly cover the adaptive versus inherit policy, either as dual supported inputs or as a normalized compatibility path with follow-up called out.
    - No SDK test expects unknown agents to resolve to "sonnet" silently.
  </acceptance_criteria>
  <done>SDK contract behavior is covered by verifiable tests and migration-safe runtime-default behavior is locked in.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] cd /home/whiskey/workspace/project/central/v2/gsd-hermes/sdk && npm test -- config-query helpers config-mutation init
- [ ] Structured resolver tests prove explicit vs inherit vs runtime-default are distinct outcomes
- [ ] Structured resolver tests prove the adaptive versus inherit migration policy is explicit and covered
- [ ] SDK config mutation accepts cross-AI keys without implementing Phase 7 routing
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- SDK becomes the single source of truth for runtime-model semantics in Phase 5 scope
- The contract recognizes cross_ai_execution capability without claiming Phase 7 hardening is finished
</success_criteria>

<output>
After completion, create `.planning/phases/05-runtime-model-contract/05-01-SUMMARY.md`
</output>
