# Verification: Phase 8.4 Docs, Gates, and Release Readiness

**Status:** Passed
**Date:** 2026-04-29

## Acceptance criteria

| Requirement | Result | Evidence |
| --- | --- | --- |
| DOC-01 | Passed | `docs/CONFIGURATION.md` documents `workflow.agent_execution_router = "provider-cli"`, OpenAI/Codex and Anthropic/Claude examples, fail-fast behavior, and `cross_ai_execution` priority. |
| DOC-02 | Passed | `docs/hermes-compatibility.md` separates provider-routed CLI command proof, Hermes `delegate_task(model=...)` child-construction proof, and provider wire-level proof. |
| DOC-03 | Passed | `README.md`, `CHANGELOG.md`, and `docs/releases/v1.8.0-provider-routed-agent-execution.md` describe matching-driver-or-fail-fast behavior in operator language. |
| TEST-06 | Passed | Full local release-readiness gates passed. |

## Commands run

```bash
npm run test:hermes
npm test
npm run lint:tests
npm pack --dry-run --json
```

## Results

```text
npm run test:hermes: PASS
npm test: PASS (5942/5942)
npm run lint:tests: PASS (326 test files checked, 0 violations)
npm pack --dry-run --json: PASS (entryCount: 950)
```

Additional targeted checks before the full gates:

```bash
git diff --check
node --test tests/hermes-provider-routing-regression.test.cjs tests/hermes-docs.test.cjs tests/provider-cli-dispatch.test.cjs tests/agent-execution-router.test.cjs
node bin/gsd-sdk.js query init.execute-phase 8.4
node bin/gsd-sdk.js query phase-plan-index 8.4
```

## SDK routing evidence

`init.execute-phase 8.4` emitted:

```text
executor: openai/gpt-5.5 → codex-cli / gpt-5.5
verifier: anthropic/claude-opus-4-7 → claude-cli / claude-opus-4-7
```

## Release boundary

This phase prepared release documentation and local verification evidence only. It did not publish npm, create a GitHub Release, create a tag, or bump `package.json`.
