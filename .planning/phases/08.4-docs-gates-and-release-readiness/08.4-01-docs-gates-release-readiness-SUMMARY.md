# Summary: 08.4-01 Docs, Gates, and Release Readiness

**Status:** Complete
**Date:** 2026-04-29

## Completed

- Updated operator-facing README copy for provider-routed agent execution.
- Updated `docs/CONFIGURATION.md` with `workflow.agent_execution_router = "provider-cli"`, copy/paste config, route table, fail-fast behavior, and `cross_ai_execution` priority semantics.
- Updated `docs/hermes-compatibility.md` to distinguish provider-routed CLI command proof from Hermes native `delegate_task(model=...)` child-construction proof and provider wire-level proof.
- Updated `docs/COMMANDS.md` execute-phase docs and settings-advanced key table for provider routing.
- Updated `CHANGELOG.md` Unreleased entries for v1.8 draft behavior.
- Added `docs/releases/v1.8.0-provider-routed-agent-execution.md` with operator configuration, CLI route table, proof boundary, and publish note.

## Routing contract documented

```text
model_overrides.gsd-executor = "openai/gpt-5.5"
→ codex-cli
→ codex exec --model gpt-5.5

model_overrides.gsd-verifier = "anthropic/claude-opus-4-7"
→ claude-cli
→ claude -p --model claude-opus-4-7
```

The docs state matching driver or fail-fast and avoid claiming external CLI wire-level provider proof.

## Verification

- `npm run test:hermes` — passed
- `npm test` — passed (`5942/5942`)
- `npm run lint:tests` — passed (`326` test files checked, `0` violations)
- `npm pack --dry-run --json` — passed (`gsd-hermes@1.7.0`, `entryCount: 950`)
- `node bin/gsd-sdk.js query init.execute-phase 8.4` — executor routes to `codex-cli`, verifier routes to `claude-cli`
- `node bin/gsd-sdk.js query phase-plan-index 8.4` — plan indexed before execution; summary now closes the plan

## Non-goals respected

- No npm publish attempted.
- No GitHub Release created.
- No `v1.8.0` tag created.
- No package version bump performed.
