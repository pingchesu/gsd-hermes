---
phase: 05-lifecycle-tooling
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - bin/install.js
  - tests/hermes-lifecycle.test.cjs
autonomous: true
requirements:
  - QUAL-01
user_setup: []
must_haves:
  truths:
    - "Hermes doctor is read-only by default."
    - "Doctor findings include severity, path, message, and suggested fix."
  artifacts:
    - path: bin/install.js
      provides: "Hermes doctor helper and CLI surface"
      contains: "doctorHermesInstall"
    - path: tests/hermes-lifecycle.test.cjs
      provides: "Hermes doctor regression coverage"
      contains: "Hermes doctor diagnostics"
---

<objective>
Add read-only Hermes install-health diagnostics that can detect common global
and project-linked lifecycle problems without requiring a real Hermes runtime.
</objective>

<execution_context>
@/home/whiskey/.codex/get-shit-done/workflows/execute-plan.md
@/home/whiskey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-lifecycle-tooling/05-CONTEXT.md
@.planning/phases/05-lifecycle-tooling/05-RESEARCH.md
@.planning/phases/05-lifecycle-tooling/05-PATTERNS.md
@bin/install.js
@tests/hermes-lifecycle.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add structured Hermes doctor helper</name>
  <files>bin/install.js</files>
  <read_first>bin/install.js, .planning/phases/05-lifecycle-tooling/05-CONTEXT.md</read_first>
  <action>Add `doctorHermesInstall({ isGlobal = true, cwd = process.cwd(), homeDir = os.homedir(), explicitDir = explicitConfigDir } = {})`. It must return an object `{ runtime: 'hermes', mode: isGlobal ? 'global' : 'project-linked', targetDir, findings }`. Each finding must be `{ severity, path, message, fix }`. Check for missing `skills/gsd-help/SKILL.md`, invalid or missing `name: gsd-help` frontmatter, missing `gsd-file-manifest.json`, executable Claude path leaks matching `/(?:~|\\$HOME)\\/\\.claude\\b|\\.\\/\\.claude\\b/` in installed `SKILL.md` files, missing project-linked `config.yaml` entry, duplicate project-linked entry count greater than 1, and stale external_dirs entries pointing to missing paths. Export `doctorHermesInstall` under `GSD_TEST_MODE`.</action>
  <verify>node --test tests/hermes-lifecycle.test.cjs</verify>
  <acceptance_criteria>
    - `bin/install.js` contains `function doctorHermesInstall`
    - `bin/install.js` contains `severity`
    - `bin/install.js` contains `stale external_dirs`
    - `bin/install.js` contains `doctorHermesInstall,`
  </acceptance_criteria>
  <done>Hermes health checks are available as structured, testable helper output.</done>
</task>

<task type="auto">
  <name>Task 2: Add doctor CLI flag and stable output</name>
  <files>bin/install.js</files>
  <read_first>bin/install.js</read_first>
  <action>Parse `const hasDoctor = args.includes('--doctor');`. Add `--doctor` to help output with description `Check GSD Hermes install health without changing files`. If `hasDoctor && selectedRuntimes.includes('hermes')`, run `doctorHermesInstall({ isGlobal: hasGlobal || !hasLocal })`, print `Hermes doctor: {mode}` and one line per finding as `{severity}: {message} [{path}] -> {fix}`. If no findings, print `Hermes doctor: no issues found`. Exit without calling `install()` or `uninstall()`. If `--doctor` is used without `--hermes`, print `--doctor currently supports --hermes` and exit code 1.</action>
  <verify>node --test tests/hermes-lifecycle.test.cjs</verify>
  <acceptance_criteria>
    - `bin/install.js` contains `const hasDoctor = args.includes('--doctor')`
    - `bin/install.js` contains `Check GSD Hermes install health without changing files`
    - `bin/install.js` contains `Hermes doctor: no issues found`
    - `bin/install.js` contains `--doctor currently supports --hermes`
  </acceptance_criteria>
  <done>Operators can invoke a read-only Hermes doctor through the installer CLI.</done>
</task>

<task type="auto">
  <name>Task 3: Cover doctor diagnostics with tests</name>
  <files>tests/hermes-lifecycle.test.cjs</files>
  <read_first>tests/hermes-lifecycle.test.cjs, bin/install.js</read_first>
  <action>Add tests under `describe('Hermes doctor diagnostics', ...)`. Test 1 creates an empty temp HOME and asserts `doctorHermesInstall({ isGlobal: true, homeDir: tmpHome })` returns a finding whose message includes `Missing Hermes gsd-help skill`. Test 2 runs project-linked install, then asserts doctor returns no `error` severity findings. Test 3 adds a duplicate project-linked external_dirs entry and asserts a finding message includes `Duplicate project-linked Hermes external_dirs entry`. Test 4 adds a stale external dir and asserts a finding message includes `Stale Hermes external_dirs entry`. Test 5 runs installer subprocess `['--hermes', '--global', '--doctor', '--no-sdk']` and asserts stdout contains `Hermes doctor:`.</action>
  <verify>node --test tests/hermes-lifecycle.test.cjs</verify>
  <acceptance_criteria>
    - `tests/hermes-lifecycle.test.cjs` contains `Hermes doctor diagnostics`
    - `tests/hermes-lifecycle.test.cjs` contains `Duplicate project-linked Hermes external_dirs entry`
    - `tests/hermes-lifecycle.test.cjs` contains `Stale Hermes external_dirs entry`
    - `node --test tests/hermes-lifecycle.test.cjs` exits 0
  </acceptance_criteria>
  <done>Doctor behavior is regression-tested without a real Hermes binary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| doctor -> filesystem | Doctor inspects user files and must not mutate them. |
| diagnostics -> operator action | Findings must be precise enough to avoid unsafe manual cleanup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- |
| T-05-03 | I | doctor | mitigate | Return structured findings with exact paths and stable suggested fixes. |
| T-05-04 | T | doctor CLI | mitigate | Keep doctor read-only and test via fixture state before/after. |
</threat_model>

<verification>
- [ ] `node --test tests/hermes-lifecycle.test.cjs`
</verification>

<success_criteria>
- `--hermes --doctor` produces stable read-only diagnostics.
- Doctor catches missing help skill, duplicate config entries, stale config entries, and path leaks.
- Doctor can run without real Hermes installed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-lifecycle-tooling/05-02-SUMMARY.md`.
</output>

