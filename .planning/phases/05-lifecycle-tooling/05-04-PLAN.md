---
phase: 05-lifecycle-tooling
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - docs/hermes-install.md
  - docs/hermes-compatibility.md
  - tests/hermes-docs.test.cjs
autonomous: true
requirements:
  - DOCS-01
  - QUAL-01
  - DIST-02
  - DIST-03
user_setup: []
must_haves:
  truths:
    - "Docs describe Hermes update, uninstall, and doctor as implemented lifecycle surfaces."
    - "Docs still reject native local Hermes install claims."
  artifacts:
    - path: docs/hermes-install.md
      provides: "operator lifecycle guide"
      contains: "Hermes Lifecycle"
    - path: docs/hermes-compatibility.md
      provides: "Phase 5 compatibility status"
      contains: "Update / uninstall / doctor | supported"
---

<objective>
Close Phase 5 by updating operator docs, compatibility status, and docs tests
so users know how to maintain Hermes installs without overstating unsupported
native local behavior.
</objective>

<execution_context>
@/home/whiskey/.codex/get-shit-done/workflows/execute-plan.md
@/home/whiskey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-lifecycle-tooling/05-CONTEXT.md
@.planning/phases/05-lifecycle-tooling/05-RESEARCH.md
@.planning/phases/05-lifecycle-tooling/05-VALIDATION.md
@docs/hermes-install.md
@docs/hermes-compatibility.md
@tests/hermes-docs.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Hermes lifecycle commands</name>
  <files>docs/hermes-install.md, tests/hermes-docs.test.cjs</files>
  <read_first>docs/hermes-install.md, tests/hermes-docs.test.cjs, .planning/phases/05-lifecycle-tooling/05-CONTEXT.md</read_first>
  <action>In `docs/hermes-install.md`, add a `## Hermes Lifecycle` section after `Optional real Hermes smoke`. Include exact subsections `### Update`, `### Uninstall`, and `### Doctor`. Include exact commands: `npx get-shit-done-cc --hermes --global`, `npx get-shit-done-cc --hermes --local`, `npx get-shit-done-cc --hermes --global --uninstall`, `npx get-shit-done-cc --hermes --local --uninstall`, `npx get-shit-done-cc --hermes --global --doctor`, and `npx get-shit-done-cc --hermes --local --doctor`. State that doctor is read-only and project-linked uninstall removes only the matching `skills.external_dirs` entry. Add docs test assertions for `Hermes Lifecycle`, `--doctor`, and `project-linked uninstall removes only the matching`.</action>
  <verify>node --test tests/hermes-docs.test.cjs</verify>
  <acceptance_criteria>
    - `docs/hermes-install.md` contains `## Hermes Lifecycle`
    - `docs/hermes-install.md` contains `npx get-shit-done-cc --hermes --local --doctor`
    - `docs/hermes-install.md` contains `project-linked uninstall removes only the matching`
    - `node --test tests/hermes-docs.test.cjs` exits 0
  </acceptance_criteria>
  <done>Operators can find exact update, uninstall, and doctor commands.</done>
</task>

<task type="auto">
  <name>Task 2: Update compatibility status without native-local overclaim</name>
  <files>docs/hermes-compatibility.md, tests/hermes-docs.test.cjs</files>
  <read_first>docs/hermes-compatibility.md, tests/hermes-docs.test.cjs</read_first>
  <action>In `docs/hermes-compatibility.md`, change the matrix row for `Update / uninstall / doctor` from `planned` to `supported` and describe global/project-linked lifecycle coverage. In Known Gaps, remove the sentence saying full lifecycle behavior is not complete. Keep `Native local Hermes install mode | out of scope`. In Phase Mapping, change the lifecycle row to `Phase 5 complete: supported through update/uninstall/doctor lifecycle tests and operator docs.` Add docs test assertions for `Update / uninstall / doctor | supported`, `Phase 5 complete`, and `Native local Hermes install mode | out of scope`.</action>
  <verify>node --test tests/hermes-docs.test.cjs</verify>
  <acceptance_criteria>
    - `docs/hermes-compatibility.md` contains `Update / uninstall / doctor | supported`
    - `docs/hermes-compatibility.md` contains `Phase 5 complete`
    - `docs/hermes-compatibility.md` contains `Native local Hermes install mode | out of scope`
    - `node --test tests/hermes-docs.test.cjs` exits 0
  </acceptance_criteria>
  <done>Compatibility docs reflect Phase 5 support while preserving the native-local boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 5 verification</name>
  <files>docs/hermes-install.md, docs/hermes-compatibility.md, tests/hermes-docs.test.cjs</files>
  <read_first>tests/hermes-lifecycle.test.cjs, tests/hermes-install.test.cjs, tests/hermes-project-linked.test.cjs, tests/hermes-docs.test.cjs</read_first>
  <action>Run targeted Hermes lifecycle tests and then the full suite. Required targeted command: `node --test tests/hermes-install.test.cjs tests/hermes-project-linked.test.cjs tests/hermes-docs.test.cjs tests/hermes-lifecycle.test.cjs tests/hermes-core-workflow.test.cjs tests/multi-runtime-select.test.cjs`. Required full command: `npm test`. If full suite fails on unrelated tests, record exact failures in the summary and rerun targeted Hermes lifecycle tests before deciding whether Phase 5 is blocked.</action>
  <verify>node --test tests/hermes-install.test.cjs tests/hermes-project-linked.test.cjs tests/hermes-docs.test.cjs tests/hermes-lifecycle.test.cjs tests/hermes-core-workflow.test.cjs tests/multi-runtime-select.test.cjs</verify>
  <acceptance_criteria>
    - Targeted Hermes lifecycle command exits 0
    - `npm test` exits 0 or unrelated failures are documented with targeted lifecycle tests passing
  </acceptance_criteria>
  <done>Phase 5 has final verification evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| docs -> operator action | Lifecycle docs tell users what commands to run against real config. |
| compatibility claims -> future scope | Overstating native local support would create unsafe future assumptions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- |
| T-05-06 | I | docs | mitigate | Include exact commands and read-only doctor wording. |
| T-05-07 | R | compatibility matrix | mitigate | Keep native local install marked out of scope. |
</threat_model>

<verification>
- [ ] `node --test tests/hermes-install.test.cjs tests/hermes-project-linked.test.cjs tests/hermes-docs.test.cjs tests/hermes-lifecycle.test.cjs tests/hermes-core-workflow.test.cjs tests/multi-runtime-select.test.cjs`
- [ ] `npm test`
</verification>

<success_criteria>
- Hermes lifecycle docs list update, uninstall, and doctor commands.
- Compatibility matrix marks lifecycle support as implemented.
- Full test suite passes or unrelated failures are documented.
</success_criteria>

<output>
After completion, create `.planning/phases/05-lifecycle-tooling/05-04-SUMMARY.md`.
</output>
