---
phase: 05-lifecycle-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/install.js
  - tests/hermes-lifecycle.test.cjs
autonomous: true
requirements:
  - DIST-02
  - DIST-03
user_setup: []
must_haves:
  truths:
    - "Hermes update preserves locally modified GSD files through the existing manifest backup path."
    - "Hermes uninstall removes only GSD-owned artifacts and cleans project-linked external_dirs safely."
  artifacts:
    - path: bin/install.js
      provides: "Hermes lifecycle helper implementation"
      contains: "removeHermesExternalDir"
    - path: tests/hermes-lifecycle.test.cjs
      provides: "Hermes update/uninstall lifecycle tests"
      contains: "Hermes lifecycle update and uninstall"
---

<objective>
Implement the Hermes update/uninstall lifecycle foundation so both global and
project-linked installs can be safely refreshed and removed without damaging
unrelated Hermes configuration.
</objective>

<execution_context>
@/home/whiskey/.codex/get-shit-done/workflows/execute-plan.md
@/home/whiskey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-lifecycle-tooling/05-CONTEXT.md
@.planning/phases/05-lifecycle-tooling/05-RESEARCH.md
@.planning/phases/05-lifecycle-tooling/05-VALIDATION.md
@.planning/phases/05-lifecycle-tooling/05-PATTERNS.md
@bin/install.js
@tests/hermes-install.test.cjs
@tests/hermes-project-linked.test.cjs
@tests/bug-1908-uninstall-manifest.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Hermes external_dirs removal helper</name>
  <files>bin/install.js, tests/hermes-lifecycle.test.cjs</files>
  <read_first>bin/install.js, tests/hermes-project-linked.test.cjs, .planning/phases/05-lifecycle-tooling/05-CONTEXT.md</read_first>
  <action>Create `removeHermesExternalDir(configPath, externalDir)` near `ensureHermesExternalDir()`. It must normalize `externalDir` using `normalizeHermesExternalDir()`, remove only list items under an `external_dirs:` block that equal either the normalized path or `yamlQuote(normalizedPath)`, preserve unrelated lines, write a trailing newline, and return `{ removed: boolean, path: normalizedPath }`. Export it under `GSD_TEST_MODE`. Create `tests/hermes-lifecycle.test.cjs` with `process.env.GSD_TEST_MODE = '1'`, `node:test`, `node:assert/strict`, `node:fs`, `node:path`, `node:child_process`, and helpers from `./helpers.cjs`. Add tests proving the helper removes one matching path, preserves unrelated `model:` and `skills.enabled`, preserves a second external dir, and returns `removed: false` when the path is absent.</action>
  <verify>node --test tests/hermes-lifecycle.test.cjs</verify>
  <acceptance_criteria>
    - `bin/install.js` contains `function removeHermesExternalDir(configPath, externalDir)`
    - `bin/install.js` contains `removeHermesExternalDir,`
    - `tests/hermes-lifecycle.test.cjs` contains `preserves unrelated Hermes config`
    - `node --test tests/hermes-lifecycle.test.cjs` exits 0
  </acceptance_criteria>
  <done>Project-linked Hermes config entries can be removed without broad config rewrites.</done>
</task>

<task type="auto">
  <name>Task 2: Add Hermes-specific uninstall branch</name>
  <files>bin/install.js, tests/hermes-lifecycle.test.cjs</files>
  <read_first>bin/install.js, tests/bug-1908-uninstall-manifest.test.cjs, tests/hermes-project-linked.test.cjs</read_first>
  <action>In `uninstall(isGlobal, runtime)`, define `const isHermes = runtime === 'hermes';` and set `runtimeLabel = 'Hermes'` when true. Add an `else if (isHermes)` branch before the generic Claude branches. The branch must remove `skills/gsd-*` directories under `targetDir/skills`, increment `removedCount` when one or more are removed, and log `Removed {skillCount} Hermes skills`. For `!isGlobal`, call `removeHermesExternalDir(path.join(getGlobalDir('hermes', explicitConfigDir), 'config.yaml'), path.join(targetDir, 'skills'))` and log `Removed project-linked Hermes skills.external_dirs entry` only when `removed` is true. Leave existing common cleanup for `get-shit-done/`, `agents/`, and `gsd-file-manifest.json` in place.</action>
  <verify>node --test tests/hermes-lifecycle.test.cjs</verify>
  <acceptance_criteria>
    - `bin/install.js` contains `const isHermes = runtime === 'hermes';`
    - `bin/install.js` contains `Removed {skillCount} Hermes skills` or `Removed ${skillCount} Hermes skills`
    - `bin/install.js` contains `Removed project-linked Hermes skills.external_dirs entry`
    - `node --test tests/hermes-lifecycle.test.cjs` exits 0
  </acceptance_criteria>
  <done>Hermes uninstall no longer falls through Claude-specific global/local assumptions.</done>
</task>

<task type="auto">
  <name>Task 3: Prove update preserves Hermes local patches</name>
  <files>tests/hermes-lifecycle.test.cjs</files>
  <read_first>tests/hermes-install.test.cjs, tests/hermes-lifecycle.test.cjs, bin/install.js</read_first>
  <action>Add a subprocess `runInstaller(args, options)` helper matching existing Hermes tests. Add a test named `project-linked update backs up modified Hermes skill files`. In a temp HOME/project, run `['--hermes', '--local', '--no-sdk']`, modify `.gsd-hermes/skills/gsd-help/SKILL.md` by appending `\\n<!-- user edit -->\\n`, run the same install command again, and assert `.gsd-hermes/gsd-local-patches/skills/gsd-help/SKILL.md` exists and contains `user edit`. Add a global-mode equivalent for `~/.hermes/skills/gsd-help/SKILL.md` and `~/.hermes/gsd-local-patches/skills/gsd-help/SKILL.md`.</action>
  <verify>node --test tests/hermes-lifecycle.test.cjs</verify>
  <acceptance_criteria>
    - `tests/hermes-lifecycle.test.cjs` contains `project-linked update backs up modified Hermes skill files`
    - `tests/hermes-lifecycle.test.cjs` contains `global update backs up modified Hermes skill files`
    - `tests/hermes-lifecycle.test.cjs` contains `gsd-local-patches`
    - `node --test tests/hermes-lifecycle.test.cjs` exits 0
  </acceptance_criteria>
  <done>Hermes update safety is proven through existing manifest-backed patch preservation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| installer lifecycle -> user Hermes config | Update/uninstall can affect user-owned Hermes configuration. |
| manifest backup -> user modifications | Update relies on the manifest to preserve modified GSD files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- |
| T-05-01 | T | update | mitigate | Test that modified Hermes skill files are backed up before reinstall replaces them. |
| T-05-02 | D | uninstall | mitigate | Remove only `gsd-*` skills and exact project-linked external_dirs entries. |
</threat_model>

<verification>
- [ ] `node --test tests/hermes-lifecycle.test.cjs`
</verification>

<success_criteria>
- Hermes project-linked external_dirs cleanup is exact and idempotent.
- Hermes uninstall removes global and project-linked GSD skills.
- Re-running Hermes install backs up modified GSD skill files.
</success_criteria>

<output>
After completion, create `.planning/phases/05-lifecycle-tooling/05-01-SUMMARY.md`.
</output>

