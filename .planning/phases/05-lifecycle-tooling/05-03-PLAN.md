---
phase: 05-lifecycle-tooling
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - tests/hermes-lifecycle.test.cjs
  - tests/hermes-docs.test.cjs
autonomous: true
requirements:
  - QUAL-01
  - DIST-02
  - DIST-03
user_setup: []
must_haves:
  truths:
    - "Hermes lifecycle behavior is covered by deterministic regression tests."
    - "Global and project-linked modes are both covered."
  artifacts:
    - path: tests/hermes-lifecycle.test.cjs
      provides: "end-to-end lifecycle regression suite"
      contains: "Hermes lifecycle end-to-end"
---

<objective>
Lock Hermes lifecycle behavior with targeted regression tests that cover
install, update, doctor, uninstall, and config preservation across both
supported install modes.
</objective>

<execution_context>
@/home/whiskey/.codex/get-shit-done/workflows/execute-plan.md
@/home/whiskey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-lifecycle-tooling/05-CONTEXT.md
@.planning/phases/05-lifecycle-tooling/05-VALIDATION.md
@tests/hermes-lifecycle.test.cjs
@tests/hermes-install.test.cjs
@tests/hermes-project-linked.test.cjs
@tests/hermes-docs.test.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add global lifecycle end-to-end regression</name>
  <files>tests/hermes-lifecycle.test.cjs</files>
  <read_first>tests/hermes-lifecycle.test.cjs, tests/hermes-install.test.cjs</read_first>
  <action>Add `describe('Hermes lifecycle end-to-end', ...)`. Add a test named `global lifecycle installs updates doctors and uninstalls`. It must run `--hermes --global --no-sdk`, assert `~/.hermes/skills/gsd-help/SKILL.md` exists, run `--hermes --global --doctor --no-sdk`, assert stdout contains `Hermes doctor: global`, append a local edit and rerun install to assert `~/.hermes/gsd-local-patches/skills/gsd-help/SKILL.md` exists, run `--hermes --global --uninstall --no-sdk`, then assert `~/.hermes/skills/gsd-help` and `~/.hermes/gsd-file-manifest.json` do not exist.</action>
  <verify>node --test tests/hermes-lifecycle.test.cjs</verify>
  <acceptance_criteria>
    - `tests/hermes-lifecycle.test.cjs` contains `global lifecycle installs updates doctors and uninstalls`
    - `tests/hermes-lifecycle.test.cjs` contains `--uninstall`
    - `tests/hermes-lifecycle.test.cjs` contains `Hermes doctor: global`
    - `node --test tests/hermes-lifecycle.test.cjs` exits 0
  </acceptance_criteria>
  <done>Global Hermes lifecycle is covered end-to-end in a temp HOME.</done>
</task>

<task type="auto">
  <name>Task 2: Add project-linked lifecycle end-to-end regression</name>
  <files>tests/hermes-lifecycle.test.cjs</files>
  <read_first>tests/hermes-lifecycle.test.cjs, tests/hermes-project-linked.test.cjs</read_first>
  <action>Add a test named `project-linked lifecycle installs updates doctors and uninstalls`. It must run `--hermes --local --no-sdk`, assert `.gsd-hermes/skills/gsd-help/SKILL.md` exists and `~/.hermes/config.yaml` contains the project skills path once, run `--hermes --local --doctor --no-sdk`, assert stdout contains `Hermes doctor: project-linked`, append a local edit and rerun install to assert `.gsd-hermes/gsd-local-patches/skills/gsd-help/SKILL.md` exists, run `--hermes --local --uninstall --no-sdk`, assert `.gsd-hermes/skills/gsd-help` and `.gsd-hermes/gsd-file-manifest.json` do not exist, and assert config no longer contains the project skills path while preserving a manually added unrelated external dir.</action>
  <verify>node --test tests/hermes-lifecycle.test.cjs</verify>
  <acceptance_criteria>
    - `tests/hermes-lifecycle.test.cjs` contains `project-linked lifecycle installs updates doctors and uninstalls`
    - `tests/hermes-lifecycle.test.cjs` contains `Hermes doctor: project-linked`
    - `tests/hermes-lifecycle.test.cjs` contains `unrelated external dir`
    - `node --test tests/hermes-lifecycle.test.cjs` exits 0
  </acceptance_criteria>
  <done>Project-linked Hermes lifecycle and config cleanup are covered end-to-end.</done>
</task>

<task type="auto">
  <name>Task 3: Run targeted lifecycle suite</name>
  <files>tests/hermes-lifecycle.test.cjs, tests/hermes-docs.test.cjs</files>
  <read_first>tests/hermes-lifecycle.test.cjs, tests/hermes-install.test.cjs, tests/hermes-project-linked.test.cjs, tests/hermes-docs.test.cjs</read_first>
  <action>Run `node --test tests/hermes-install.test.cjs tests/hermes-project-linked.test.cjs tests/hermes-docs.test.cjs tests/hermes-lifecycle.test.cjs`. If docs tests fail because lifecycle docs still say Phase 5 is planned, record that Plan 04 owns that failure and keep the lifecycle tests green. Do not edit docs in this plan except adding a temporary assertion only if needed for lifecycle helper coverage.</action>
  <verify>node --test tests/hermes-install.test.cjs tests/hermes-project-linked.test.cjs tests/hermes-docs.test.cjs tests/hermes-lifecycle.test.cjs</verify>
  <acceptance_criteria>
    - `node --test tests/hermes-lifecycle.test.cjs` exits 0
    - Targeted lifecycle command exits 0, or any docs-only failure is documented in `05-03-SUMMARY.md` for Plan 04 follow-up
  </acceptance_criteria>
  <done>The lifecycle regression suite proves the implementation before docs are closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| test fixture -> user environment | Lifecycle tests must not touch real `~/.hermes`. |
| uninstall -> config preservation | Project-linked uninstall must remove only its own path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- |
| T-05-04 | I | test isolation | mitigate | Every subprocess run uses temp `HOME` and temp project cwd. |
| T-05-05 | T | config cleanup | mitigate | End-to-end test verifies unrelated external dir survives uninstall. |
</threat_model>

<verification>
- [ ] `node --test tests/hermes-install.test.cjs tests/hermes-project-linked.test.cjs tests/hermes-docs.test.cjs tests/hermes-lifecycle.test.cjs`
</verification>

<success_criteria>
- Global lifecycle test covers install, update, doctor, uninstall.
- Project-linked lifecycle test covers install, update, doctor, uninstall, and external_dirs cleanup.
- Targeted Hermes lifecycle suite passes or docs-only gap is explicitly handed to Plan 04.
</success_criteria>

<output>
After completion, create `.planning/phases/05-lifecycle-tooling/05-03-SUMMARY.md`.
</output>

