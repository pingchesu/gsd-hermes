---
phase: 02-hermes-runtime-install
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/install.js
  - tests/multi-runtime-select.test.cjs
  - package.json
autonomous: true
requirements:
  - DIST-01
user_setup: []
must_haves:
  truths:
    - "Developers can choose Hermes during install with `--hermes`, `--all`, or the interactive runtime picker."
    - "Hermes local installs are rejected clearly in Phase 2, and installer help/prompt output describe Hermes as global-only without claiming unsupported project-linked behavior."
  artifacts:
    - path: bin/install.js
      provides: "Hermes runtime flag parsing, selection registration, local-install rejection, and installer-facing help/prompt output"
      contains: "hermes"
  key_links:
    - from: bin/install.js
      to: selectedRuntimes
      via: "CLI flag parsing and `hasAll`/`selectedRuntimes = [...]` expansion"
      pattern: "hasHermes|hasAll|selectedRuntimes\\s*=\\s*\\[[\\s\\S]*['\"]hermes['\"]"
    - from: bin/install.js
      to: install mode guards and prompt copy
      via: "direct `--local` rejection plus truthful runtime/help text"
      pattern: "hasHermes\\s*&&\\s*hasLocal|runtimeMap|Hermes|\\.hermes|global only|external_dirs"
---

<objective>
Make Hermes selectable at the installer entrypoint level without expanding into path ownership or command-discovery work.

Purpose: Satisfy the first part of DIST-01 by making Hermes visible and selectable through the same installer seams upstream already uses for supported runtimes.
Output: Hermes runtime registration, flag handling, and truthful help/prompt copy in `bin/install.js`
</objective>

<execution_context>
@/home/whiskey/.codex/get-shit-done/workflows/execute-plan.md
@/home/whiskey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/fork-ownership.md
@docs/upstream-sync.md
@docs/hermes-compatibility.md
@.planning/phases/02-hermes-runtime-install/02-RESEARCH.md
@.planning/phases/02-hermes-runtime-install/02-PATTERNS.md
@.planning/phases/01-fork-foundation/01-01-PLAN.md
@.planning/phases/01-fork-foundation/01-02-PLAN.md
@.planning/phases/01-fork-foundation/01-03-PLAN.md

<interfaces>
Precondition from Phase 1 governance:
- This repo is still planning-only until the documented first import runs. Phase 2 work cannot edit installer/test seams until `bin/install.js` and `tests/*` exist in the worktree.

Target seams expected after upstream import, per `02-RESEARCH.md`:
- CLI flag parsing near the installer argument block should gain `hasHermes` and Hermes inclusion in `selectedRuntimes`.
- Interactive selection should continue using `promptRuntime()` and `runtimeMap`, extended additively instead of refactoring the menu flow.
- Installer help/examples should stay in `bin/install.js` and describe Hermes mode truthfully: global support now, direct local install rejected in Phase 2, project-linked `external_dirs` bridge later.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Satisfy the upstream-source preflight before Hermes file edits</name>
  <files>bin/install.js, tests/multi-runtime-select.test.cjs, package.json</files>
  <action>Before making any Hermes-specific edits, confirm the upstream source import has happened. If `bin/install.js` or `tests/multi-runtime-select.test.cjs` is missing, run the documented first-import flow from `docs/upstream-sync.md`: ensure the `upstream` remote exists, `git fetch upstream`, `git checkout main`, then `git merge --allow-unrelated-histories upstream/main`. Resolve only merge conflicts required to land the upstream base; do not start Hermes edits until the imported installer/test seams are present. If the merge exposes drift outside the approved installer/test/compatibility seams from Phase 1, stop and record the conflict instead of widening Phase 2 scope.</action>
  <verify>
    <automated>git remote get-url upstream >/dev/null && test -f bin/install.js && test -f tests/multi-runtime-select.test.cjs && node -e 'const fs=require("fs");const pkg=JSON.parse(fs.readFileSync("package.json","utf8"));if(!pkg.engines||!pkg.engines.node){process.exit(1)}'</automated>
  </verify>
  <done>The repo contains the upstream installer and shared runtime test files needed for Phase 2, so Hermes edits no longer target a planning-only tree.</done>
</task>

<task type="auto">
  <name>Task 2: Register Hermes in installer runtime selection and reject direct local mode</name>
  <files>bin/install.js</files>
  <action>Update the imported `bin/install.js` runtime-registration block so Hermes is selectable through the same additive seams used by existing runtimes. Add a `hasHermes` flag for `--hermes`, include Hermes in the real `hasAll`/`selectedRuntimes = [...]` expansion used upstream, and append Hermes into `selectedRuntimes` with the same dedupe behavior as the existing runtime pushes. Extend the interactive `runtimeMap`/selection source so Hermes can be chosen from the installer prompt. In the same selection seam, add a direct guard for `--hermes --local` that exits with a Phase-2-truthful error instead of falling through to unsupported local semantics. Keep this bounded to selection and direct flag validation; do not add Hermes command discovery, `external_dirs` mutation, or lifecycle commands in this task.</action>
  <verify>
    <automated>node -e 'const fs=require("fs");const s=fs.readFileSync("bin/install.js","utf8");const checks=[/hasHermes\\s*=\\s*args\\.includes\\([\"\\x27]--hermes[\"\\x27]\\)/,/const hasAll\\s*=\\s*args\\.includes\\([\"\\x27]--all[\"\\x27]\\)/,/if\\s*\\(hasAll\\)\\s*\\{[\\s\\S]{0,400}selectedRuntimes\\s*=\\s*\\[[\\s\\S]*?[\"\\x27]hermes[\"\\x27]/,/selectedRuntimes\\.push\\([\"\\x27]hermes[\"\\x27]\\)/,/runtimeMap[\\s\\S]{0,1200}[\"\\x27]hermes[\"\\x27]/,/if\\s*\\(\\s*hasHermes\\s*&&\\s*hasLocal\\s*\\)[\\s\\S]{0,400}(process\\.exit|throw new Error|return)/];if(checks.some(re=>!re.test(s))){process.exit(1)}'</automated>
  </verify>
  <done>`bin/install.js` exposes Hermes as a selectable runtime through direct flags, the real upstream `hasAll` expansion, and the interactive runtime picker, while direct `--hermes --local` requests fail clearly instead of claiming unsupported local behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Publish truthful Hermes help and prompt copy</name>
  <files>bin/install.js</files>
  <action>Update installer help text, usage examples, and prompt labels so Hermes appears wherever runtime choices are documented. The copy must say Hermes is installable now in global mode, must say direct local install is not supported in Phase 2, and must not imply a native local install; if project-linked behavior is referenced at all, describe it only as a later `external_dirs` bridge. In the interactive runtime prompt, show the truthful Hermes home root (`~/.hermes`) so install choices reflect the real path semantics users will get in later plans. Keep the language installer-facing; do not add workflow-parity promises or `/gsd-*` discovery claims.</action>
  <verify>
    <automated>node -e 'const fs=require("fs");const s=fs.readFileSync("bin/install.js","utf8");const checks=[/--hermes/,/Hermes/,/\\.hermes/,/global mode|global-only|global only/,/external_dirs/,/local install is not supported|project-linked.*later|global only in Phase 2/];if(checks.some(re=>!re.test(s))){process.exit(1)}'</automated>
  </verify>
  <done>`bin/install.js` help and prompt output mention Hermes consistently, show the Hermes home path, and preserve the project boundary that Phase 2 is global-only while project-linked mode remains a later `external_dirs` bridge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer CLI input -> runtime selection | User-provided flags and prompt selections decide which runtime branch will execute |
| installer copy -> user expectations | Help/prompt text becomes the public contract for what Hermes install modes exist in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | S | `bin/install.js` runtime registration | mitigate | Add Hermes through the same `hasRuntime`/`selectedRuntimes` seams as existing runtimes so the installer cannot advertise Hermes without actually routing selection into the runtime set |
| T-02-02 | T | installer help/prompt text | mitigate | Require help and menu copy to include Hermes plus `.hermes` and forbid native-local wording so the public contract cannot overclaim unsupported project-linked behavior |
| T-02-03 | R | interactive runtime picker | mitigate | Keep Hermes in `runtimeMap` and prompt output alongside existing runtimes so selection behavior remains source-auditable and covered later by shared runtime tests |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `git remote get-url upstream >/dev/null && test -f bin/install.js && test -f tests/multi-runtime-select.test.cjs && node -e 'const fs=require("fs");const pkg=JSON.parse(fs.readFileSync("package.json","utf8"));if(!pkg.engines||!pkg.engines.node){process.exit(1)}'`
- [ ] `node -e 'const fs=require("fs");const s=fs.readFileSync("bin/install.js","utf8");const checks=[/hasHermes\\s*=\\s*args\\.includes\\([\"\\x27]--hermes[\"\\x27]\\)/,/const hasAll\\s*=\\s*args\\.includes\\([\"\\x27]--all[\"\\x27]\\)/,/if\\s*\\(hasAll\\)\\s*\\{[\\s\\S]{0,400}selectedRuntimes\\s*=\\s*\\[[\\s\\S]*?[\"\\x27]hermes[\"\\x27]/,/selectedRuntimes\\.push\\([\"\\x27]hermes[\"\\x27]\\)/,/runtimeMap[\\s\\S]{0,1200}[\"\\x27]hermes[\"\\x27]/,/if\\s*\\(\\s*hasHermes\\s*&&\\s*hasLocal\\s*\\)[\\s\\S]{0,400}(process\\.exit|throw new Error|return)/];if(checks.some(re=>!re.test(s))){process.exit(1)}'`
- [ ] `node -e 'const fs=require("fs");const s=fs.readFileSync("bin/install.js","utf8");const checks=[/--hermes/,/Hermes/,/\\.hermes/,/global mode|global-only|global only/,/external_dirs/,/local install is not supported|project-linked.*later|global only in Phase 2/];if(checks.some(re=>!re.test(s))){process.exit(1)}'`
</verification>

<success_criteria>

- Hermes is selectable via npm/npx installer entrypoints through `--hermes`, `--all`, and the runtime picker
- Direct Hermes local installs fail clearly instead of falling into unsupported Phase 2 behavior
- Installer-facing copy names Hermes without claiming local-install parity that belongs to later phases
- The implementation stays confined to upstream import plus `bin/install.js` runtime-selection/help seams and does not expand into discovery, lifecycle, or workflow logic
</success_criteria>

<output>
After completion, create `.planning/phases/02-hermes-runtime-install/02-01-SUMMARY.md`
</output>
