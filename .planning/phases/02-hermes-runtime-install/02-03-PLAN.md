---
phase: 02-hermes-runtime-install
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - tests/multi-runtime-select.test.cjs
  - tests/hermes-install.test.cjs
autonomous: true
requirements:
  - DIST-01
user_setup: []
must_haves:
  truths:
    - "Hermes runtime selection is regression-tested through the shared runtime picker tests, including the real upstream `hasAll` expansion shape."
    - "Hermes install-path semantics, global-only rejection paths, and the accepted `~/.gsd/defaults.json` side effect are covered by a dedicated runtime test."
  artifacts:
    - path: tests/multi-runtime-select.test.cjs
      provides: "shared selection coverage for Hermes"
      contains: "hermes"
    - path: tests/hermes-install.test.cjs
      provides: "Hermes-specific helper, global-only rejection, and install-branch coverage"
      contains: "Hermes"
  key_links:
    - from: tests/multi-runtime-select.test.cjs
      to: bin/install.js
      via: "mirrored runtimeMap/allRuntimes and source assertions against `hasAll`/`selectedRuntimes = [...]`"
      pattern: "runtimeMap|allRuntimes|--hermes|hasAll|selectedRuntimes"
    - from: tests/hermes-install.test.cjs
      to: bin/install.js
      via: "helper extraction plus runtime branch, local-rejection, and finish side-effect assertions"
      pattern: "getGlobalDir|getConfigDirFromHome|isHermes|global mode|resolve_model_ids|!isGlobal"
---

<objective>
Lock the Phase 2 installer work behind focused runtime tests without expanding into broader workflow or lifecycle suites.

Purpose: Close DIST-01 with regression coverage that proves Hermes selection, path semantics, and install output survive future upstream syncs.
Output: Updated `tests/multi-runtime-select.test.cjs` and new `tests/hermes-install.test.cjs`
</objective>

<execution_context>
@/home/whiskey/.codex/get-shit-done/workflows/execute-plan.md
@/home/whiskey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/fork-ownership.md
@docs/hermes-compatibility.md
@.planning/phases/02-hermes-runtime-install/02-RESEARCH.md
@.planning/phases/02-hermes-runtime-install/02-PATTERNS.md
@.planning/phases/02-hermes-runtime-install/02-01-PLAN.md
@.planning/phases/02-hermes-runtime-install/02-02-PLAN.md

<interfaces>
Testing seams from `02-PATTERNS.md`:
- `tests/multi-runtime-select.test.cjs` should mirror `runtimeMap` and `allRuntimes`, then assert prompt/menu source still contains the Hermes option and that the installer still uses the real upstream `hasAll`/`selectedRuntimes = [...]` structure.
- `tests/hermes-install.test.cjs` should cover helper behavior (`getDirName`, `getConfigDirFromHome`, `getGlobalDir`) plus source assertions for `--hermes`, direct and interactive local rejection, the bounded install root under `~/.hermes/skills/`, finish output that reports `global mode`, and the accepted `resolve_model_ids: "omit"` write in `~/.gsd/defaults.json`.
- Leave `tests/runtime-converters.test.cjs` untouched unless execution adds a real Hermes converter helper, which this phase should avoid.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend shared runtime-selection regression coverage for Hermes</name>
  <files>tests/multi-runtime-select.test.cjs</files>
  <action>Update the shared runtime-selection test so its mirrored `runtimeMap` and `allRuntimes` include Hermes, then add assertions that cover single-select, multi-select/`--all`, dedupe behavior, and prompt/menu ordering for Hermes. Add source assertions that the installer prompt still exposes the Hermes option, that `--hermes` flag text exists in `bin/install.js`, and that the source still uses the real upstream `const hasAll = ...` plus `if (hasAll) { selectedRuntimes = [...] }` structure rather than an invented `args.all` shape. Keep this test focused on selection behavior only; do not turn it into path or install-branch coverage.</action>
  <verify>
    <automated>node --test tests/multi-runtime-select.test.cjs</automated>
  </verify>
  <done>`tests/multi-runtime-select.test.cjs` fails if Hermes drops out of the runtime picker, the real upstream `hasAll` selection flow, or the installer menu/help source.</done>
</task>

<task type="auto">
  <name>Task 2: Create dedicated Hermes install-path regression coverage</name>
  <files>tests/hermes-install.test.cjs</files>
  <action>Create `tests/hermes-install.test.cjs` following the existing per-runtime install-test style. Cover `getDirName("hermes")`, `getConfigDirFromHome("hermes", true/false)` as applicable, `getGlobalDir("hermes")`, and source assertions that `bin/install.js` contains `--hermes`, `isHermes`, the direct `--hermes --local` rejection, the interactive `runtime === "hermes" && !isGlobal` rejection path, the bounded Hermes install target rooted beneath `~/.hermes/skills/` without asserting a deeper child directory contract, finish output that states `global mode`, and the accepted non-Claude `resolve_model_ids: "omit"` write to `~/.gsd/defaults.json`. If the Phase 2 implementation keeps Hermes in the no-`settings.json` path, assert that explicitly. Do not add `external_dirs` mutation tests, workflow-parity tests, or lifecycle-tooling assertions.</action>
  <verify>
    <automated>node --test tests/hermes-install.test.cjs</automated>
  </verify>
  <done>`tests/hermes-install.test.cjs` gives Hermes its own helper/install regression suite and protects the Phase 2 contract for global-only install semantics, explicit rejection paths, and the accepted inherited defaults side effect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source changes -> regression suite | Future upstream merges or Hermes edits can silently remove runtime support unless tests pin the installer seams |
| helper semantics -> documented compatibility boundary | Tests are the enforcement layer that prevents project-linked or lifecycle behavior from leaking into Phase 2 by accident |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | R | `tests/multi-runtime-select.test.cjs` | mitigate | Mirror the runtime picker structure and assert Hermes menu/help/source presence so installer-selection regressions are caught immediately |
| T-02-08 | T | `tests/hermes-install.test.cjs` | mitigate | Add helper-level and source-level assertions for `.hermes`, the bounded `skills/` root, `isHermes`, local rejection, `global mode`, and `resolve_model_ids` so path/output drift becomes a test failure |
| T-02-09 | I | Phase 2 test boundary | mitigate | Keep tests limited to selection, install-path semantics, and accepted finish side effects so passing coverage cannot be misread as proof of `external_dirs`, discovery, or lifecycle support |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/multi-runtime-select.test.cjs`
- [ ] `node --test tests/hermes-install.test.cjs`
</verification>

<success_criteria>

- Shared runtime-selection tests fail if Hermes disappears from installer choices or if the real upstream `hasAll` structure is rewritten incorrectly
- Dedicated Hermes tests prove `.hermes` path semantics, bounded install-root ownership, explicit local-mode rejection, accepted `resolve_model_ids` side effect, and explicit `global mode` output
- Phase 2 ends with runtime-focused regression coverage only, leaving discovery, bridge mode, and lifecycle tooling for later phases
</success_criteria>

<output>
After completion, create `.planning/phases/02-hermes-runtime-install/02-03-SUMMARY.md`
</output>
