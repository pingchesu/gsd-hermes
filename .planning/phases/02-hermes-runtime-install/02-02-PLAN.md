---
phase: 02-hermes-runtime-install
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - bin/install.js
autonomous: true
requirements:
  - DIST-01
user_setup: []
must_haves:
  truths:
    - "Installer logic resolves Hermes paths consistently to the Hermes-owned global root and rejects interactive local installs in Phase 2."
    - "Hermes install behavior writes only the Phase 2 supported global install shape under `~/.hermes/skills/`, reports the configured mode clearly, and keeps the inherited `~/.gsd/defaults.json` side effect explicit."
  artifacts:
    - path: bin/install.js
      provides: "Hermes path helpers, global-only enforcement, runtime branch, and mode-specific post-install reporting"
      contains: ".hermes"
  key_links:
    - from: bin/install.js
      to: Hermes path helpers
      via: "getDirName/getConfigDirFromHome/getGlobalDir"
      pattern: "getDirName|getConfigDirFromHome|getGlobalDir"
    - from: bin/install.js
      to: install/finishInstall
      via: "Hermes runtime branch, local-mode rejection, `~/.hermes/skills/` root, inherited defaults side effect, and final mode summary"
      pattern: "isHermes|runtime\\s*===\\s*['\"]hermes['\"]\\s*&&\\s*!isGlobal|global mode|resolve_model_ids|\\.hermes"
---

<objective>
Define Hermes install ownership semantics inside the installer without pulling in command discovery or lifecycle tooling.

Purpose: Satisfy the path-resolution and install-mode parts of DIST-01 by making Hermes resolve to its real global root and by keeping project-linked behavior explicitly deferred.
Output: Hermes helper cases, install branch, and finish output in `bin/install.js`
</objective>

<execution_context>
@/home/whiskey/.codex/get-shit-done/workflows/execute-plan.md
@/home/whiskey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/fork-ownership.md
@docs/upstream-sync.md
@docs/hermes-compatibility.md
@.planning/phases/02-hermes-runtime-install/02-RESEARCH.md
@.planning/phases/02-hermes-runtime-install/02-PATTERNS.md
@.planning/phases/02-hermes-runtime-install/02-01-PLAN.md

<interfaces>
Hermes path/install contracts from `02-RESEARCH.md`:
- `getConfigDirFromHome("hermes", true)` should resolve to `.hermes`
- `getGlobalDir("hermes")` should default to `path.join(os.homedir(), ".hermes")`
- Phase 2 global install should stay scoped to the proven `~/.hermes/skills/` root; do not commit to a child directory contract that Phase 3 may need to revisit
- Project-linked mode remains deferred; any `getDirName("hermes")` case added now must be treated as an internal placeholder, and interactive local requests must be rejected rather than treated as supported native local install
- `finishInstall()` currently writes `~/.gsd/defaults.json` for all non-Claude runtimes; Phase 2 accepts that inherited GSD-owned side effect for Hermes and must verify it explicitly rather than hand-wave it away
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Hermes path-helper cases with global-first ownership semantics</name>
  <files>bin/install.js</files>
  <action>Extend the imported upstream helper seams `getDirName(runtime)`, `getConfigDirFromHome(runtime, isGlobal)`, and `getGlobalDir(runtime, explicitDir)` with Hermes cases. Resolve the Hermes home root to `path.join(os.homedir(), ".hermes")` and make the home-config helper return `.hermes` for global install path substitution. If `getDirName("hermes")` needs a value for helper consistency, keep it as an internal placeholder only and do not let that placeholder become a supported local/project-linked install claim anywhere in installer output. Follow the existing helper style used by additive runtimes; do not refactor the helper structure while adding Hermes.</action>
  <verify>
    <automated>node -e 'const fs=require("fs");const s=fs.readFileSync("bin/install.js","utf8");const checks=[/getDirName[\\s\\S]{0,1600}hermes/,/getConfigDirFromHome[\\s\\S]{0,2000}[\"\\x27]\\.hermes[\"\\x27]/,/getGlobalDir[\\s\\S]{0,2400}path\\.join\\(os\\.homedir\\(\\),\\s*[\"\\x27]\\.hermes[\"\\x27]\\)/];if(checks.some(re=>!re.test(s))){process.exit(1)}'</automated>
  </verify>
  <done>`bin/install.js` contains Hermes helper logic that resolves `.hermes` consistently and keeps Phase 2 ownership global-first rather than inventing native local install semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the Hermes global-only install branch and explicit finish behavior</name>
  <files>bin/install.js</files>
  <action>Add `const isHermes = runtime === "hermes"` beside the other runtime booleans, then implement one bounded Hermes branch in `install()` that writes the managed Hermes payload under the proven Hermes global skills root `path.join(getGlobalDir("hermes", explicitDir), "skills")`. Do not make a Phase-2 contract about a deeper child directory name; if the copy helper naturally creates `gsd-*` entries, treat that as an implementation detail to be pinned later by Phase 3 command-discovery work, not user-facing truth. Enforce the Phase 2 global-only boundary in both remaining installer flows: if `runtime === "hermes"` and the flow reaches a local/non-global install path (for example via interactive location selection), reject it with an explicit message that directs the user to global install now and `external_dirs` bridge work later. Keep Hermes aligned with the runtimes that do not write runtime-owned `settings.json`/`config.yaml` files unless the imported upstream code proves a Hermes-owned config fragment is required. Phase 2 explicitly accepts the inherited `finishInstall()` non-Claude write to `~/.gsd/defaults.json` with `resolve_model_ids: "omit"` for Hermes; verify that behavior stays intact and do not suppress it here. Update the post-install/finish path so the final output explicitly states that Hermes was configured in `global mode` and prints the resolved Hermes root. Do not add `external_dirs` mutation, update/uninstall/doctor behavior, or `/gsd-*` discovery in this plan.</action>
  <verify>
    <automated>node -e 'const fs=require("fs");const s=fs.readFileSync("bin/install.js","utf8");const checks=[/isHermes\\s*=\\s*runtime\\s*===\\s*[\"\\x27]hermes[\"\\x27]/,/runtime\\s*===\\s*[\"\\x27]hermes[\"\\x27]\\s*&&\\s*!isGlobal[\\s\\S]{0,500}(global only|global-only|external_dirs|not supported)/,/isHermes[\\s\\S]{0,1600}path\\.join\\([^\\n]*[\"\\x27]skills[\"\\x27]/,/!isCodex[\\s\\S]{0,220}!isHermes[\\s\\S]{0,220}writeSettings/,/if\\s*\\(\\s*runtime\\s*!==\\s*[\"\\x27]claude[\"\\x27]\\s*\\)[\\s\\S]{0,1200}resolve_model_ids/,/global mode/];if(checks.some(re=>!re.test(s))){process.exit(1)}'</automated>
  </verify>
  <done>`bin/install.js` contains a Hermes-specific install branch that stays scoped to the Hermes-owned global skills root, rejects interactive/local mode attempts, keeps the inherited `~/.gsd/defaults.json` side effect explicit, and reports the configured mode clearly at the end of install.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer helper resolution -> filesystem writes | Helper output controls where the installer will create or copy Hermes-managed files |
| install branch -> user-owned runtime config | Runtime-specific completion logic can accidentally mutate shared config or imply unsupported modes if not bounded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | Hermes path helpers in `bin/install.js` | mitigate | Centralize Hermes resolution in `getDirName`, `getConfigDirFromHome`, and `getGlobalDir` so install targets are derived once and stay auditable against `.hermes` semantics |
| T-02-05 | E | Hermes install branch | mitigate | Constrain the Hermes branch to the Hermes-owned global skills root under `~/.hermes/skills/`, reject local/non-global Hermes paths explicitly, and avoid runtime-owned config mutation or lifecycle side effects in Phase 2 |
| T-02-06 | I | finish output / install summary | mitigate | Require explicit `global mode` messaging and an explicit `resolve_model_ids` side-effect decision so the installer tells the truth about which Hermes mode was configured and what inherited GSD config changed |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `node -e 'const fs=require("fs");const s=fs.readFileSync("bin/install.js","utf8");const checks=[/getDirName[\\s\\S]{0,1600}hermes/,/getConfigDirFromHome[\\s\\S]{0,2000}[\"\\x27]\\.hermes[\"\\x27]/,/getGlobalDir[\\s\\S]{0,2400}path\\.join\\(os\\.homedir\\(\\),\\s*[\"\\x27]\\.hermes[\"\\x27]\\)/];if(checks.some(re=>!re.test(s))){process.exit(1)}'`
- [ ] `node -e 'const fs=require("fs");const s=fs.readFileSync("bin/install.js","utf8");const checks=[/isHermes\\s*=\\s*runtime\\s*===\\s*[\"\\x27]hermes[\"\\x27]/,/runtime\\s*===\\s*[\"\\x27]hermes[\"\\x27]\\s*&&\\s*!isGlobal[\\s\\S]{0,500}(global only|global-only|external_dirs|not supported)/,/isHermes[\\s\\S]{0,1600}path\\.join\\([^\\n]*[\"\\x27]skills[\"\\x27]/,/!isCodex[\\s\\S]{0,220}!isHermes[\\s\\S]{0,220}writeSettings/,/if\\s*\\(\\s*runtime\\s*!==\\s*[\"\\x27]claude[\"\\x27]\\s*\\)[\\s\\S]{0,1200}resolve_model_ids/,/global mode/];if(checks.some(re=>!re.test(s))){process.exit(1)}'`
</verification>

<success_criteria>

- Hermes path resolution is consistent and centered on `.hermes` as the runtime-owned global root
- The installer writes Hermes assets to the bounded global skills root, rejects local/project-linked attempts in Phase 2, and does not claim or implement bridge mode yet
- The final install output names the configured Hermes mode explicitly and the plan treats the inherited `~/.gsd/defaults.json` side effect as accepted, verified behavior
</success_criteria>

<output>
After completion, create `.planning/phases/02-hermes-runtime-install/02-02-SUMMARY.md`
</output>
