---
phase: 01-fork-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - docs/hermes-compatibility.md
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
user_setup: []
must_haves:
  truths:
    - "Maintainers can see which Hermes-facing surfaces are not implemented yet versus only documented for later phases."
    - "Maintainers have explicit guardrails for where Hermes patches belong and when a change has crossed into forbidden workflow rewrite territory."
  artifacts:
    - path: docs/hermes-compatibility.md
      provides: "compatibility matrix, known gaps inventory, and adapter guardrails"
      contains: "# Hermes Compatibility and Guardrails"
  key_links:
    - from: docs/hermes-compatibility.md
      to: docs/fork-ownership.md
      via: "ownership boundary references"
      pattern: "Upstream base|Hermes adapter seam|Downstream governance"
    - from: docs/hermes-compatibility.md
      to: docs/upstream-sync.md
      via: "sync and patch guardrails"
      pattern: "merge upstream/main|external_dirs|not implemented|known gap"
---

<objective>
Publish the initial Hermes compatibility matrix and the adapter guardrails that future runtime work must follow.

Purpose: Close Phase 1’s governance scope by making unsupported surfaces explicit and tying future Hermes work back to the ownership and sync rules.
Output: `docs/hermes-compatibility.md`
</objective>

<execution_context>
@/home/whiskey/.codex/get-shit-done/workflows/execute-plan.md
@/home/whiskey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-fork-foundation/01-CONTEXT.md
@.planning/phases/01-fork-foundation/01-RESEARCH.md
@.planning/phases/01-fork-foundation/01-VALIDATION.md
@.planning/phases/01-fork-foundation/01-PATTERNS.md
@AGENTS.md
@docs/fork-ownership.md
@docs/upstream-sync.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the initial Hermes compatibility matrix and known-gaps inventory</name>
  <files>docs/hermes-compatibility.md</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/phases/01-fork-foundation/01-CONTEXT.md, .planning/phases/01-fork-foundation/01-RESEARCH.md, .planning/phases/01-fork-foundation/01-VALIDATION.md, docs/fork-ownership.md, docs/upstream-sync.md, AGENTS.md</read_first>
  <action>Create `docs/hermes-compatibility.md` with the exact heading `# Hermes Compatibility and Guardrails` and these sections: `## Compatibility Matrix`, `## Known Gaps`, `## Adapter Guardrails`, and `## Phase Mapping`. In `## Compatibility Matrix`, add a markdown table with these exact rows in the first column: `Global Hermes install`, `Project-linked external_dirs mode`, `Hermes runtime selection in installer`, `Command discovery for /gsd-*`, `Core workflow execution`, `Update / uninstall / doctor`, `Upstream sync routine`, and `Native local Hermes install mode`. Add a `Phase 1 status` column that uses only these exact values: `not implemented`, `planned`, `documented boundary`, and `out of scope`. Mark `Project-linked external_dirs mode` as `documented boundary`, `Upstream sync routine` as `documented boundary`, `Native local Hermes install mode` as `out of scope`, and the remaining future runtime features as `planned` or `not implemented` without claiming they already work. In `## Known Gaps`, add bullets that contain the exact phrase `known gap` and explicitly say there is no upstream import yet, Hermes runtime support is not implemented yet, and Node 22+ is not installed locally for upstream package scripts.</action>
  <verify>
    <automated>test -f docs/hermes-compatibility.md && rg -n "# Hermes Compatibility and Guardrails|## Compatibility Matrix|Global Hermes install|Project-linked external_dirs mode|Command discovery for /gsd-\\*|Native local Hermes install mode|not implemented|documented boundary|out of scope|known gap|Node 22\\+ is not installed locally" docs/hermes-compatibility.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/hermes-compatibility.md` contains `# Hermes Compatibility and Guardrails`
    - `docs/hermes-compatibility.md` contains `## Compatibility Matrix`
    - `docs/hermes-compatibility.md` contains `Project-linked external_dirs mode`
    - `docs/hermes-compatibility.md` contains `Native local Hermes install mode`
    - `docs/hermes-compatibility.md` contains `not implemented`, `documented boundary`, and `out of scope`
    - `docs/hermes-compatibility.md` contains `known gap`
    - `docs/hermes-compatibility.md` contains `Node 22+ is not installed locally`
  </acceptance_criteria>
  <done>`docs/hermes-compatibility.md` exists with a concrete status matrix that tells the truth about what Phase 1 does and does not deliver.</done>
</task>

<task type="auto">
  <name>Task 2: Encode adapter guardrails and phase routing rules</name>
  <files>docs/hermes-compatibility.md</files>
  <read_first>docs/hermes-compatibility.md, docs/fork-ownership.md, docs/upstream-sync.md, .planning/phases/01-fork-foundation/01-CONTEXT.md, .planning/phases/01-fork-foundation/01-RESEARCH.md, AGENTS.md</read_first>
  <action>Extend `docs/hermes-compatibility.md` so `## Adapter Guardrails` contains a bullet list with these exact rules: `Prefer adapter or shim changes over workflow rewrites.`, `Do not claim a native local Hermes install mode.`, `Keep Hermes patches concentrated in installer, runtime conversion, compatibility, documentation, and test layers.`, `If a change touches get-shit-done/workflows/ broadly, stop and justify it against D-02 before implementation.`, and `Use merge history from upstream/main to review Hermes drift before broadening a patch.` In `## Phase Mapping`, add a markdown table that maps the matrix rows to later phases using these exact strings at least once: `Phase 2`, `Phase 3`, `Phase 4`, `Phase 5`, and `Phase 6`. Also include the ownership labels `Upstream base`, `Hermes adapter seam`, and `Downstream governance` so the guardrails tie back to `docs/fork-ownership.md`.</action>
  <verify>
    <automated>rg -n "## Adapter Guardrails|Prefer adapter or shim changes over workflow rewrites|Do not claim a native local Hermes install mode|installer, runtime conversion, compatibility, documentation, and test layers|get-shit-done/workflows/|merge history from upstream/main|## Phase Mapping|Phase 2|Phase 3|Phase 4|Phase 5|Phase 6|Upstream base|Hermes adapter seam|Downstream governance" docs/hermes-compatibility.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/hermes-compatibility.md` contains `## Adapter Guardrails`
    - `docs/hermes-compatibility.md` contains `Prefer adapter or shim changes over workflow rewrites.`
    - `docs/hermes-compatibility.md` contains `Do not claim a native local Hermes install mode.`
    - `docs/hermes-compatibility.md` contains `get-shit-done/workflows/`
    - `docs/hermes-compatibility.md` contains `merge history from upstream/main`
    - `docs/hermes-compatibility.md` contains `Phase 2`, `Phase 3`, `Phase 4`, `Phase 5`, and `Phase 6`
    - `docs/hermes-compatibility.md` contains `Upstream base`, `Hermes adapter seam`, and `Downstream governance`
  </acceptance_criteria>
  <done>`docs/hermes-compatibility.md` includes explicit guardrails and phase routing that future Hermes work can follow without re-interpreting Phase 1 decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| governance docs -> future Hermes implementation | Later phases will use the compatibility matrix and guardrails as the truth source for where Hermes patches may land |
| ownership/sync docs -> compatibility matrix | The new matrix must stay consistent with the ownership and merge rules already established in Plans 01 and 02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | S | docs/hermes-compatibility.md | mitigate | Use explicit status labels `not implemented`, `documented boundary`, and `out of scope` so the doc cannot overstate Hermes parity |
| T-01-08 | T | docs/hermes-compatibility.md | mitigate | Bake the exact adapter guardrail sentences into the file, including `get-shit-done/workflows/` and `merge history from upstream/main`, to prevent later expansion into broad workflow rewrites |
| T-01-09 | R | docs/hermes-compatibility.md | mitigate | Include the phase mapping table and ownership labels `Upstream base`, `Hermes adapter seam`, and `Downstream governance` so future patch placement remains reviewable against Phase 1 decisions |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `test -f docs/hermes-compatibility.md`
- [ ] `rg -n "Project-linked external_dirs mode|Native local Hermes install mode|not implemented|documented boundary|out of scope|known gap|Node 22\\+ is not installed locally" docs/hermes-compatibility.md`
- [ ] `rg -n "Prefer adapter or shim changes over workflow rewrites|Do not claim a native local Hermes install mode|get-shit-done/workflows/|merge history from upstream/main|Phase 2|Phase 3|Phase 4|Phase 5|Phase 6|Upstream base|Hermes adapter seam|Downstream governance" docs/hermes-compatibility.md`
</verification>

<success_criteria>

- `docs/hermes-compatibility.md` states the current Hermes support boundary truthfully and names known gaps explicitly
- The guardrails point future work toward adapter seams and away from broad workflow rewrites
- The compatibility matrix, ownership map, and sync runbook use consistent language across ARCH-01 and ARCH-02
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-foundation/01-03-SUMMARY.md`
</output>
