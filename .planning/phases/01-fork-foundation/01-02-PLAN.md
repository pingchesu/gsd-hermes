---
phase: 01-fork-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .git/config
  - docs/upstream-sync.md
autonomous: true
requirements:
  - ARCH-02
user_setup: []
must_haves:
  truths:
    - "The fork repository has a real `upstream` remote pointing at `gsd-build/get-shit-done` while preserving `origin` as the fork remote."
    - "Maintainers can follow a documented first-import flow and a separate steady-state merge flow without guessing."
  artifacts:
    - path: .git/config
      provides: "configured upstream remote"
      contains: 'url = https://github.com/gsd-build/get-shit-done.git'
    - path: docs/upstream-sync.md
      provides: "remote, merge, and patch-discipline runbook"
      contains: "# Upstream Sync Workflow"
  key_links:
    - from: .git/config
      to: docs/upstream-sync.md
      via: "documented remote name and URL"
      pattern: "git remote add upstream https://github.com/gsd-build/get-shit-done\\.git"
    - from: docs/upstream-sync.md
      to: main branch history
      via: "merge policy"
      pattern: "git merge --allow-unrelated-histories upstream/main|git merge upstream/main"
---

<objective>
Establish the repo remote configuration and the written upstream merge discipline for first import and steady-state sync.

Purpose: Satisfy ARCH-02 by making the fork’s remote topology and merge-only sync routine explicit and reproducible.
Output: `.git/config` update for `upstream` and `docs/upstream-sync.md`
</objective>

<execution_context>
@/home/whiskey/.codex/get-shit-done/workflows/execute-plan.md
@/home/whiskey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-fork-foundation/01-CONTEXT.md
@.planning/phases/01-fork-foundation/01-RESEARCH.md
@.planning/phases/01-fork-foundation/01-VALIDATION.md
@.planning/phases/01-fork-foundation/01-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure the upstream remote without altering origin</name>
  <files>.git/config</files>
  <read_first>.git/config, .planning/phases/01-fork-foundation/01-CONTEXT.md, .planning/phases/01-fork-foundation/01-RESEARCH.md</read_first>
  <action>Inspect the current git remotes. If `upstream` is missing, run `git remote add upstream https://github.com/gsd-build/get-shit-done.git`. If `upstream` already exists but points anywhere else, run `git remote set-url upstream https://github.com/gsd-build/get-shit-done.git`. Do not rename or remove `origin`. Do not add any extra remotes. After the update, verify with `git remote get-url upstream` and `git remote -v` so the repo state matches D-04 exactly.</action>
  <verify>
    <automated>git remote get-url upstream | rg "^https://github.com/gsd-build/get-shit-done\\.git$" && git remote -v | rg "^origin|^upstream"</automated>
  </verify>
  <acceptance_criteria>
    - `git remote get-url upstream` prints `https://github.com/gsd-build/get-shit-done.git`
    - `git remote -v` shows both `origin` and `upstream`
    - `git remote -v` does not show any new remote other than the existing `origin` plus `upstream`
  </acceptance_criteria>
  <done>The local repo has a correctly configured `upstream` remote and preserves `origin` as the fork remote.</done>
</task>

<task type="auto">
  <name>Task 2: Write the merge-based upstream sync and patch-discipline runbook</name>
  <files>docs/upstream-sync.md</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-fork-foundation/01-CONTEXT.md, .planning/phases/01-fork-foundation/01-RESEARCH.md, .planning/phases/01-fork-foundation/01-VALIDATION.md, AGENTS.md</read_first>
  <action>Create `docs/upstream-sync.md` with these exact sections: `# Upstream Sync Workflow`, `## Remote Roles`, `## First Import Into This Planning-Only Repo`, `## Steady-State Merge Flow`, `## Patch Discipline`, `## Conflict Review Checklist`, and `## Explicit Non-Goals`. In `## Remote Roles`, state that `origin` is the fork remote and `upstream` is `gsd-build/get-shit-done` per D-04. In `## First Import Into This Planning-Only Repo`, include this exact command sequence on separate fenced-code lines: `git remote add upstream https://github.com/gsd-build/get-shit-done.git`, `git fetch upstream`, `git checkout main`, and `git merge --allow-unrelated-histories upstream/main`. In `## Steady-State Merge Flow`, include `git fetch upstream`, `git checkout main`, and `git merge upstream/main`, and say merges are required per D-05 and visible merge history is preserved per D-06. In `## Patch Discipline`, say Hermes-specific patches belong in installer, runtime conversion, compatibility, documentation, and test layers per D-02, and broad workflow rewrites are not allowed in Phase 1. In `## Explicit Non-Goals`, say Phase 1 does not import upstream code yet, does not run upstream package scripts because Node 22+ is not installed locally, and does not use rebase or cherry-pick-only maintenance.</action>
  <verify>
    <automated>test -f docs/upstream-sync.md && rg -n "# Upstream Sync Workflow|origin|upstream|git remote add upstream https://github.com/gsd-build/get-shit-done\\.git|git merge --allow-unrelated-histories upstream/main|git merge upstream/main|Patch Discipline|visible merge history|Node 22\\+ is not installed locally|rebase|cherry-pick-only" docs/upstream-sync.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/upstream-sync.md` contains `# Upstream Sync Workflow`
    - `docs/upstream-sync.md` contains `git remote add upstream https://github.com/gsd-build/get-shit-done.git`
    - `docs/upstream-sync.md` contains `git merge --allow-unrelated-histories upstream/main`
    - `docs/upstream-sync.md` contains `git merge upstream/main`
    - `docs/upstream-sync.md` contains `Patch Discipline`
    - `docs/upstream-sync.md` contains `visible merge history`
    - `docs/upstream-sync.md` contains `Node 22+ is not installed locally`
    - `docs/upstream-sync.md` contains `rebase` and `cherry-pick-only`
  </acceptance_criteria>
  <done>`docs/upstream-sync.md` exists with separate first-import and steady-state flows plus explicit patch-placement and non-goal rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local git config -> maintainer runbook | The written sync procedure must match the actual remote names and URLs configured in the repository |
| upstream history -> downstream fork policy | The sync doc controls how future upstream changes enter the fork and must prevent history-rewriting shortcuts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T | .git/config | mitigate | Configure the exact `upstream` URL `https://github.com/gsd-build/get-shit-done.git` and explicitly preserve `origin` so future sync commands target the correct repos |
| T-01-05 | R | docs/upstream-sync.md | mitigate | Document merge-only commands `git merge --allow-unrelated-histories upstream/main` and `git merge upstream/main` so visible history is preserved and later conflict resolution stays traceable |
| T-01-06 | S | docs/upstream-sync.md | mitigate | Call out `rebase` and `cherry-pick-only` as non-goals and tie Hermes patches back to D-02 so the runbook cannot imply unsupported maintenance patterns |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `git remote get-url upstream | rg "^https://github.com/gsd-build/get-shit-done\\.git$"`
- [ ] `git remote -v | rg "^origin|^upstream"`
- [ ] `test -f docs/upstream-sync.md && rg -n "git remote add upstream https://github.com/gsd-build/get-shit-done\\.git|git merge --allow-unrelated-histories upstream/main|git merge upstream/main|Patch Discipline|Node 22\\+ is not installed locally|rebase|cherry-pick-only" docs/upstream-sync.md`
</verification>

<success_criteria>

- The repository has the `upstream` remote required by D-04
- `docs/upstream-sync.md` gives maintainers a first-import flow and a steady-state merge flow that satisfy D-05 and D-06
- The runbook explicitly limits Hermes patches to the adapter/documentation/test seams and does not promise Node-22-dependent validation in Phase 1
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-foundation/01-02-SUMMARY.md`
</output>
