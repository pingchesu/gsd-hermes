---
phase: 01-fork-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/README.md
  - docs/fork-ownership.md
autonomous: true
requirements:
  - ARCH-01
user_setup: []
must_haves:
  truths:
    - "Maintainers can open one docs index and see the Phase 1 governance artifacts for fork layout, sync, and compatibility."
    - "Maintainers can identify whether a path belongs to the upstream base, Hermes adapter seam, or downstream governance without scanning the whole repository."
  artifacts:
    - path: docs/README.md
      provides: "Phase 1 governance docs index"
      contains: "## Phase 1 Governance Docs"
    - path: docs/fork-ownership.md
      provides: "Fork layout, ownership matrix, and change-routing rules"
      contains: "# Fork Ownership"
  key_links:
    - from: docs/README.md
      to: docs/fork-ownership.md
      via: "markdown link"
      pattern: "\\[Fork Ownership\\]\\(fork-ownership\\.md\\)"
    - from: docs/fork-ownership.md
      to: AGENTS.md
      via: "shared boundary language"
      pattern: "Hermes adapter seam|Downstream governance|upstream-owned"
---

<objective>
Publish the Phase 1 governance document set entrypoint and the fork ownership map that implements the root-preserving fork rules.

Purpose: Satisfy ARCH-01 by making path ownership and change routing explicit before any upstream import or Hermes runtime work begins.
Output: `docs/README.md` and `docs/fork-ownership.md`
</objective>

<execution_context>
@/home/whiskey/.codex/get-shit-done/workflows/execute-plan.md
@/home/whiskey/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-fork-foundation/01-CONTEXT.md
@.planning/phases/01-fork-foundation/01-RESEARCH.md
@.planning/phases/01-fork-foundation/01-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 1 governance docs index</name>
  <files>docs/README.md</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-fork-foundation/01-CONTEXT.md, .planning/phases/01-fork-foundation/01-RESEARCH.md, AGENTS.md</read_first>
  <action>Create the `docs/` directory if it does not exist, then create `docs/README.md` with the exact heading `# Governance Docs` and a section `## Phase 1 Governance Docs`. In that section, add a markdown table whose first column uses these exact link labels and targets: `[Fork Ownership](fork-ownership.md)`, `[Upstream Sync Workflow](upstream-sync.md)`, and `[Hermes Compatibility and Guardrails](hermes-compatibility.md)`. Describe them as the ownership map, merge-based upstream sync runbook, and compatibility/guardrails matrix respectively. Add a `## Phase 1 Scope` section that says Phase 1 is documentation-first per D-07, does not implement Hermes runtime support per D-08, and exists to prepare later installer/runtime phases without repo-wide restructuring per D-03. Do not add implementation claims or runtime instructions outside those three governance docs.</action>
  <verify>
    <automated>test -f docs/README.md && rg -n "## Phase 1 Governance Docs|\\[Fork Ownership\\]\\(fork-ownership\\.md\\)|\\[Upstream Sync Workflow\\]\\(upstream-sync\\.md\\)|\\[Hermes Compatibility and Guardrails\\]\\(hermes-compatibility\\.md\\)|documentation-first|does not implement Hermes runtime support|repo-wide restructuring" docs/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/README.md` contains `# Governance Docs`
    - `docs/README.md` contains `## Phase 1 Governance Docs`
    - `docs/README.md` contains `[Fork Ownership](fork-ownership.md)`, `[Upstream Sync Workflow](upstream-sync.md)`, and `[Hermes Compatibility and Guardrails](hermes-compatibility.md)`
    - `docs/README.md` contains `documentation-first`, `does not implement Hermes runtime support`, and `repo-wide restructuring`
  </acceptance_criteria>
  <done>`docs/README.md` exists and enumerates the three Phase 1 governance artifacts with explicit Phase 1 scope limits.</done>
</task>

<task type="auto">
  <name>Task 2: Write the fork ownership map with exact path boundaries</name>
  <files>docs/fork-ownership.md</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/phases/01-fork-foundation/01-CONTEXT.md, .planning/phases/01-fork-foundation/01-RESEARCH.md, .planning/phases/01-fork-foundation/01-PATTERNS.md, AGENTS.md</read_first>
  <action>Create `docs/fork-ownership.md` with these exact sections: `# Fork Ownership`, `## Goal`, `## Root-Preserving Layout`, `## Path Ownership Matrix`, `## Change Routing Rules`, and `## Phase 1 Boundaries`. In `## Root-Preserving Layout`, state that the upstream `get-shit-done` layout will remain at repo root per D-01 and Phase 1 will avoid repo-wide restructuring per D-03. In `## Path Ownership Matrix`, add a markdown table that includes these exact path rows: `.planning/`, `AGENTS.md`, `agents/`, `commands/`, `get-shit-done/`, `hooks/`, `sdk/`, `tests/`, `docs/`, and `bin/install.js`. Assign each row to one of these exact owners: `Upstream base`, `Hermes adapter seam`, or `Downstream governance`. In `## Change Routing Rules`, include these exact sentences: `If a change touches workflow behavior broadly, default to "upstream-owned".`, `If a change adds runtime path, config, or conversion logic, default to "Hermes adapter seam".`, and `If a change records project policy, compatibility truth, or sync rules, default to "Downstream governance".` In `## Phase 1 Boundaries`, state that Hermes runtime implementation is intentionally out of scope for this phase per D-08.</action>
  <verify>
    <automated>test -f docs/fork-ownership.md && rg -n "# Fork Ownership|## Path Ownership Matrix|\\.planning/|AGENTS\\.md|agents/|commands/|get-shit-done/|hooks/|sdk/|tests/|docs/|bin/install\\.js|upstream-owned|Hermes adapter seam|Downstream governance|out of scope" docs/fork-ownership.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/fork-ownership.md` contains `# Fork Ownership`
    - `docs/fork-ownership.md` contains `## Path Ownership Matrix`
    - `docs/fork-ownership.md` contains rows for `.planning/`, `AGENTS.md`, `agents/`, `commands/`, `get-shit-done/`, `hooks/`, `sdk/`, `tests/`, `docs/`, and `bin/install.js`
    - `docs/fork-ownership.md` contains `If a change touches workflow behavior broadly, default to "upstream-owned".`
    - `docs/fork-ownership.md` contains `If a change adds runtime path, config, or conversion logic, default to "Hermes adapter seam".`
    - `docs/fork-ownership.md` contains `If a change records project policy, compatibility truth, or sync rules, default to "Downstream governance".`
  </acceptance_criteria>
  <done>`docs/fork-ownership.md` exists with a concrete ownership matrix and exact change-routing rules that implement D-01, D-02, and D-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifacts -> governance docs | Locked decisions from `.planning/` are being translated into maintainer-facing docs and must not drift |
| runtime contract -> ownership map | `AGENTS.md` and the new ownership doc must describe the same boundaries so future work routes changes correctly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | S | docs/fork-ownership.md | mitigate | Use exact path rows and exact ownership labels `Upstream base`, `Hermes adapter seam`, and `Downstream governance` so the doc cannot imply a different fork model |
| T-01-02 | T | docs/README.md | mitigate | Constrain the index to the three Phase 1 governance docs and explicitly state `documentation-first` plus `does not implement Hermes runtime support` to prevent scope creep |
| T-01-03 | R | docs/fork-ownership.md | mitigate | Embed the literal D-01/D-02/D-03 routing rules in the doc so later maintainers can trace why a path belongs in a given layer |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `test -f docs/README.md && test -f docs/fork-ownership.md`
- [ ] `rg -n "fork-ownership\\.md|upstream-sync\\.md|hermes-compatibility\\.md|documentation-first|does not implement Hermes runtime support" docs/README.md`
- [ ] `rg -n "\\.planning/|AGENTS\\.md|bin/install\\.js|upstream-owned|Hermes adapter seam|Downstream governance" docs/fork-ownership.md`
</verification>

<success_criteria>

- `docs/README.md` exists and points maintainers at the three Phase 1 governance docs
- `docs/fork-ownership.md` makes ARCH-01 mechanically answerable with exact path ownership labels
- The written guidance preserves D-01, D-02, D-03, D-07, and D-08 without claiming runtime support
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-foundation/01-01-SUMMARY.md`
</output>
